# RAG-Lab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eine zweisprachige, statische Lernumgebung (10 Labs, 55 Übungen) zu Retrieval-Augmented Generation und Sprachmodellen, in der das Embedding-Modell des Fallbeispiels im Browser läuft und alle Zahlen aus dem Fallbeispiel reproduzierbar erzeugt werden.

**Architecture:** Fork der PROM-Laufzeit (`rag.js`/`rag.css`) ohne BPMN, plus reine Bewertungsmodule (`pruefung.js`, `chunking.js`), ein transformers.js-Wrapper (`modell.js`), 16 Werkzeug-Module unter `assets/werkzeuge/`, ein nachgebildetes Terminal (`terminal.js`, aus PITM) und eine Python-Datenpipeline unter `tools/`, die `data/*.json` aus dem Fallbeispiel-Repo erzeugt. Übungen liegen als JSON mit Platzhaltern im HTML; `tools/verify.mjs` und `node --test` sichern alles ohne Browser ab.

**Tech Stack:** HTML/CSS/ES-Module ohne Build; `@huggingface/transformers` 4.x (vendored) mit `Xenova/multilingual-e5-small` q8 vom Hub; Python 3.12 mit llama-index 0.14, sentence-transformers 5.6, torch 2.12, scikit-learn, PyMuPDF (Umgebung `/Users/robert/miniforge3/bin/python3.12`); Node 20+ für Tests; mermaid-cli für Diagramme; GitHub Pages.

**Spec:** `UMSETZUNGSPLAN.md` (Repo-Wurzel). Der Plan argumentiert aus dem Spec; wer eine Aufgabe ausführt, liest beide.

## Global Constraints

- Statische Seite, kein Build-Schritt, kein Server, keine Anmeldung; GitHub Pages aus `main`, Wurzel `/`, `.nojekyll` vorhanden.
- Jeder sichtbare Text zweisprachig: im HTML als `<span lang="de">…</span><span lang="en">…</span>`, im JSON als `{ "de": …, "en": … }`. `verify.mjs` erzwingt beides.
- Fachbegriffe bleiben englisch, wo sie es sind: Embedding, Chunk, Reranker, Guardrail, Token, Prompt, Cross-Encoder, Bi-Encoder, Overfetch, Streaming, Endpoint, Cache. Kein Denglisch (herunterladen, nicht downloaden). Kein Emoji; ✓ ✗ ▸ · erlaubt.
- Palette exakt: Sandbeige `#EFE7D6`, Sand hell `#F8F4EC`, Sand dunkel `#E2D6BC`, Umbra `#2E2418`, Umbra weich `#5B4E3E`, Terrakotta `#C4602A` (nie als Schriftfarbe auf Weiß), Terrakotta dunkel `#9E4A1C`, Ok `#2F7D32`, Warnung `#B23B1E`; EN-Textakzent Oliv `#6B6A1C`. Schrift Space Grotesk (Google Fonts), Monospace für Code.
- localStorage-Schlüssel: `rag:sprache`, `rag:fortschritt:<lab>`, `rag:terminal:<id>`, `rag:llm`. Events: `rag:sprache`, `rag:fortschritt`, `rag:modell`.
- Übungs-IDs `R<Lab>-<Nr>` zweistellig (`R06-03`); Lab-IDs `lab-01` … `lab-10`; Übungszahlen je Lab: 5,5,5,6,6,6,6,6,5,5 (Summe 55).
- Kein Wert von Hand in Text oder JSON: Zahlen aus dem Fallbeispiel tragen im HTML `<span data-wert="…">` und werden von `verify.mjs` gegen `data/` geprüft. Jede Zahl mit externer Quelle (Preis, Modellgröße) trägt ihr Datum im Text.
- Kein Modell-Download ohne Klick. Reranker-Scores immer als „vorberechnet“ gekennzeichnet. Jedes Werkzeug läuft ohne Hub mit `data/`-Daten.
- Fallbeispiel-Repo (Lesezugriff für `tools/*.py`): `/Users/robert/Library/CloudStorage/OneDrive-Persönlich/Vorlesungen/Datenbasierte Fallstudien/SiemensWashingMachineTroubleShooting_LocalLLM`, überschreibbar mit Umgebungsvariable `RAG_FALL`. Dessen Dateien werden nie verändert.
- Commit nach jeder Aufgabe, Nachrichten deutsch, Abschluss mit `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- Nach jedem Schreibdurchgang an Texten: Skill `/folientext-pruefen` (Aufgabe 19), Zahlangaben gegen Daten.

## Dateiplan

| Datei | Verantwortung |
|---|---|
| `index.html` | Startseite: Hero, Kacheln, Gesamtfortschritt, Erklärung des Aufbaus |
| `lab-01-sprachmodelle.html` … `lab-10-nachbauen.html` | Eine Seite je Lab: Kopf, Seitennavigation, Abschnitte, Werkzeug- und Übungsplatzhalter, Quellen |
| `assets/rag.css` | Stylesheet aus `prom.css`, Palette, plus Klassen für Werkzeuge, Terminal, Übungstypen |
| `assets/rag.js` | Laufzeit: Sprache, `LABS`, Fortschritt, Übungsboxen aller Typen, Werkzeug-Einbindung, Datenlader, Navigation |
| `assets/pruefung.js` | Reine Funktionen: Vektoren, Fehlercodes, Hybrid, Kontextauswahl, Gedeckt, Antwort-Parser, Metriken, Projektion, Prüfer je Übungstyp |
| `assets/chunking.js` | Chunking-Strategien inkl. Port der Tabellen-Explosion |
| `assets/modell.js` | transformers.js: Tokenizer, Embedding, Selbsttest, Zustand |
| `assets/terminal.js` | Nachgebildete Shell (zsh, PowerShell) für Lab 10 samt Weltzustand und Prüfprädikaten |
| `assets/werkzeuge/index.js` | Registry aller Werkzeuge |
| `assets/werkzeuge/<name>.js` | Ein lebendes Stück je Datei (16 Dateien) |
| `assets/transformers/` | Vendored transformers.js + ONNX-Runtime-WASM + Lizenz |
| `assets/diagramme/` | Aus Mermaid erzeugte SVGs, `-de`/`-en` |
| `data/uebungen/lab-XX.json` | Übungen je Lab |
| `data/chunks.json`, `chunks-ohne-praefix.json`, `handbuch.md`, `fragen.json`, `projektion.json`, `antworten.json`, `logits.json`, `tokens.json`, `seite-33.md`, `eval/*.json` | Erzeugte Daten (Aufgabe 2) |
| `diagramme/quellen/*.mmd` | Mermaid-Quellen mit `[[de|en]]`-Platzhaltern |
| `tools/*.py` | Datenpipeline gegen das Fallbeispiel |
| `tools/gen_diagramme.mjs` | Mermaid → SVG in beiden Sprachen |
| `tools/verify.mjs`, `tools/pruefung.test.mjs`, `tools/pruefung/durchlauf.js`, `tools/pruefung/audit.js` | Abnahme |
| `README.md`, `QUELLEN.md`, `PRUEFBERICHT.md`, `package.json`, `.nojekyll`, `.gitignore` | Repo |

---

### Task 1: Grundgerüst und Palette

**Files:**
- Create: `assets/rag.css`, `assets/rag.js`, `index.html`, `package.json`, `.nojekyll`, `tools/palette.html`, `tools/verify.mjs`, `data/uebungen/lab-01.json` … `lab-10.json` (leere Arrays), zehn Lab-Seiten als Rohgerüst
- Read: `../PROM/assets/prom.css`, `../PROM/assets/prom.js`, `../PROM/index.html`, `../PROM/lab-10-ki.html`

**Interfaces:**
- Produces: `assets/rag.js` exportiert `LABS`, `aktuelleSprache()`, `txt(o)`, `starteUebersicht({basis})`, `starteLab({lab, basis})`, `el(tag, klasse, text)`, `html(tag, klasse, inhalt)`, `status(ziel, art, titel, text)`, `merkeFortschritt(lab, id)`, `ladeFortschritt(lab)`; Übungstyp `quiz` funktioniert.
- Produces: `LABS`-Einträge `{ id, nr, datei, anzahl, titel, voraussetzung, ziel, dauer }` (Felder wie PROM).

- [ ] **Step 1: Stylesheet forken und umfärben**

```bash
cd "/Users/robert/Library/CloudStorage/OneDrive-Persönlich/Vorlesungen/Lernumgebungen/RAG-Lab"
mkdir -p assets/werkzeuge assets/diagramme assets/transformers data/uebungen data/eval diagramme/quellen tools/pruefung
cp ../PROM/assets/prom.css assets/rag.css
```

Dann in `assets/rag.css` den Kopfkommentar und den `:root`-Block ersetzen:

```css
/* ===========================================================================
   RAG-Lab · Gemeinsames Stylesheet
   Retrieval-Augmented Generation · THWS Business School

   Abgeleitet vom PROM-Lab-Stylesheet (gleiche Formsprache). Die Farben
   folgen der Vorgabe „helles Sandbeige":

     Sandbeige  #EFE7D6  traegt die Flaechen: Hero, weiche Abschnitte, Chips.
                         Kann keine weisse Schrift tragen; Umbra darauf 12,6:1.
     Umbra      #2E2418  Text, Ueberschriften, Buttons mit weisser Schrift (15,0:1).
     Terrakotta #C4602A  signalisiert: Akzentlinie, Badges, Fortschritt,
                         Kachelnummern. Als Schriftfarbe auf Weiss ungeeignet
                         (3,9:1); Textakzente nehmen das dunklere #9E4A1C (6,2:1).
   =========================================================================== */

:root {
  --primary:    #2E2418;   /* Umbra: Flaechen mit weisser Schrift, Buttons */
  --primary-d:  #9E4A1C;   /* Textakzent, Links (6,2:1 auf Weiss) */
  --primary-l:  #EFE7D6;   /* Sandbeige: Hinweiskaesten, aktive Navigation, Hero */
  --signal:     #C4602A;   /* Terrakotta: Akzente, nie als Schriftfarbe */
  --signal-l:   #F5E3D6;   /* aufgehelltes Terrakotta: Chips, Fortschritt */
  --accent:     #9E4A1C;
  --on-primary: #FFFFFF;
  --ink:        #2E2418;
  --ink-soft:   #5B4E3E;
  --line:       #E2D6BC;   /* Sand dunkel: Haarlinien */
  --bg:         #FFFFFF;
  --bg-soft:    #F8F4EC;   /* Sand hell */
  --sand:       #EFE7D6;
  --ok:         #2F7D32;
  --ok-bg:      #F1F8F1;
  --warn:       #B23B1E;
  --warn-bg:    #FDF3F0;
  --mono: 'SFMono-Regular', 'SF Mono', Menlo, Consolas, 'Liberation Mono', monospace;
  --sans: 'Space Grotesk', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}
[data-lang="en"] { --accent: #6B6A1C; --primary-l: #EEE9D9; }
```

Der Hero wird sandfarben mit dunkler Schrift (PROM hat dunklen Hero mit weißer Schrift):

```css
.hero { background: var(--sand); color: var(--ink); }
.hero-tag, .hero-badge { color: var(--ink); border-color: rgba(46,36,24,0.25); }
.hero .btn.solid { background: var(--primary); color: var(--on-primary); }
.hero .btn { border-color: var(--ink); color: var(--ink); }
```

(Diese vier Regeln ans Ende der Datei anhängen; sie überschreiben die PROM-Werte. Beim Ausführen prüfen, welche Selektoren PROM für Hero-Text nutzt, und die Farbwerte darin auf `var(--ink)` setzen, bis `grep -n "#fff\|white" assets/rag.css` im Hero-Block nichts mehr liefert.)

Alle Blöcke entfernen, deren Kommentarkopf `Modell`, `Editor`, `bpmn`, `Marken` oder `Fehlerliste` nennt. Danach:

```bash
grep -c "bpmn" assets/rag.css   # Erwartet: 0
```

- [ ] **Step 2: Palette als Musterseite**

`tools/palette.html` (nur zur Ansicht, nicht verlinkt):

```html
<!DOCTYPE html><html lang="de"><head><meta charset="UTF-8"><title>RAG-Lab · Palette</title>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<link rel="stylesheet" href="../assets/rag.css">
<style>.probe{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:1rem;padding:5rem 2rem 2rem}
.probe div{padding:1rem;border-radius:8px;border:1px solid var(--line)}</style></head><body>
<header class="header"><div class="header-left"><a class="header-logo" href="#"><span class="mark">RAG</span>-Lab</a></div></header>
<section class="hero"><div class="hero-inner"><span class="hero-tag">Lernumgebung · RAG</span><h1 class="hero-title">RAG</h1>
<p class="hero-subtitle">Antworten, die im Dokument stehen</p><div class="cta-row"><a class="btn solid" href="#">Mit Lab 01 beginnen</a><a class="btn" href="#">Alle Labs</a></div></div></section>
<div class="probe">
<div style="background:#EFE7D6">Sandbeige #EFE7D6 · Umbra-Text</div>
<div style="background:#F8F4EC">Sand hell #F8F4EC</div>
<div style="background:#E2D6BC">Sand dunkel #E2D6BC</div>
<div style="background:#2E2418;color:#fff">Umbra #2E2418 · weiße Schrift</div>
<div style="background:#C4602A;color:#fff">Terrakotta #C4602A (Fläche)</div>
<div style="color:#9E4A1C">Terrakotta dunkel #9E4A1C als Text</div>
<div class="concept-box">concept-box</div><div class="tip-box">tip-box</div><div class="warn-box">warn-box</div><div class="challenge-box">challenge-box</div>
<div><span class="badge">Badge</span> <div class="accent-line"></div><button class="btn-sm primary">Prüfen</button> <button class="btn-sm">Hinweis</button></div>
</div></body></html>
```

Im Browser öffnen (`preview_start` mit Konfiguration `lernumgebungen` im Elternordner, Pfad `/RAG-Lab/tools/palette.html`), Screenshot an den Nutzer senden, weiterarbeiten (kein Blockieren; Änderungswunsch wird nachgezogen).

- [ ] **Step 3: Laufzeit forken**

```bash
cp ../PROM/assets/prom.js assets/rag.js
```

In `assets/rag.js`:
1. Kopfkommentar ersetzen (RAG-Lab, Zuständigkeiten: Sprache, Übungsboxen aller Typen, Werkzeuge, Fortschritt).
2. `import { leseGraph, … } from './pruefung.js'` entfernen; stattdessen `import * as P from './pruefung.js'` (Datei entsteht in Aufgabe 3; bis dahin eine leere `assets/pruefung.js` mit `export {}` anlegen) und `import { WERKZEUGE } from './werkzeuge/index.js'` (bis Aufgabe 5 eine Datei mit `export const WERKZEUGE = {}`).
3. Alle Vorkommen `prom:` → `rag:`, `PROM` in Texten → `RAG`, ID-Muster `^P${l.nr}` → `^R${l.nr}`.
4. Entfernen: `ladeBpmn`, `E` (Erklärungen), `erklaerungFuer`, `baueModell`, `baueModellPlatzhalter`, die Zweige `uebung.typ === 'fehler'` und `'modell'` in `baueBox`, alle `T`-Einträge zu Marke/Editor/Import/Regeln (`marke`, `markeAus`, `einpassen`, `gross`, `klein`, `klickHilfe`, `herunterladen`, `importieren`, `zuklein`, `leerenFrage`, `loesungFrage`, `regeln`, `struktur`, `keineBefunde`, `fehlerWaehlen`, `regelWaehlen`, `gefunden`, `falschMarkiert`, `falscheRegel`, `nochOffen`).
5. `T.typ` ersetzen durch:

```js
typ: {
  quiz:       { de: 'Verständnis', en: 'Understanding' },
  experiment: { de: 'Experiment', en: 'Experiment' },
  zuordnen:   { de: 'Zuordnen', en: 'Matching' },
  sortieren:  { de: 'Reihenfolge', en: 'Ordering' },
  rechnen:    { de: 'Rechnen', en: 'Calculate' },
  luecken:    { de: 'Lückentext', en: 'Fill the gaps' },
  belegen:    { de: 'Belege prüfen', en: 'Check the evidence' },
  terminal:   { de: 'Kommandozeile', en: 'Command line' },
  checkliste: { de: 'Checkliste', en: 'Checklist' }
}
```

6. `LABS` ersetzen (Voraussetzung/Ziel/Dauer in beiden Sprachen; Ziele aus Spec Abschnitt 2):

```js
export const LABS = [
  { id: 'lab-01', nr: '01', datei: 'lab-01-sprachmodelle.html', anzahl: 5,
    titel: { de: 'Sprachmodelle', en: 'Language models' },
    voraussetzung: { de: 'Keine. Dies ist der Anfang.', en: 'None. This is the start.' },
    ziel: { de: 'Sie können erklären, was ein Token ist, wie ein Sprachmodell das nächste Token vorhersagt und was Kontextfenster, Temperatur, Halluzination und Wissensstand bedeuten.',
            en: 'You can explain what a token is, how a language model predicts the next token, and what context window, temperature, hallucination and knowledge cutoff mean.' },
    dauer: { de: 'ca. 35 Minuten', en: 'about 35 minutes' } },
  { id: 'lab-02', nr: '02', datei: 'lab-02-warum-rag.html', anzahl: 5,
    titel: { de: 'Warum RAG?', en: 'Why RAG?' },
    voraussetzung: { de: 'Lab 01.', en: 'Lab 01.' },
    ziel: { de: 'Sie können die Grenzen eines reinen Sprachmodells benennen, RAG gegen Fine-Tuning, Long Context und Prompting abgrenzen und Kosten- und Datenschutzgründe für lokale Modelle nennen.',
            en: 'You can name the limits of a bare language model, distinguish RAG from fine-tuning, long context and prompting, and give cost and privacy reasons for local models.' },
    dauer: { de: 'ca. 30 Minuten', en: 'about 30 minutes' } },
  { id: 'lab-03', nr: '03', datei: 'lab-03-aufbereiten.html', anzahl: 5,
    titel: { de: 'Dokumente aufbereiten', en: 'Preparing documents' },
    voraussetzung: { de: 'Lab 02.', en: 'Lab 02.' },
    ziel: { de: 'Sie können ein PDF in Markdown überführen, OCR-Artefakte erkennen und Seitenmarker und Tabellen als Provenienz nutzen.',
            en: 'You can convert a PDF to Markdown, spot OCR artefacts and use page markers and tables as provenance.' },
    dauer: { de: 'ca. 30 Minuten', en: 'about 30 minutes' } },
  { id: 'lab-04', nr: '04', datei: 'lab-04-chunking.html', anzahl: 6,
    titel: { de: 'Chunking', en: 'Chunking' },
    voraussetzung: { de: 'Lab 03.', en: 'Lab 03.' },
    ziel: { de: 'Sie können Chunking-Strategien vergleichen, Größe und Überlappung in Tokens begründen und Tabellenzeilen und Metadaten richtig behandeln.',
            en: 'You can compare chunking strategies, justify size and overlap in tokens and handle table rows and metadata correctly.' },
    dauer: { de: 'ca. 40 Minuten', en: 'about 40 minutes' } },
  { id: 'lab-05', nr: '05', datei: 'lab-05-embeddings.html', anzahl: 6,
    titel: { de: 'Embeddings', en: 'Embeddings' },
    voraussetzung: { de: 'Lab 04.', en: 'Lab 04.' },
    ziel: { de: 'Sie können Vektoren und Kosinusähnlichkeit erklären, das query:/passage:-Präfix begründen und Embedding-Modelle nach Sprache, Dimension und Länge auswählen.',
            en: 'You can explain vectors and cosine similarity, justify the query:/passage: prefix and choose embedding models by language, dimension and length.' },
    dauer: { de: 'ca. 45 Minuten', en: 'about 45 minutes' } },
  { id: 'lab-06', nr: '06', datei: 'lab-06-retrieval.html', anzahl: 6,
    titel: { de: 'Retrieval und Reranking', en: 'Retrieval and reranking' },
    voraussetzung: { de: 'Lab 05.', en: 'Lab 05.' },
    ziel: { de: 'Sie können Top-k-Suche, Hybrid mit Schlüsselwörtern, BM25 und RRF einordnen und Cross-Encoder-Reranking und Relevanzschwelle erklären.',
            en: 'You can place top-k search, hybrid search with keywords, BM25 and RRF, and explain cross-encoder reranking and the relevance threshold.' },
    dauer: { de: 'ca. 45 Minuten', en: 'about 45 minutes' } },
  { id: 'lab-07', nr: '07', datei: 'lab-07-generierung.html', anzahl: 6,
    titel: { de: 'Prompt und Generierung', en: 'Prompt and generation' },
    voraussetzung: { de: 'Lab 06.', en: 'Lab 06.' },
    ziel: { de: 'Sie können einen Systemprompt für belegte Antworten schreiben, Kontext als Daten übergeben, strukturierte Ausgabe erzwingen und Tokenbudget und Streaming einordnen.',
            en: 'You can write a system prompt for grounded answers, pass context as data, enforce structured output and place token budget and streaming.' },
    dauer: { de: 'ca. 45 Minuten', en: 'about 45 minutes' } },
  { id: 'lab-08', nr: '08', datei: 'lab-08-evaluation.html', anzahl: 6,
    titel: { de: 'Evaluation und Guardrails', en: 'Evaluation and guardrails' },
    voraussetzung: { de: 'Lab 06.', en: 'Lab 06.' },
    ziel: { de: 'Sie können Hit@k, MRR und Abdeckung berechnen, Negativtests und Ablationen lesen und die Grenzen von Stichwort-Proxys und Belegtreue erklären.',
            en: 'You can compute hit@k, MRR and coverage, read negative tests and ablations, and explain the limits of keyword proxies and faithfulness.' },
    dauer: { de: 'ca. 40 Minuten', en: 'about 40 minutes' } },
  { id: 'lab-09', nr: '09', datei: 'lab-09-architektur.html', anzahl: 5,
    titel: { de: 'Architektur, Betrieb, Sicherheit', en: 'Architecture, operations, security' },
    voraussetzung: { de: 'Lab 07.', en: 'Lab 07.' },
    ziel: { de: 'Sie können die Komponenten eines RAG-Systems und den API-Vertrag beschreiben, Cache-Invalidierung, Schlüsselschutz und Loopback-Betrieb begründen und Kosten lokal gegen Cloud schätzen.',
            en: 'You can describe the components of a RAG system and its API contract, justify cache invalidation, key protection and loopback operation, and estimate local versus cloud cost.' },
    dauer: { de: 'ca. 35 Minuten', en: 'about 35 minutes' } },
  { id: 'lab-10', nr: '10', datei: 'lab-10-nachbauen.html', anzahl: 5,
    titel: { de: 'Nachbauen', en: 'Rebuilding it' },
    voraussetzung: { de: 'Lab 09.', en: 'Lab 09.' },
    ziel: { de: 'Sie können das Fallbeispiel auf dem eigenen Rechner einrichten, die Phasen des Aufbaus nachvollziehen und die typischen Fehlerbilder beheben.',
            en: 'You can set up the case study on your own computer, retrace the phases of its construction and fix the typical failure modes.' },
    dauer: { de: 'ca. 45 Minuten', en: 'about 45 minutes' } }
]
```

7. In `baueBox`: der `quiz`-Zweig bleibt; `erkunden` wird zu `experiment` (siehe Aufgabe 4). Vorläufig: `if (uebung.typ === 'quiz' || uebung.typ === 'experiment')` ohne Modell.
8. `starteLab` und `starteUebersicht` behalten; den Aufruf `baueModellPlatzhalter()` durch `baueWerkzeuge(document, ctx)` ersetzen (Aufgabe 5 füllt die Funktion; bis dahin leer).

- [ ] **Step 4: Startseite und Rohgerüste**

`index.html` aus `../PROM/index.html` kopieren; Titel `RAG-Lab · Retrieval-Augmented Generation · THWS Business School`; alle bpmn-Stylesheets entfernen; Favicon-SVG: `rect fill='%232E2418'`, Text `R` in `%23C4602A`; Hero-Texte (endgültige Texte in Aufgabe 18), zehn Kacheln entsprechend `LABS`, Stats-Leiste `10 Labs · 55 Übungen · 346 Chunks · 1 Modell im Browser`; Abschnitt „Wie das hier funktioniert“ mit drei `concept-box`: „Das Modell läuft bei Ihnen“, „Echte Zahlen“, „Eigenes Modell anschließen“; Fußzeile `THWS Business School · RAG-Lab · Prof. Dr. Robert Butscher`; Modulscript `import { starteUebersicht } from './assets/rag.js'; starteUebersicht({ basis: '.' })`.

Je Lab eine Datei nach dem Muster von `../PROM/lab-10-ki.html` (Kopfleiste, Sidebar, `lab-header` mit `lab-num-big`, `lab-title`, `lab-intro`, `lernziele`, `main.main-content` mit `section-block`-Abschnitten `#einordnung`, `#uebungen`, `#zusammenfassung`, `#quellen`, `nav.nav-bottom`, Fußzeile, `starteLab({ lab: 'lab-0N', basis: '.' })`). Übungsplatzhalter `<div data-uebung="R0N-01"></div>` entsprechend `LABS.anzahl`. Übungsdateien als `[]`.

`package.json`:

```json
{ "name": "rag-lab", "private": true, "type": "module",
  "scripts": { "test": "node --test tools/pruefung.test.mjs", "verify": "node tools/verify.mjs" } }
```

`.nojekyll` leer anlegen.

- [ ] **Step 5: Erster Abnahmelauf**

`tools/verify.mjs` (Grundfassung; Aufgabe 3 und 8 erweitern sie):

```js
#!/usr/bin/env node
/**
 * verify.mjs - Abnahmelauf ohne Browser.
 *   1. LABS ↔ HTML-Dateien ↔ JSON-Übungsdateien (Anzahl, Platzhalter beidseitig)
 *   2. Jede Übung: Titel/Auftrag/Hinweis/Rückmeldung in DE und EN, Typ bekannt, typspezifische Felder gültig
 *   3. Werkzeug-Platzhalter kennen ihr Werkzeug, data-parameter ist JSON
 *   4. Datendateien vorhanden und formgerecht
 *   5. data-wert-Marker im HTML entsprechen den Daten
 *   6. Keine Platzhaltertexte (TODO, TBD, Lorem)
 */
import { readFileSync, existsSync, readdirSync } from 'node:fs'
import { fileURLToPath } from 'node:url'
import { dirname, join } from 'node:path'
const HIER = dirname(fileURLToPath(import.meta.url)); const ROOT = join(HIER, '..')
const { LABS } = await import('../assets/rag.js')
let fehler = 0
const meld = (ok, text) => { console.log((ok ? '  ok   ' : '  FEHL ') + text); if (!ok) fehler++ }
const zwei = (o) => !!(o && typeof o === 'object' && typeof o.de === 'string' && typeof o.en === 'string' && o.de.trim() && o.en.trim())
const TYPEN = ['quiz', 'experiment', 'zuordnen', 'sortieren', 'rechnen', 'luecken', 'belegen', 'terminal', 'checkliste']

console.log('Labs und Übungen')
for (const lab of LABS) {
  const html = existsSync(join(ROOT, lab.datei)) ? readFileSync(join(ROOT, lab.datei), 'utf8') : ''
  meld(!!html, `${lab.id}: ${lab.datei} vorhanden`)
  const pfad = join(ROOT, 'data', 'uebungen', lab.id + '.json')
  const uebungen = existsSync(pfad) ? JSON.parse(readFileSync(pfad, 'utf8')) : null
  meld(Array.isArray(uebungen), `${lab.id}: Übungsdatei lesbar`)
  if (!uebungen) continue
  meld(lab.anzahl === uebungen.length, `${lab.id}: LABS.anzahl ${lab.anzahl} = ${uebungen.length} Übungen`)
  const platzhalter = [...html.matchAll(/data-uebung="([^"]+)"/g)].map(m => m[1])
  for (const p of platzhalter) meld(uebungen.some(u => u.id === p), `${p}: Platzhalter hat eine Übung`)
  uebungen.forEach((u, i) => {
    meld(u.id === `R${lab.nr}-${String(i + 1).padStart(2, '0')}`, `${u.id}: ID folgt R${lab.nr}-NN in Reihenfolge`)
    meld(platzhalter.includes(u.id), `${u.id}: Platzhalter im HTML`)
    meld(TYPEN.includes(u.typ), `${u.id}: Typ ${u.typ} bekannt`)
    meld(zwei(u.titel) && zwei(u.aufgabe), `${u.id}: Titel und Auftrag in DE und EN`)
    if (u.hinweis) meld(zwei(u.hinweis), `${u.id}: Hinweis in DE und EN`)
    if (u.rueckmeldung) meld(zwei(u.rueckmeldung), `${u.id}: Rückmeldung in DE und EN`)
  })
  meld(!/TODO|TBD|Lorem ipsum/.test(html), `${lab.id}: keine Platzhaltertexte`)
}
console.log(fehler ? `\n${fehler} Befund(e).` : '\nAlles in Ordnung.')
process.exit(fehler ? 1 : 0)
```

Run: `node tools/verify.mjs` — Expected: alle Zeilen `ok` (Übungsdateien leer, `anzahl` stimmt deshalb noch nicht: für diesen ersten Lauf `lab.anzahl === uebungen.length` als `FEHL` erwartet, 10 Befunde). Das ist der gewollte rote Ausgangszustand.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "Grundgerüst: Stylesheet, Laufzeit, Startseite, Lab-Rohgerüste, verify

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: Datenpipeline gegen das Fallbeispiel

**Files:**
- Create: `tools/fall.py`, `tools/export_chunks.py`, `tools/embed_fragen.py`, `tools/projektion.py`, `tools/tokens.py`, `tools/logits.py`, `tools/aufzeichnen.py`, `tools/seite.py`, `tools/daten.sh`
- Create (erzeugt): `data/chunks.json`, `data/chunks-ohne-praefix.json`, `data/handbuch.md`, `data/seite-33.md`, `data/eval/vector.json`, `data/eval/hybrid-no-rerank.json`, `data/eval/hybrid-rerank.json`, `data/eval/negative-checks.json`, `data/eval/questions.json`, `data/fragen.json`, `data/projektion.json`, `data/tokens.json`, `data/logits.json`, `data/antworten.json`, `assets/seite-33.png`
- Modify: `tools/verify.mjs` (Datenprüfung)

**Interfaces:**
- Produces: die JSON-Formen unten; jede spätere Aufgabe liest genau diese Felder.

- [ ] **Step 1: Gemeinsame Helfer**

`tools/fall.py`:

```python
"""Pfade und Helfer der Datenpipeline. Das Fallbeispiel wird nur gelesen."""
from __future__ import annotations
import json, os, sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
DATA = ROOT / "data"
FALL = Path(os.environ.get("RAG_FALL", "/Users/robert/Library/CloudStorage/OneDrive-Persönlich/Vorlesungen/"
                           "Datenbasierte Fallstudien/SiemensWashingMachineTroubleShooting_LocalLLM"))
PYTHON = "/Users/robert/miniforge3/bin/python3.12"


def pruefe_fall():
    for name in ("siemens_wissen.md", "storage/docstore.json", "storage/default__vector_store.json", "rag_engine.py"):
        if not (FALL / name).exists():
            sys.exit(f"Fallbeispiel unvollständig: {FALL / name} fehlt (RAG_FALL setzen).")
    if str(FALL) not in sys.path:
        sys.path.insert(0, str(FALL))


def schreibe(name: str, obj) -> Path:
    ziel = DATA / name
    ziel.parent.mkdir(parents=True, exist_ok=True)
    ziel.write_text(json.dumps(obj, ensure_ascii=False, separators=(",", ":")), encoding="utf-8")
    print(f"{ziel.relative_to(ROOT)}: {ziel.stat().st_size // 1024} KB")
    return ziel


def lese(name: str):
    return json.loads((DATA / name).read_text(encoding="utf-8"))


def rund(v, n=4):
    return [round(float(x), n) for x in v]
```

- [ ] **Step 2: Chunks, Handbuch, Evaluationsprotokolle exportieren**

`tools/export_chunks.py`:

```python
"""Persistierten Index des Fallbeispiels nach data/chunks.json; Handbuch und Protokolle kopieren."""
import json, shutil, datetime
from fall import FALL, DATA, ROOT, pruefe_fall, schreibe, rund

pruefe_fall()
store = json.loads((FALL / "storage/default__vector_store.json").read_text(encoding="utf-8"))["embedding_dict"]
docs = json.loads((FALL / "storage/docstore.json").read_text(encoding="utf-8"))["docstore/data"]
key = (FALL / "storage/.cache_key").read_text(encoding="utf-8").strip()
chunks = []
for nr, (nid, d) in enumerate(docs.items(), start=1):
    n = d["__data__"]; md = n.get("metadata", {})
    assert nid in store, f"Vektor fehlt für {nid}"
    chunks.append({"id": f"c{nr:03d}", "nr": nr, "nodeId": nid, "abschnitt": md.get("section", ""),
                   "gruppe": md.get("context_group"), "seite": md.get("pdf_page"),
                   "text": n["text"], "v": rund(store[nid])})
assert len(chunks) == 346 and all(len(c["v"]) == 384 for c in chunks)
schreibe("chunks.json", {"modell": "intfloat/multilingual-e5-small", "dimension": 384,
                         "parserVersion": "md-v5-procedure-context", "cacheKey": key,
                         "quelle": "swrobuts/SiemensWashingMachineTroubleShooting_LocalLLM · storage/ · exportiert " + datetime.date.today().isoformat(),
                         "anzahl": len(chunks), "chunks": chunks})
shutil.copy(FALL / "siemens_wissen.md", DATA / "handbuch.md")
shutil.copy(FALL / ".audit/reparsed-page33.md", DATA / "seite-33.md")
(DATA / "eval").mkdir(exist_ok=True)
for name in ("vector.json", "hybrid-no-rerank.json", "hybrid-rerank.json", "negative-checks.json"):
    shutil.copy(FALL / "docs/evaluation" / name, DATA / "eval" / name)
shutil.copy(FALL / "eval/questions.json", DATA / "eval/questions.json")
print("Handbuch, Seite 33, fünf Protokolle kopiert.")
```

Run: `cd tools && /Users/robert/miniforge3/bin/python3.12 export_chunks.py` — Expected: `data/chunks.json: ~1400 KB`, keine Assertion.

- [ ] **Step 3: Fragenkatalog mit Vektoren, Trefferlisten, Reranker-Scores**

`tools/embed_fragen.py` (nutzt die Modelle des Fallbeispiels; erster Lauf lädt Gewichte aus dem HF-Cache):

```python
"""Fragenkatalog: Vektoren mit/ohne Präfix, Vektortreffer, Hybrid, Reranker, Kontext, Gedeckt."""
import json, re, math, time
import numpy as np, torch
from fall import FALL, pruefe_fall, schreibe, lese, rund
pruefe_fall()
import rag_engine  # aus dem Fallbeispiel
from llama_index.embeddings.huggingface import HuggingFaceEmbedding
from huggingface_hub.constants import HF_HUB_CACHE
from sentence_transformers import CrossEncoder

RETRIEVE_K, FINAL_K = rag_engine.RETRIEVE_K, rag_engine.FINAL_K
chunks = lese("chunks.json")["chunks"]
M = np.array([c["v"] for c in chunks], dtype=np.float32)          # mit passage:-Präfix (aus dem Index)
texte = {c["id"]: c["text"] for c in chunks}

mit = HuggingFaceEmbedding(model_name=rag_engine.DEFAULT_EMBED_MODEL, cache_folder=HF_HUB_CACHE,
                           query_instruction="query: ", text_instruction="passage: ")
ohne = HuggingFaceEmbedding(model_name=rag_engine.DEFAULT_EMBED_MODEL, cache_folder=HF_HUB_CACHE)
print("Chunks ohne Präfix einbetten …")
Mo = np.array([ohne.get_text_embedding(c["text"]) for c in chunks], dtype=np.float32)
schreibe("chunks-ohne-praefix.json", {"hinweis": "Dieselben 346 Chunks ohne passage:-Präfix (zum Nachstellen des Präfixfehlers)",
                                      "chunks": [{"id": c["id"], "v": rund(v)} for c, v in zip(chunks, Mo)]})

reranker = CrossEncoder(rag_engine.RERANK_MODEL, trust_remote_code=False)

def topk(q, mat, k=RETRIEVE_K):
    s = mat @ q
    idx = np.argsort(-s)[:k]
    return [{"chunk": chunks[i]["id"], "score": round(float(s[i]), 4)} for i in idx]

def hybrid(frage, vektor):
    codes = rag_engine.extract_error_codes(frage)
    if not codes: return [dict(t, exakt=False) for t in vektor]
    exakt = [{"chunk": c["id"], "score": 1.0, "exakt": True} for c in chunks if codes & rag_engine.extract_error_codes(c["text"])]
    have = {e["chunk"] for e in exakt}
    return exakt + [dict(t, exakt=False) for t in vektor if t["chunk"] not in have]

def rerank(frage, kandidaten):
    paare = [(frage, texte[k["chunk"]]) for k in kandidaten]
    scores = reranker.predict(paare, activation_fn=torch.nn.Sigmoid())
    out = [{"chunk": k["chunk"], "score": round(float(s), 4), "exakt": k.get("exakt", False)} for k, s in zip(kandidaten, scores)]
    return sorted(out, key=lambda x: -x["score"])

def kontext(sortiert):
    top = sortiert[:FINAL_K]
    if not top: return []
    boden = max(rag_engine.GUARDRAIL_MIN_SCORE, top[0]["score"] * rag_engine.CONTEXT_SCORE_RATIO)
    return [k["chunk"] for k in top if k["score"] >= boden]

def gedeckt(frage, ctx_ids, sortiert):
    ok = bool(ctx_ids) and sortiert[0]["score"] >= rag_engine.GUARDRAIL_MIN_SCORE
    asked = rag_engine.extract_error_codes(frage)
    known = set().union(*(rag_engine.extract_error_codes(texte[i]) for i in ctx_ids)) if ctx_ids else set()
    return ok and (not asked or asked <= known)

eval_q = json.loads((FALL / "eval/questions.json").read_text(encoding="utf-8"))["questions"]
negativ = json.loads((FALL / "docs/evaluation/negative-checks.json").read_text(encoding="utf-8"))
katalog = [dict(id=q["id"], art="eval", frage=q["frage"], erwartet=q["expect"]) for q in eval_q]
katalog += [dict(id=f"negativ-{i+1}", art="negativ", frage=n["question"], erwartet=[]) for i, n in enumerate(negativ)]
katalog += [dict(id=k, art="beispiel", frage=f, erwartet=[]) for k, f in [
    ("laugenpumpe-reinigen", "Wie reinige ich die Laugenpumpe? Welche Sicherheitsmaßnahmen sind vorher nötig?"),
    ("wasser-schiesst", "Wasser schießt aus der Maschine"),
    ("wasser-laeuft", "Wasser läuft aus"),
    ("fehler-18-ohne-doppelpunkt", "Fehler 18 an der Waschmaschine"),
    ("e23-englisch", "What does error E:23 mean?"),
    ("pumpe-blockiert", "Die Pumpe ist blockiert"),
    ("gewicht", "Wie schwer ist die Waschmaschine?"),
    ("transport", "Wie bereite ich die Maschine für einen Umzug vor?"),
    ("schleudern", "Die Wäsche ist nach dem Schleudern noch nass"),
    ("geruch", "Die Maschine riecht unangenehm")]]

fragen = []
for q in katalog:
    t0 = time.perf_counter()
    v_mit = np.array(mit.get_query_embedding(q["frage"]), dtype=np.float32)
    v_ohne = np.array(ohne.get_query_embedding(q["frage"]), dtype=np.float32)
    vek = topk(v_mit, M); vek_ohne = topk(v_ohne, Mo)
    hyb = hybrid(q["frage"], vek); rr = rerank(q["frage"], hyb); ctx = kontext(rr)
    fragen.append({**q, "codes": sorted(rag_engine.extract_error_codes(q["frage"])),
                   "vMit": rund(v_mit), "vOhne": rund(v_ohne), "vektor": vek, "vektorOhne": vek_ohne,
                   "hybrid": hyb, "rerank": rr, "kontext": ctx, "gedeckt": gedeckt(q["frage"], ctx, rr),
                   "obersterScore": rr[0]["score"] if rr else 0.0, "sekunden": round(time.perf_counter() - t0, 2)})
    print(f"{q['id']:28s} top={fragen[-1]['obersterScore']:.4f} gedeckt={fragen[-1]['gedeckt']} kontext={len(ctx)}")

schreibe("fragen.json", {"modell": rag_engine.DEFAULT_EMBED_MODEL, "reranker": rag_engine.RERANK_MODEL,
                         "retrieveK": RETRIEVE_K, "finalK": FINAL_K, "schwelle": rag_engine.GUARDRAIL_MIN_SCORE,
                         "verhaeltnis": rag_engine.CONTEXT_SCORE_RATIO, "fragen": fragen})
```

Run: `cd tools && /Users/robert/miniforge3/bin/python3.12 embed_fragen.py` — Expected: 24 Zeilen; `fehler-e18` gedeckt True mit Kontext ≥ 1; `negativ-3` (E:180) gedeckt False; `wasser-schiesst` gedeckt False (Top-Score unter 0,15, wie im Mac-Protokoll); `kein-wassereinlauf` Top-Score nahe 0,119 (der echte Grenzfall).

Gegenprobe gegen das Protokoll: die `obersterScore`-Werte der zehn Eval-Fragen müssen mit `top_score` in `data/eval/hybrid-rerank.json` auf zwei Nachkommastellen übereinstimmen (Reranker deterministisch; Abweichung nur durch Rundung). Bei Abweichung > 0,01: Ursache klären (Modellversion), nicht überschreiben.

- [ ] **Step 4: Projektion**

`tools/projektion.py`:

```python
"""PCA der 346 Chunk-Vektoren auf zwei Dimensionen; Matrix wird mitgeliefert, damit der Browser Fragen projiziert."""
import numpy as np
from sklearn.decomposition import PCA
from fall import lese, schreibe, rund
chunks = lese("chunks.json")["chunks"]
M = np.array([c["v"] for c in chunks], dtype=np.float64)
pca = PCA(n_components=2, random_state=0).fit(M)
P = pca.transform(M)
schreibe("projektion.json", {"verfahren": "PCA (scikit-learn), zentriert, zwei Komponenten",
    "mittel": rund(pca.mean_, 5), "komponenten": [rund(k, 5) for k in pca.components_],
    "varianz": rund(pca.explained_variance_ratio_, 4),
    "punkte": [{"id": c["id"], "x": round(float(x), 4), "y": round(float(y), 4)} for c, (x, y) in zip(chunks, P)]})
```

Run: `python3.12 projektion.py` — Expected: `varianz` zwei Werte, Summe zwischen 0,1 und 0,4 (typisch für Satz-Embeddings); Datei ~30 KB.

- [ ] **Step 5: Tokenisierungsbeispiele**

`tools/tokens.py`:

```python
"""Tokenisierung von Beispieltexten mit dem e5-Tokenizer (XLM-R SentencePiece); Zählungen für die Labs."""
from transformers import AutoTokenizer
from fall import FALL, pruefe_fall, lese, schreibe
pruefe_fall()
import server  # SYSTEM_PROMPT des Fallbeispiels
tok = AutoTokenizer.from_pretrained("intfloat/multilingual-e5-small")
def stuecke(text):
    ids = tok.encode(text, add_special_tokens=False)
    return {"text": text, "tokens": tok.convert_ids_to_tokens(ids), "ids": ids}
beispiele = {k: stuecke(t) for k, t in {
    "e18": "Was bedeutet E:18?", "laugenpumpe": "Laugenpumpe reinigen", "laugenpumpe-en": "Clean the drain pump",
    "satz-de": "Die Laugenpumpe ist verstopft. Laugenpumpe reinigen.", "satz-en": "The drain pump is blocked. Clean the drain pump.",
    "kindersicherung": "Kindersicherung deaktivieren", "zahl": "Programm 60 °C, 1400 U/min", "code": "E:18 E18 Fehler 18 E:180"}.items()}
chunks = lese("chunks.json")["chunks"]
e18 = next(c for c in chunks if "Anzeige: E:18;" in c["text"])
handbuch = (FALL / "siemens_wissen.md").read_text(encoding="utf-8")
zaehl = {"systemprompt": len(tok.encode(server.SYSTEM_PROMPT, add_special_tokens=False)),
         "e18Chunk": len(tok.encode(e18["text"], add_special_tokens=False)),
         "handbuch": len(tok.encode(handbuch, add_special_tokens=False)),
         "handbuchZeichen": len(handbuch), "handbuchWoerter": len(handbuch.split()),
         "chunkTokens": [len(tok.encode(c["text"], add_special_tokens=False)) for c in chunks]}
zaehl["zeichenJeToken"] = round(zaehl["handbuchZeichen"] / zaehl["handbuch"], 2)
zaehl["maxChunkTokens"] = max(zaehl["chunkTokens"]); zaehl["e18ChunkId"] = e18["id"]
schreibe("tokens.json", {"tokenizer": "intfloat/multilingual-e5-small (XLM-RoBERTa, SentencePiece, 250 002 Einträge)",
                         "beispiele": beispiele, "zaehlungen": zaehl})
print(zaehl["zeichenJeToken"], "Zeichen je Token;", zaehl["maxChunkTokens"], "max Tokens je Chunk")
```

Expected: `maxChunkTokens ≤ 440`; `zeichenJeToken` zwischen 3 und 5. Den gemessenen Wert in `assets/pruefung.js` als `ZEICHEN_JE_TOKEN` eintragen (Aufgabe 3).

- [ ] **Step 6: Logits für den Temperatur-Regler**

`tools/logits.py` (Modell `Qwen/Qwen2.5-0.5B`, Apache-2.0, ~1 GB, einmaliger Download):

```python
"""Top-20 nächste Tokens mit Logits und drei Fortsetzungen je Prompt (kleines offenes Modell)."""
import torch, datetime
from transformers import AutoTokenizer, AutoModelForCausalLM
from fall import schreibe
NAME = "Qwen/Qwen2.5-0.5B"
tok = AutoTokenizer.from_pretrained(NAME); model = AutoModelForCausalLM.from_pretrained(NAME, torch_dtype=torch.float32).eval()
PROMPTS = {"e18": "Die Waschmaschine zeigt den Fehlercode E:18. Das bedeutet, dass",
           "hauptstadt": "Die Hauptstadt von Frankreich ist",
           "pumpe": "Vor dem Reinigen der Laugenpumpe muss man zuerst",
           "zahlen": "1, 2, 3, 4, 5,", "wasser": "Wasser läuft aus der Waschmaschine, weil",
           "englisch": "The washing machine shows error E:18, which means that"}
def fortsetzung(ids, temperatur, seed=7):
    torch.manual_seed(seed)
    out = model.generate(ids, max_new_tokens=24, do_sample=temperatur > 0, temperature=max(temperatur, 1e-3) if temperatur > 0 else None,
                         top_k=0, top_p=1.0, pad_token_id=tok.eos_token_id)
    return tok.decode(out[0][ids.shape[1]:], skip_special_tokens=True)
eintraege = []
for key, p in PROMPTS.items():
    ids = tok(p, return_tensors="pt").input_ids
    with torch.no_grad(): logits = model(ids).logits[0, -1]
    top = torch.topk(logits, 20)
    eintraege.append({"id": key, "prompt": p,
        "tokens": [{"token": tok.decode([int(i)]), "logit": round(float(l), 3)} for l, i in zip(top.values, top.indices)],
        "restLogsumexp": round(float(torch.logsumexp(logits, 0)), 3),
        "fortsetzungen": {"0": fortsetzung(ids, 0), "0.7": fortsetzung(ids, 0.7), "1.5": fortsetzung(ids, 1.5)}})
schreibe("logits.json", {"modell": NAME, "datum": datetime.date.today().isoformat(),
                         "hinweis": "Basismodell ohne Instruktionstuning; Fortsetzungen mit festem Zufallsstartwert 7, 24 neue Tokens.",
                         "prompts": eintraege})
```

`restLogsumexp` erlaubt im Browser die exakte Wahrscheinlichkeit der Top-20 gegenüber dem Rest (Softmax über alle Tokens: p_i = exp(l_i − logsumexp)). Expected: sechs Einträge; Fortsetzung bei T=0 für `hauptstadt` enthält „Paris“.

- [ ] **Step 7: Antworten aufzeichnen (LM Studio muss laufen)**

`tools/aufzeichnen.py`:

```python
"""Echte Antworten des Fallbeispiels: nackt (ohne Handbuch) und per RAG, über LM Studio."""
import datetime, time, json
from fall import FALL, pruefe_fall, schreibe
pruefe_fall()
from server import Backend, _messages, parse_ai_response, SYSTEM_PROMPT
from rag_engine import NOT_IN_MANUAL
backend = Backend(); llm = backend.llm("local")
FRAGEN = [("e18", "Was bedeutet der Fehlercode E:18?"), ("e23", "Was bedeutet E:23?"),
          ("pumpe", "Wie reinige ich die Laugenpumpe? Welche Sicherheitsmaßnahmen sind vorher nötig?"),
          ("kindersicherung", "Wie schalte ich die Kindersicherung wieder aus?"),
          ("wasser", "Wasser läuft unter meiner Waschmaschine aus, was kann ich tun?"),
          ("e180", "Was bedeutet E:180?"), ("hauptstadt", "Was ist die Hauptstadt von Frankreich?")]
NACKT_SYSTEM = "Du bist ein hilfsbereiter Assistent. Antworte auf Deutsch in höchstens sechs Sätzen."
eintraege = []
for key, frage in FRAGEN:
    t0 = time.perf_counter()
    r = llm.answer([{"role": "system", "content": NACKT_SYSTEM}, {"role": "user", "content": frage}])
    eintraege.append({"id": f"{key}-nackt", "frage": frage, "modus": "nackt", "anbieter": "LM Studio", "modell": llm.model_id(),
                      "messages": [{"role": "system", "content": NACKT_SYSTEM}, {"role": "user", "content": frage}],
                      "antwort": r.choices[0].message.content, "geparst": None,
                      "usage": {"ein": r.usage.prompt_tokens, "aus": r.usage.completion_tokens}, "sekunden": round(time.perf_counter() - t0, 1)})
    t0 = time.perf_counter()
    ret = backend.retrieve(frage, "hybrid", provider="local")
    if not ret.grounded:
        eintraege.append({"id": f"{key}-rag", "frage": frage, "modus": "rag", "anbieter": "LM Studio", "modell": llm.model_id(),
                          "messages": None, "antwort": NOT_IN_MANUAL, "geparst": None, "abgelehnt": True,
                          "quellen": [], "usage": None, "sekunden": round(time.perf_counter() - t0, 1)}); continue
    msgs = _messages(frage, ret.context)
    r = llm.answer(msgs)
    roh = r.choices[0].message.content or ""
    try: s, body, _ = parse_ai_response(roh); geparst = {"summary": s, "inhalt": body}
    except ValueError as e: geparst = {"fehler": str(e)}
    eintraege.append({"id": f"{key}-rag", "frage": frage, "modus": "rag", "anbieter": "LM Studio", "modell": llm.model_id(),
                      "messages": msgs, "antwort": roh, "geparst": geparst, "abgelehnt": False, "referenz": ret.reference,
                      "quellen": [{"id": q["id"], "abschnitt": q["section"], "text": q["text"], "score": q.get("score")} for q in ret.sources],
                      "usage": {"ein": r.usage.prompt_tokens, "aus": r.usage.completion_tokens}, "sekunden": round(time.perf_counter() - t0, 1)})
    print(key, "ok")
schreibe("antworten.json", {"datum": datetime.date.today().isoformat(), "systemprompt": SYSTEM_PROMPT,
    "nacktSystem": NACKT_SYSTEM, "protokolliert": {"quelle": "docs/evaluation/LIVE.md des Fallbeispiels, gpt-4.1-mini, 19.09.2026",
    "tokens": {"pumpe": [646, 375], "e18": [355, 121], "e23": [341, 107], "e23-pageindex": [1227, 102]}}, "eintraege": eintraege})
```

Run: `python3.12 aufzeichnen.py` — Expected: 14 Einträge; `e180-rag` und `hauptstadt-rag` mit `abgelehnt: true`; `e18-rag` geparst mit `summary`. Wenn LM Studio nicht läuft: Skript bricht mit Verbindungsfehler ab; Server im LM-Studio-Fenster starten und wiederholen.

- [ ] **Step 8: PDF-Seite 33 rendern**

`tools/seite.py`:

```python
"""PDF-Seite 33 als PNG (150 dpi) für Lab 03."""
import fitz  # PyMuPDF
from fall import FALL, ROOT, pruefe_fall
pruefe_fall()
doc = fitz.open(FALL / "siemens-handbuch.pdf")
pix = doc[32].get_pixmap(dpi=150)   # physische Seite 33
pix.save(ROOT / "assets/seite-33.png")
print(pix.width, pix.height)
```

Run — Expected: PNG ~1240 × 1750 px, unter 600 KB (sonst `dpi=120`). Sichtprüfung: die Tabelle „Hinweise im Anzeigefeld“ mit E:18 und E:23 ist zu sehen.

- [ ] **Step 9: Sammellauf und Datenprüfung**

`tools/daten.sh`:

```bash
#!/bin/sh
set -e
cd "$(dirname "$0")"
PY=${PY:-/Users/robert/miniforge3/bin/python3.12}
$PY export_chunks.py && $PY embed_fragen.py && $PY projektion.py && $PY tokens.py && $PY logits.py && $PY seite.py
[ "$1" = "--mit-llm" ] && $PY aufzeichnen.py
echo "Daten erzeugt."
```

In `tools/verify.mjs` nach dem Lab-Block ergänzen:

```js
console.log('Daten')
const daten = (n) => JSON.parse(readFileSync(join(ROOT, 'data', n), 'utf8'))
const ch = daten('chunks.json')
meld(ch.anzahl === 346 && ch.chunks.length === 346, 'chunks.json: 346 Chunks')
meld(ch.chunks.every(c => c.v.length === 384 && typeof c.text === 'string' && /^c\d{3}$/.test(c.id)), 'chunks.json: 384 Dimensionen, Text, IDs')
meld(ch.chunks.some(c => c.text.includes('Anzeige: E:18;')), 'chunks.json: E:18-Zeile als eigener Chunk')
const fr = daten('fragen.json')
meld(fr.fragen.length >= 24, 'fragen.json: mindestens 24 Fragen')
meld(fr.fragen.every(f => f.vMit.length === 384 && f.vektor.length === 12 && f.hybrid.length >= 12 && f.rerank.length >= 12 && Array.isArray(f.kontext) && typeof f.gedeckt === 'boolean'), 'fragen.json: alle Stufen je Frage')
meld(fr.fragen.filter(f => f.art === 'eval').length === 10 && fr.fragen.filter(f => f.art === 'negativ').length === 4, 'fragen.json: 10 Eval, 4 Negativ')
const pr = daten('projektion.json')
meld(pr.komponenten.length === 2 && pr.komponenten[0].length === 384 && pr.punkte.length === 346, 'projektion.json: Matrix und Punkte')
const an = daten('antworten.json')
meld(an.eintraege.some(e => e.id === 'e18-nackt') && an.eintraege.some(e => e.id === 'e18-rag' && e.geparst && e.geparst.summary), 'antworten.json: E:18 nackt und RAG')
const lg = daten('logits.json'); meld(lg.prompts.length >= 6 && lg.prompts.every(p => p.tokens.length === 20), 'logits.json: sechs Prompts, Top-20')
const tk = daten('tokens.json'); meld(tk.beispiele.e18 && tk.zaehlungen.systemprompt > 0 && tk.zaehlungen.maxChunkTokens <= 440, 'tokens.json: Beispiele und Zählungen')
for (const n of ['handbuch.md', 'seite-33.md', 'eval/vector.json', 'eval/hybrid-no-rerank.json', 'eval/hybrid-rerank.json', 'eval/negative-checks.json', 'eval/questions.json']) meld(existsSync(join(ROOT, 'data', n)), `data/${n} vorhanden`)
meld(existsSync(join(ROOT, 'assets/seite-33.png')), 'assets/seite-33.png vorhanden')
```

Run: `node tools/verify.mjs` — Expected: Datenblock komplett `ok`.

- [ ] **Step 10: Commit**

```bash
git add -A && git commit -m "Datenpipeline: Chunks, Fragenkatalog, Projektion, Tokens, Logits, Antworten, Seite 33

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Bewertungslogik und Chunking mit Tests

**Files:**
- Create: `assets/pruefung.js`, `assets/chunking.js`, `tools/pruefung.test.mjs`

**Interfaces:**
- Produces (`pruefung.js`): `kosinus(a,b)`, `normalisiere(v)`, `topK(q, chunks, k)`, `fehlercodes(text)`, `hybridKandidaten(vektorTreffer, chunks, frage)`, `waehleKontext(kandidaten, {schwelle, verhaeltnis, finalK, reranked})`, `istGedeckt(frage, kontext, chunkText, {schwelle, reranked})`, `parseAntwort(roh)`, `macheCheckboxen(text)`, `bewerteTreffer(texte, erwartet)`, `metriken(faelle)`, `projiziere(v, projektion)`, `schaetzeTokens(text)`, `ZEICHEN_JE_TOKEN`, `softmax(logits, temperatur)`, `pruefeQuiz(fragen, antworten)`, `pruefeZuordnen(elemente, zuordnung)`, `pruefeSortieren(reihenfolge, eingabe)`, `pruefeRechnen(felder, werte)`, `pruefeLuecken(luecken, eingaben)`, `pruefeBelegen(saetze, markierung)`.
- Produces (`chunking.js`): `chunkeFest(text, {groesse, overlap})`, `chunkeSaetze(text, {groesse, overlap})`, `chunkeMarkdown(text)`, `explodiereTabellen(abschnitte, maxZeichen)`, `begrenzeLaenge(abschnitte, {groesse, overlap, zaehler})`.
- Consumes: `data/chunks.json`, `data/fragen.json`, `data/eval/hybrid-rerank.json`, `data/tokens.json` (nur in Tests).

- [ ] **Step 1: Tests schreiben (schlagen fehl)**

`tools/pruefung.test.mjs`:

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'
import { fileURLToPath } from 'node:url'
import { dirname, join } from 'node:path'
import * as P from '../assets/pruefung.js'
import * as C from '../assets/chunking.js'
const ROOT = join(dirname(fileURLToPath(import.meta.url)), '..')
const daten = (n) => JSON.parse(readFileSync(join(ROOT, 'data', n), 'utf8'))
const chunks = daten('chunks.json').chunks
const fragen = daten('fragen.json').fragen
const text = (id) => chunks.find(c => c.id === id).text

test('kosinus: identische Vektoren 1, orthogonale 0, Rechenbeispiel', () => {
  assert.equal(P.kosinus([1, 2, 2], [1, 2, 2]), 1)
  assert.equal(P.kosinus([1, 0], [0, 1]), 0)
  assert.ok(Math.abs(P.kosinus([1, 2, 2], [2, 0, 1]) - 4 / (3 * Math.sqrt(5))) < 1e-12)
})
test('topK gegen die Python-Trefferliste (mit Präfix)', () => {
  const f = fragen.find(x => x.id === 'fehler-e23')
  const js = P.topK(f.vMit, chunks, 12).map(t => t.id)
  const py = f.vektor.map(t => t.chunk)
  assert.deepEqual(js.slice(0, 5), py.slice(0, 5))
})
test('fehlercodes: Varianten und Nicht-Treffer', () => {
  assert.deepEqual([...P.fehlercodes('Was bedeutet E:18?')].sort(), ['E18', 'E:18'].sort())
  assert.deepEqual([...P.fehlercodes('Fehler 18 und fehlercode: 23')].sort(), ['E18', 'E23', 'E:18', 'E:23'].sort())
  assert.equal(P.fehlercodes('E:180').has('E:18'), false)
  assert.equal(P.fehlercodes('18 Grad').size, 0)
})
test('hybridKandidaten: exakte Codes zuerst mit Score 1, Rest dedupliziert', () => {
  const f = fragen.find(x => x.id === 'fehler-e18')
  const h = P.hybridKandidaten(f.vektor.map(t => ({ id: t.chunk, score: t.score })), chunks, f.frage)
  assert.deepEqual(h.map(k => k.id), f.hybrid.map(k => k.chunk))
  assert.ok(h[0].exakt && h[0].score === 1)
})
test('waehleKontext: relativer Boden 0,5 × bester Score, mindestens 0,15', () => {
  const k = [{ id: 'a', score: 0.98 }, { id: 'b', score: 0.6 }, { id: 'c', score: 0.4 }, { id: 'd', score: 0.2 }, { id: 'e', score: 0.1 }, { id: 'f', score: 0.99 }]
  assert.deepEqual(P.waehleKontext(k).map(x => x.id), ['a', 'b'])
  assert.deepEqual(P.waehleKontext([{ id: 'a', score: 0.2 }, { id: 'b', score: 0.12 }]).map(x => x.id), ['a'])
  assert.deepEqual(P.waehleKontext([{ id: 'a', score: 0.9, exakt: false }, { id: 'b', score: 1, exakt: true }], { reranked: false }).map(x => x.id), ['b'])
})
test('waehleKontext und istGedeckt reproduzieren fragen.json', () => {
  for (const f of fragen) {
    const kontext = P.waehleKontext(f.rerank.map(r => ({ id: r.chunk, score: r.score, exakt: r.exakt })))
    assert.deepEqual(kontext.map(k => k.id), f.kontext, f.id)
    assert.equal(P.istGedeckt(f.frage, kontext, text), f.gedeckt, f.id)
  }
})
test('istGedeckt: unbekannter Code E:180 wird abgelehnt, auch bei hohem Score', () => {
  const kontext = [{ id: chunks.find(c => c.text.includes('Anzeige: E:18;')).id, score: 0.9 }]
  assert.equal(P.istGedeckt('Was bedeutet E:180?', kontext, text), false)
  assert.equal(P.istGedeckt('Was bedeutet E:18?', kontext, text), true)
})
test('parseAntwort: Tags, wiederholte Schrittblöcke, Codezäune, Fehlerfall', () => {
  const r = P.parseAntwort('```xml\n<summary>Kurz</summary>\n<manual_intro>Einleitung</manual_intro>\n<manual_steps>- **A:** eins\n- **B:** zwei</manual_steps>\n<manual_steps>3. drei</manual_steps>\n```')
  assert.equal(r.ok, true); assert.equal(r.summary, 'Kurz'); assert.equal(r.intro, 'Einleitung')
  assert.deepEqual(r.schritte, ['**A:** eins', '**B:** zwei', 'drei'])
  assert.equal(P.parseAntwort('Nur Text ohne Tags').ok, false)
  assert.equal(P.parseAntwort('<manual_intro>x').summary, 'Hinweise aus dem Handbuch')
})
test('macheCheckboxen entfernt Aufzählungszeichen und Nummern', () => {
  assert.equal(P.macheCheckboxen('- eins\n2) zwei\n[ ] drei\n\n• vier'), '- [ ] eins\n- [ ] zwei\n- [ ] drei\n- [ ] vier')
})
test('bewerteTreffer und metriken reproduzieren das Protokoll hybrid-rerank', () => {
  const prot = daten('eval/hybrid-rerank.json')
  const faelle = prot.results.map(r => { const f = fragen.find(x => x.id === r.id); const texte = f.rerank.slice(0, 5).map(k => text(k.chunk)); return P.bewerteTreffer(texte, f.erwartet) })
  const m = P.metriken(faelle)
  assert.ok(Math.abs(m.trefferquote - prot.keyword_hit_rate) < 1e-9)
  assert.ok(Math.abs(m.hitAt1 - prot.keyword_hit_at_1) < 1e-9)
  assert.ok(Math.abs(m.mrr - prot.keyword_mrr) < 1e-9)
  assert.ok(Math.abs(m.abdeckung - prot.keyword_coverage) < 1e-9)
})
test('metriken: Rechenbeispiel MRR', () => {
  const m = P.metriken([1, 2, 1, null, 3].map(r => ({ ersterRang: r, gefunden: 1, erwartet: 1 })))
  assert.ok(Math.abs(m.mrr - (1 + 0.5 + 1 + 0 + 1 / 3) / 5) < 1e-12)
  assert.equal(m.trefferquote, 0.8); assert.equal(m.hitAt1, 0.4)
})
test('projiziere: Chunk-Punkt aus projektion.json wird reproduziert', () => {
  const pr = daten('projektion.json'); const c = chunks[10]; const p = pr.punkte.find(x => x.id === c.id)
  const [x, y] = P.projiziere(c.v, pr)
  assert.ok(Math.abs(x - p.x) < 0.01 && Math.abs(y - p.y) < 0.01)
})
test('softmax: T→0 konzentriert, T groß flacht ab', () => {
  const l = [3, 1, 0]
  const kalt = P.softmax(l, 0.1), warm = P.softmax(l, 2)
  assert.ok(kalt[0] > 0.999 && warm[0] < 0.7)
  assert.ok(Math.abs(warm.reduce((a, b) => a + b) - 1) < 1e-12)
  assert.deepEqual(P.softmax(l, 0), [1, 0, 0])
})
test('schaetzeTokens nutzt die gemessene Zeichenzahl je Token', () => {
  const tk = daten('tokens.json')
  assert.equal(P.ZEICHEN_JE_TOKEN, tk.zaehlungen.zeichenJeToken)
  assert.equal(P.schaetzeTokens('a'.repeat(100)), Math.ceil(100 / tk.zaehlungen.zeichenJeToken))
})
test('Prüfer je Übungstyp', () => {
  assert.equal(P.pruefeQuiz([{ richtig: [1] }, { richtig: [0, 2], mehrfach: true }], [[1], [0, 2]]).ok, true)
  assert.equal(P.pruefeQuiz([{ richtig: [1] }], [[0]]).ok, false)
  assert.deepEqual(P.pruefeZuordnen([{ ziel: 'a' }, { ziel: 'b' }], { 0: 'a', 1: 'a' }), { ok: false, falsch: [1], fehlend: [] })
  assert.deepEqual(P.pruefeSortieren([2, 0, 1], [2, 1, 0]), { ok: false, falschPlatziert: [1, 2] })
  assert.equal(P.pruefeRechnen([{ loesung: 0.596, toleranz: 0.005 }], [0.5963]).ok, true)
  assert.equal(P.pruefeRechnen([{ loesung: 0.596, toleranz: 0.005 }], [NaN]).ok, false)
  assert.equal(P.pruefeLuecken([{ loesung: ['440'] }, { loesung: ['gemma-4-12b-it-mlx'] }], [' 440', 'GEMMA-4-12B-IT-MLX']).ok, true)
  assert.equal(P.pruefeLuecken([{ muster: '^query:\\s*$' }], ['query: ']).ok, true)
  assert.deepEqual(P.pruefeBelegen([{ belegt: true }, { belegt: false }, { belegt: true }], [true, true, null]), { ok: false, uebersehen: [1], zuUnrecht: [], offen: [2] })
})
test('chunking: Tabellenexplosion reproduziert den E:18-Chunk aus Python', () => {
  const md = readFileSync(join(ROOT, 'data/handbuch.md'), 'utf8')
  const abschnitte = C.chunkeMarkdown(md)
  const anzeige = abschnitte.find(a => a.ueberschrift === 'Hinweise im Anzeigefeld')
  const zeilen = C.explodiereTabellen([anzeige], 1600)
  const e18 = zeilen.find(z => z.text.includes('Anzeige: E:18;'))
  assert.equal(e18.text, text(chunks.find(c => c.text.includes('Anzeige: E:18;')).id))
})
test('chunking: feste Länge mit Überlappung, Sätze, Markdown', () => {
  const t = 'abcdefghij'.repeat(10)
  const f = C.chunkeFest(t, { groesse: 30, overlap: 5 })
  assert.equal(f[0].text.length, 30); assert.equal(f[1].start, 25)
  assert.ok(f.every(c => c.text.length <= 30))
  const s = C.chunkeSaetze('Eins. Zwei! Drei? Vier.', { groesse: 12, overlap: 0 })
  assert.deepEqual(s.map(c => c.text), ['Eins. Zwei!', 'Drei? Vier.'])
  const m = C.chunkeMarkdown('# A\ntext a\n## B\ntext b\n')
  assert.deepEqual(m.map(x => x.ueberschrift), ['A', 'B'])
  assert.equal(m[1].text, '## B\ntext b')
})
```

Run: `node --test tools/pruefung.test.mjs` — Expected: FAIL (Module fehlen).

- [ ] **Step 2: `assets/pruefung.js` schreiben**

```js
/**
 * RAG-Lab · Bewertungslogik
 *
 * Reine Funktionen ohne DOM: dieselbe Datei laeuft im Browser (Werkzeuge,
 * Uebungsboxen) und in tools/pruefung.test.mjs. Die Retrieval-Funktionen sind
 * Ports aus rag_engine.py und server.py des Fallbeispiels; wo das Original
 * abweicht, steht es im Kommentar.
 */

/* ------------------------------------------------------------- Vektoren */

export function kosinus (a, b) {
  let s = 0, na = 0, nb = 0
  for (let i = 0; i < a.length; i++) { s += a[i] * b[i]; na += a[i] * a[i]; nb += b[i] * b[i] }
  return na && nb ? s / Math.sqrt(na * nb) : 0
}
export function normalisiere (v) {
  let n = 0; for (const x of v) n += x * x; n = Math.sqrt(n) || 1
  return Array.from(v, x => x / n)
}
/** Top-k Chunks nach Kosinus; chunks: [{ id, v }]. Rueckgabe [{ id, score }] absteigend. */
export function topK (q, chunks, k = 12) {
  return chunks.map(c => ({ id: c.id, score: kosinus(q, c.v) })).sort((a, b) => b.score - a.score).slice(0, k)
}
/** PCA-Projektion mit den Komponenten aus data/projektion.json. */
export function projiziere (v, p) {
  const d = v.map((x, i) => x - p.mittel[i])
  return p.komponenten.map(k => k.reduce((s, x, i) => s + x * d[i], 0))
}

/* ---------------------------------------------------------- Fehlercodes */

// Port von _CODE_RE: E:18, E18, "Fehler 18", "Fehlercode: 18" -> {E:18, E18}; E:180 ist nicht E:18.
const CODE_RE = /(?:\bE\s*:?\s*|\bfehler(?:code)?\s*:?\s*)(\d{1,3})\b/gi
export function fehlercodes (text) {
  const s = new Set()
  for (const m of String(text || '').matchAll(CODE_RE)) { s.add('E:' + m[1]); s.add('E' + m[1]) }
  return s
}
const schneidet = (a, b) => { for (const x of a) if (b.has(x)) return true; return false }

/** Port von _HybridRetriever: exakte Codetreffer (Score 1) vor den Vektortreffern, ohne Dubletten. */
export function hybridKandidaten (vektorTreffer, chunks, frage) {
  const codes = fehlercodes(frage)
  if (!codes.size) return vektorTreffer.map(t => ({ ...t, exakt: false }))
  const exakt = [], ids = new Set()
  for (const c of chunks) if (schneidet(codes, fehlercodes(c.text))) { exakt.push({ id: c.id, score: 1, exakt: true }); ids.add(c.id) }
  return exakt.concat(vektorTreffer.filter(t => !ids.has(t.id)).map(t => ({ ...t, exakt: false })))
}

/** Port von select_context_nodes auf nodes[:FINAL_K]: relativer Boden, mindestens die Schwelle. */
export function waehleKontext (kandidaten, o = {}) {
  const { schwelle = 0.15, verhaeltnis = 0.5, finalK = 5, reranked = true } = o
  const top = kandidaten.slice(0, finalK)
  if (!reranked) return top.filter(k => k.exakt)
  const best = top.length && Number.isFinite(top[0].score) ? top[0].score : 0
  const boden = Math.max(schwelle, best * verhaeltnis)
  return top.filter(k => Number.isFinite(k.score) && k.score >= boden)
}

/** Port von is_grounded plus der Regel aus server.py: gefragte Codes muessen im Kontext vorkommen. */
export function istGedeckt (frage, kontext, chunkText, o = {}) {
  const { schwelle = 0.15, reranked = true } = o
  let ok = reranked
    ? kontext.length > 0 && Number.isFinite(kontext[0].score) && kontext[0].score >= schwelle
    : kontext.some(k => k.exakt)
  const gefragt = fehlercodes(frage)
  if (gefragt.size) {
    const bekannt = new Set()
    for (const k of kontext) for (const c of fehlercodes(chunkText(k.id))) bekannt.add(c)
    for (const c of gefragt) if (!bekannt.has(c)) ok = false
  }
  return ok
}

/* ------------------------------------------------------- Antwortformat */

const tag = (text, name) => [...text.matchAll(new RegExp(`<${name}>([\\s\\S]*?)(?:</${name}>|$)`, 'gi'))].map(m => m[1].trim()).join('\n')
export function macheCheckboxen (text) {
  return text.split('\n').filter(l => l.trim()).map(l => '- [ ] ' + l.trim().replace(/^(?:[-*•]\s+|\d+[.)]\s+|\[ \]\s*)+/, '')).join('\n')
}
/** Port von parse_ai_response: drei Tags, wiederholte Bloecke werden gesammelt, Codezaeune entfernt. */
export function parseAntwort (roh) {
  const text = String(roh || '').replace(/```[a-zA-Z]*\n?/g, '').replace(/```/g, '').trim()
  const summary = tag(text, 'summary'), intro = tag(text, 'manual_intro'), steps = tag(text, 'manual_steps')
  if (!summary && !intro && !steps) return { ok: false, fehler: 'kein-format' }
  const schritte = macheCheckboxen(steps).split('\n').filter(Boolean).map(l => l.replace(/^- \[ \] /, ''))
  return { ok: true, summary: summary || 'Hinweise aus dem Handbuch', intro, schritte }
}

/* ------------------------------------------------------------ Metriken */

/** Stichworttreffer wie in eval/run_eval.py: erster Rang mit irgendeinem Stichwort, Abdeckung ueber alle. */
export function bewerteTreffer (texte, erwartet) {
  const erw = erwartet.map(k => k.toLowerCase())
  const gefunden = new Set(); let erster = null
  texte.forEach((t, i) => {
    const c = t.toLowerCase(); const hits = erw.filter(k => c.includes(k))
    if (hits.length && erster == null) erster = i + 1
    hits.forEach(k => gefunden.add(k))
  })
  return { ersterRang: erster, gefunden: gefunden.size, erwartet: erw.length, stichworte: [...gefunden].sort() }
}
export function metriken (faelle) {
  const n = faelle.length || 1
  const hit = faelle.filter(f => f.ersterRang != null).length
  const hit1 = faelle.filter(f => f.ersterRang === 1).length
  const mrr = faelle.reduce((s, f) => s + (f.ersterRang ? 1 / f.ersterRang : 0), 0)
  const abd = faelle.reduce((s, f) => s + (f.erwartet ? f.gefunden / f.erwartet : 0), 0)
  return { trefferquote: hit / n, hitAt1: hit1 / n, mrr: mrr / n, abdeckung: abd / n }
}

/* ------------------------------------------------------------- Tokens */

export const ZEICHEN_JE_TOKEN = 3.8   // wird in Aufgabe 2 auf den gemessenen Wert aus tokens.json gesetzt
export function schaetzeTokens (text) { return Math.ceil(String(text || '').length / ZEICHEN_JE_TOKEN) }
export function softmax (logits, temperatur) {
  if (temperatur <= 0) { const i = logits.indexOf(Math.max(...logits)); return logits.map((_, j) => j === i ? 1 : 0) }
  const m = Math.max(...logits); const e = logits.map(l => Math.exp((l - m) / temperatur)); const s = e.reduce((a, b) => a + b, 0)
  return e.map(x => x / s)
}

/* ------------------------------------------------ Pruefer je Uebungstyp */

export function pruefeQuiz (fragen, antworten) {
  const je = fragen.map((fr, i) => {
    const gew = antworten[i] || []; const richtig = new Set(fr.richtig)
    return { ok: gew.length === richtig.size && gew.every(g => richtig.has(g)), beantwortet: gew.length > 0 }
  })
  return { ok: je.every(j => j.ok), alleBeantwortet: je.every(j => j.beantwortet), je }
}
export function pruefeZuordnen (elemente, zuordnung) {
  const falsch = [], fehlend = []
  elemente.forEach((e, i) => { const z = zuordnung[i]; if (!z) fehlend.push(i); else if (z !== e.ziel) falsch.push(i) })
  return { ok: !falsch.length && !fehlend.length, falsch, fehlend }
}
export function pruefeSortieren (reihenfolge, eingabe) {
  const falschPlatziert = reihenfolge.map((r, i) => (eingabe[i] === r ? null : i)).filter(i => i != null)
  return { ok: !falschPlatziert.length, falschPlatziert }
}
export function pruefeRechnen (felder, werte) {
  const je = felder.map((f, i) => Number.isFinite(werte[i]) && Math.abs(werte[i] - f.loesung) <= (f.toleranz ?? 0))
  return { ok: je.every(Boolean), je }
}
const norm = (s) => String(s ?? '').trim().toLowerCase().replace(/\s+/g, ' ')
export function pruefeLuecken (luecken, eingaben) {
  const je = luecken.map((l, i) => {
    if (l.muster) return new RegExp(l.muster, 'i').test(String(eingaben[i] ?? '').trim())
    return (l.loesung || []).map(norm).includes(norm(eingaben[i]))
  })
  return { ok: je.every(Boolean), je }
}
export function pruefeBelegen (saetze, markierung) {
  const uebersehen = [], zuUnrecht = [], offen = []
  saetze.forEach((s, i) => {
    const m = markierung[i]
    if (m == null) offen.push(i)
    else if (m && !s.belegt) uebersehen.push(i)
    else if (!m && s.belegt) zuUnrecht.push(i)
  })
  return { ok: !uebersehen.length && !zuUnrecht.length && !offen.length, uebersehen, zuUnrecht, offen }
}
```

- [ ] **Step 3: `assets/chunking.js` schreiben**

```js
/**
 * RAG-Lab · Chunking-Strategien
 *
 * Vier Stufen, wie sie das Fallbeispiel in prepare_nodes() faehrt, dazu zwei
 * einfache Strategien zum Vergleich. explodiereTabellen ist ein Port von
 * _explode_markdown_tables aus rag_engine.py und liefert dieselben Texte wie
 * der Python-Index (tools/pruefung.test.mjs prueft das an der E:18-Zeile).
 */

/** Feste Laenge in Zeichen mit Ueberlappung. */
export function chunkeFest (text, { groesse = 500, overlap = 50 } = {}) {
  const out = []; const schritt = Math.max(1, groesse - overlap)
  for (let start = 0; start < text.length; start += schritt) {
    out.push({ text: text.slice(start, start + groesse), start, ende: Math.min(text.length, start + groesse) })
    if (start + groesse >= text.length) break
  }
  return out
}

/** Saetze greedy bis zur Groesse packen; Ueberlappung wiederholt Endsaetze bis `overlap` Zeichen. */
export function chunkeSaetze (text, { groesse = 500, overlap = 0 } = {}) {
  const saetze = text.split(/(?<=[.!?])\s+/).filter(Boolean)
  const out = []; let akt = []
  const laenge = (arr) => arr.join(' ').length
  for (const s of saetze) {
    if (akt.length && laenge([...akt, s]) > groesse) {
      out.push({ text: akt.join(' ') })
      const rest = []; let l = 0
      for (let i = akt.length - 1; i >= 0 && l + akt[i].length <= overlap; i--) { rest.unshift(akt[i]); l += akt[i].length }
      akt = rest
    }
    akt.push(s)
  }
  if (akt.length) out.push({ text: akt.join(' ') })
  return out
}

/** Abschnitte je Markdown-Ueberschrift (wie MarkdownNodeParser: Ueberschriftzeile bleibt im Text). */
export function chunkeMarkdown (text) {
  const out = []; let zeilen = []; let ueberschrift = ''
  const schliesse = () => { const t = zeilen.join('\n').trim(); if (t) out.push({ ueberschrift, text: t }); zeilen = [] }
  for (const z of text.split('\n')) {
    const m = /^(#{1,6})\s+(.*)$/.exec(z)
    if (m) { schliesse(); ueberschrift = m[2].trim() }
    zeilen.push(z)
  }
  schliesse()
  return out
}

const zelle = (line) => line.trim().replace(/^\|+/, '').replace(/\|+$/, '').split(/(?<!\\)\|/)
  .map(c => c.replace(/\s+/g, ' ').replace(/\\\|/g, '|').trim())

/** Port von _explode_markdown_tables: grosse Tabellenabschnitte zeilenweise, Kopf und Vorzeile bleiben. */
export function explodiereTabellen (abschnitte, maxZeichen = 1600) {
  const out = []
  for (const a of abschnitte) {
    const text = a.text
    const tabellenZeilen = text.split('\n').filter(l => l.trim().startsWith('|'))
    if (text.length <= maxZeichen || tabellenZeilen.length < 3) { out.push({ ...a, art: 'abschnitt' }); continue }
    const zeilen = text.split('\n')
    const kopf = zeilen.filter(l => l.trim().startsWith('#')).map(l => l.trim()).join(' ')
    const prosa = zeilen.filter(l => l.trim() && !l.trim().startsWith('|') && !l.trim().startsWith('#')).join('\n').trim()
    if (prosa) out.push({ ...a, text: `${kopf}\n${prosa}`.trim(), art: 'prosa' })
    for (const block of text.match(/(?:^[ \t]*\|.*(?:\n|$))+/gm) || []) {
      const rows = block.trim().split('\n')
      if (rows.length < 3 || !/^[|\s:\-]+$/.test(rows[1])) { out.push({ ...a, text: `${kopf}\n${block}`.trim(), art: 'tabelle' }); continue }
      const labels = zelle(rows[0]); let vorher = ''
      for (const row of rows.slice(2)) {
        const cells = zelle(row); if (!cells.some(Boolean)) continue
        if (cells[0]) vorher = cells[0]; else if (vorher) cells[0] = vorher
        const paare = cells.map((val, i) => [labels[i] || '', val]).filter(([, val]) => val)
          .map(([lbl, val]) => (lbl ? `${lbl}: ${val}` : val)).join('; ')
        out.push({ ...a, text: `${kopf}\n${paare}`.trim(), art: 'zeile' })
      }
    }
  }
  return out
}

/**
 * Laengenbegrenzung in Tokens (vereinfachter SentenceSplitter): Saetze greedy
 * bis `groesse` Tokens, Ueberlappung in Tokens. `zaehler(text)` liefert die
 * Tokenzahl (exakt mit geladenem Tokenizer, sonst Schaetzung).
 */
export function begrenzeLaenge (abschnitte, { groesse = 440, overlap = 40, zaehler }) {
  const out = []
  for (const a of abschnitte) {
    if (zaehler(a.text) <= groesse) { out.push(a); continue }
    const saetze = a.text.split(/(?<=[.!?])\s+|\n+/).filter(Boolean)
    let akt = []
    const flush = () => { if (akt.length) out.push({ ...a, text: akt.join(' '), art: 'geteilt' }) }
    for (const s of saetze) {
      if (akt.length && zaehler([...akt, s].join(' ')) > groesse) {
        flush()
        const rest = []; for (let i = akt.length - 1; i >= 0 && zaehler([s, ...rest].join(' ')) <= overlap + zaehler(s); i--) rest.unshift(akt[i])
        akt = rest
      }
      akt.push(s)
    }
    flush()
  }
  return out
}
```

- [ ] **Step 4: Tests laufen lassen**

Run: `node --test tools/pruefung.test.mjs` — Expected: alle Tests PASS. Schlägt `chunking: Tabellenexplosion` fehl, die Differenz zwischen JS-Text und Python-Text zeichenweise vergleichen (häufig: Whitespace in Zellen, `\r`, Zeilenende ohne `\n`); die JS-Fassung an Python angleichen, nie umgekehrt. Schlägt `topK` fehl, prüfen, ob `fragen.json` mit gerundeten Chunk-Vektoren berechnet wurde (Ränge 1–5 müssen übereinstimmen).

`ZEICHEN_JE_TOKEN` auf den Wert aus `data/tokens.json` setzen.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "Bewertungslogik und Chunking mit Regressionstests

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: Übungstypen in der Laufzeit

**Files:**
- Modify: `assets/rag.js` (`baueBox`, `T`), `assets/rag.css` (Klassen `.zuordnen`, `.sortieren`, `.rechnen`, `.luecken`, `.belegen`, `.checkliste`)
- Modify: `tools/verify.mjs` (typspezifische Prüfung)

**Interfaces:**
- Consumes: Prüfer aus `pruefung.js` (Aufgabe 3).
- Produces: Übungs-JSON-Schema je Typ (unten), das Aufgaben 8–17 verwenden. `baueBox` ruft für `experiment` das Werkzeug `uebung.werkzeug` mit `uebung.parameter` über `baueWerkzeug(ziel, name, parameter, ctx)` aus Aufgabe 5 auf (bis dahin: Platzhalter-Text „Werkzeug folgt“).

**Schema je Typ** (Pflichtfelder neben `id`, `typ`, `titel`, `aufgabe`):

```jsonc
// quiz / experiment
{ "fragen": [{ "frage": {de,en}, "optionen": [{de,en}], "richtig": [0], "mehrfach": false, "erklaerung": {de,en} }],
  "werkzeug": "suche", "parameter": { "frage": "fehler-e18" } }            // nur experiment
// zuordnen
{ "kategorien": [{ "id": "rag", "name": {de,en} }], "elemente": [{ "text": {de,en}, "ziel": "rag" }] }
// sortieren: elemente in RICHTIGER Reihenfolge; die Anzeige mischt deterministisch (Sortierung nach sha-artigem Hash der ID)
{ "elemente": [{ "text": {de,en} }] }
// rechnen
{ "felder": [{ "name": {de,en}, "loesung": 0.596, "toleranz": 0.005, "einheit": "" }], "loesungsweg": {de,en} }
// luecken: text mit ___1___ … ___n___ (in beiden Sprachen gleich viele), luecken[i] fuer ___i+1___
{ "text": {de,en}, "luecken": [{ "loesung": ["440"] }, { "muster": "^query:\\s?$" }] }
// belegen: saetze der Antwort; quellen als Chunk-IDs oder freie Texte
{ "saetze": [{ "text": {de,en}, "belegt": true }], "quellen": [{ "chunk": "c123" } | { "text": {de,en}, "titel": {de,en} }] }
// terminal (Aufgabe 7): { "os": "mac"|"win"|"alle", "welt": {...}, "schritte": [{ "text": {de,en}, "muster": "regex", "zustand": {...} }] }
// checkliste
{ "schritte": [{ "text": {de,en}, "frage": {de,en}, "optionen": [{de,en}], "richtig": 0 }] }
```

- [ ] **Step 1: Texte ergänzen**

In `T` (rag.js):

```js
waehlen:      { de: 'Zuordnen …', en: 'Assign …' },
alleZuordnen: { de: 'Bitte ordnen Sie alle Elemente zu.', en: 'Please assign every element.' },
hoch:         { de: 'Nach oben', en: 'Move up' },
runter:       { de: 'Nach unten', en: 'Move down' },
loesungsweg:  { de: 'Lösungsweg', en: 'Worked solution' },
alleFelder:   { de: 'Bitte füllen Sie alle Felder.', en: 'Please fill every field.' },
belegt:       { de: 'belegt', en: 'supported' },
unbelegt:     { de: 'nicht belegt', en: 'not supported' },
alleSaetze:   { de: 'Bitte bewerten Sie jeden Satz.', en: 'Please rate every sentence.' },
uebersehen:   { de: 'als belegt markiert, aber ohne Beleg', en: 'marked as supported, but without evidence' },
zuUnrecht:    { de: 'als unbelegt markiert, steht aber in der Quelle', en: 'marked as unsupported, but it is in the source' },
quelle:       { de: 'Quelle', en: 'Source' },
schritt:      { de: 'Schritt', en: 'Step' },
pruefFrage:   { de: 'Prüffrage', en: 'Check question' },
werkzeugFolgt:{ de: 'Werkzeug folgt.', en: 'Tool to follow.' }
```

- [ ] **Step 2: Zweige in `baueBox`**

Nach dem `quiz`/`experiment`-Zweig einfügen (die Helfer `el`, `html`, `status`, `txt`, `geloestMarkieren`, `aktionen`, `statusZiel`, `bPruefen`, `hinweis()` existieren aus PROM; `zwei(elem, o)` setzt `textContent` aus `txt(o)` und aktualisiert bei `rag:sprache`):

```js
const zwei = (node, o, attr) => {
  const setze = () => { if (attr) node.setAttribute(attr, txt(o)); else node.textContent = txt(o) }
  setze(); document.addEventListener('rag:sprache', setze); return node
}
const misch = (n, id) => {   // deterministische Permutation aus der Uebungs-ID
  let h = 0; for (const ch of id) h = (h * 31 + ch.charCodeAt(0)) >>> 0
  const idx = [...Array(n).keys()]
  for (let i = n - 1; i > 0; i--) { h = (h * 1103515245 + 12345) >>> 0; const j = h % (i + 1); [idx[i], idx[j]] = [idx[j], idx[i]] }
  return idx
}

/* ---- zuordnen ---- */
if (uebung.typ === 'zuordnen') {
  const gitter = el('div', 'zuordnen'); const felder = []
  uebung.elemente.forEach((e, i) => {
    const zeile = el('div', 'paar'); zeile.append(zwei(el('span', 'begriff'), e.text))
    const sel = el('select'); const leer = el('option'); leer.value = ''; zwei(leer, T.waehlen); sel.append(leer)
    for (const k of uebung.kategorien) { const o = el('option'); o.value = k.id; zwei(o, k.name); sel.append(o) }
    zwei(sel, e.text, 'aria-label'); zeile.append(sel); gitter.append(zeile); felder.push({ sel, zeile })
  })
  body.append(gitter); aktionen.append(bPruefen); body.append(aktionen, statusZiel); const h = hinweis(); if (h) body.append(h)
  bPruefen.addEventListener('click', () => {
    const r = P.pruefeZuordnen(uebung.elemente, Object.fromEntries(felder.map((f, i) => [i, f.sel.value])))
    if (r.fehlend.length) { status(statusZiel, 'note', txt(T.alleZuordnen)); return }
    felder.forEach((f, i) => { f.zeile.classList.toggle('falsch', r.falsch.includes(i)); f.zeile.classList.toggle('richtig', !r.falsch.includes(i)) })
    if (r.ok) { status(statusZiel, 'ok', txt(T.richtig), txt(uebung.rueckmeldung)); geloestMarkieren() } else status(statusZiel, 'fail', txt(T.nochNicht))
  })
}

/* ---- sortieren ---- */
if (uebung.typ === 'sortieren') {
  const liste = el('ol', 'sortieren'); let ordnung = misch(uebung.elemente.length, uebung.id)   // ordnung[pos] = Elementindex
  const zeichne = () => {
    liste.replaceChildren()
    ordnung.forEach((idx, pos) => {
      const li = el('li'); li.dataset.idx = String(idx)
      li.append(zwei(el('span', 'sort-text'), uebung.elemente[idx].text))
      const bh = el('button', 'btn-mini', '▲'); zwei(bh, T.hoch, 'aria-label'); bh.disabled = pos === 0
      const br = el('button', 'btn-mini', '▼'); zwei(br, T.runter, 'aria-label'); br.disabled = pos === ordnung.length - 1
      bh.addEventListener('click', () => { [ordnung[pos - 1], ordnung[pos]] = [ordnung[pos], ordnung[pos - 1]]; zeichne() })
      br.addEventListener('click', () => { [ordnung[pos + 1], ordnung[pos]] = [ordnung[pos], ordnung[pos + 1]]; zeichne() })
      li.append(bh, br); liste.append(li)
    })
  }
  zeichne(); body.append(liste); aktionen.append(bPruefen); body.append(aktionen, statusZiel); const h = hinweis(); if (h) body.append(h)
  bPruefen.addEventListener('click', () => {
    const r = P.pruefeSortieren(uebung.elemente.map((_, i) => i), ordnung)
    ;[...liste.children].forEach((li, pos) => { li.classList.toggle('falsch', r.falschPlatziert.includes(pos)); li.classList.toggle('richtig', !r.falschPlatziert.includes(pos)) })
    if (r.ok) { status(statusZiel, 'ok', txt(T.richtig), txt(uebung.rueckmeldung)); geloestMarkieren() } else status(statusZiel, 'fail', txt(T.nochNicht))
  })
}

/* ---- rechnen ---- */
if (uebung.typ === 'rechnen') {
  const form = el('div', 'rechnen'); const eingaben = []
  uebung.felder.forEach((f, i) => {
    const zeile = el('label', 'rechen-feld'); zeile.append(zwei(el('span'), f.name))
    const inp = el('input'); inp.type = 'text'; inp.inputMode = 'decimal'; inp.autocomplete = 'off'
    zeile.append(inp); if (f.einheit) zeile.append(el('span', 'einheit', f.einheit)); form.append(zeile); eingaben.push(inp)
  })
  body.append(form); aktionen.append(bPruefen); body.append(aktionen, statusZiel); const h = hinweis(); if (h) body.append(h)
  const zahl = (s) => parseFloat(String(s).replace(',', '.').replace('%', '').trim())
  bPruefen.addEventListener('click', () => {
    const werte = eingaben.map(e => zahl(e.value))
    if (eingaben.some(e => !e.value.trim())) { status(statusZiel, 'note', txt(T.alleFelder)); return }
    const r = P.pruefeRechnen(uebung.felder, werte)
    eingaben.forEach((e, i) => { e.classList.toggle('falsch', !r.je[i]); e.classList.toggle('richtig', r.je[i]) })
    if (r.ok) {
      status(statusZiel, 'ok', txt(T.richtig), txt(uebung.rueckmeldung)); geloestMarkieren()
      if (uebung.loesungsweg) statusZiel.append(html('div', 'line-hilfe', txt(uebung.loesungsweg)))
    } else status(statusZiel, 'fail', txt(T.nochNicht))
  })
}

/* ---- luecken ---- */
if (uebung.typ === 'luecken') {
  const pre = el('pre', 'luecken code-block'); const felder = []
  const zeichne = () => {
    pre.replaceChildren(); felder.length = 0
    const teile = txt(uebung.text).split(/___(\d+)___/)
    teile.forEach((t, i) => {
      if (i % 2 === 0) pre.append(document.createTextNode(t))
      else { const inp = el('input'); inp.type = 'text'; inp.autocomplete = 'off'; inp.spellcheck = false; inp.dataset.nr = t; inp.setAttribute('aria-label', `${txt(T.schritt)} ${t}`); pre.append(inp); felder[+t - 1] = inp }
    })
  }
  zeichne(); document.addEventListener('rag:sprache', () => { const alt = felder.map(f => f.value); zeichne(); felder.forEach((f, i) => { f.value = alt[i] || '' }) })
  body.append(pre); aktionen.append(bPruefen); body.append(aktionen, statusZiel); const h = hinweis(); if (h) body.append(h)
  bPruefen.addEventListener('click', () => {
    if (felder.some(f => !f.value.trim())) { status(statusZiel, 'note', txt(T.alleFelder)); return }
    const r = P.pruefeLuecken(uebung.luecken, felder.map(f => f.value))
    felder.forEach((f, i) => { f.classList.toggle('falsch', !r.je[i]); f.classList.toggle('richtig', r.je[i]) })
    if (r.ok) { status(statusZiel, 'ok', txt(T.richtig), txt(uebung.rueckmeldung)); geloestMarkieren() } else status(statusZiel, 'fail', txt(T.nochNicht))
  })
}

/* ---- belegen ---- */
if (uebung.typ === 'belegen') {
  const wrap = el('div', 'belegen'); const q = el('div', 'belegen-quellen'); q.append(zwei(el('h4'), T.quelle))
  ctx.daten('chunks.json').then(d => {
    for (const s of uebung.quellen) {
      const box = el('div', 'quelle')
      if (s.chunk) { const c = d.chunks.find(x => x.id === s.chunk); box.append(el('div', 'quelle-titel', c.abschnitt || c.id), el('pre', null, c.text)) }
      else { box.append(zwei(el('div', 'quelle-titel'), s.titel || { de: '', en: '' }), zwei(el('pre'), s.text)) }
      q.append(box)
    }
  })
  const liste = el('ol', 'belegen-saetze'); const wahl = []
  uebung.saetze.forEach((s, i) => {
    const li = el('li'); li.append(zwei(el('span', 'satz'), s.text))
    const grp = el('span', 'wahl')
    for (const [wert, label] of [[true, T.belegt], [false, T.unbelegt]]) {
      const lab = el('label'); const inp = el('input'); inp.type = 'radio'; inp.name = `${uebung.id}-s${i}`; inp.value = String(wert)
      lab.append(inp, zwei(el('span'), label)); grp.append(lab)
    }
    li.append(grp); liste.append(li); wahl.push({ li, grp })
  })
  wrap.append(liste, q); body.append(wrap); aktionen.append(bPruefen); body.append(aktionen, statusZiel); const h = hinweis(); if (h) body.append(h)
  bPruefen.addEventListener('click', () => {
    const m = wahl.map(w => { const c = w.grp.querySelector('input:checked'); return c ? c.value === 'true' : null })
    const r = P.pruefeBelegen(uebung.saetze, m)
    if (r.offen.length) { status(statusZiel, 'note', txt(T.alleSaetze)); return }
    wahl.forEach((w, i) => { const falsch = r.uebersehen.includes(i) || r.zuUnrecht.includes(i); w.li.classList.toggle('falsch', falsch); w.li.classList.toggle('richtig', !falsch) })
    if (r.ok) { status(statusZiel, 'ok', txt(T.richtig), txt(uebung.rueckmeldung)); geloestMarkieren() }
    else {
      const teile = []
      if (r.uebersehen.length) teile.push(`${r.uebersehen.map(i => i + 1).join(', ')}: ${txt(T.uebersehen)}`)
      if (r.zuUnrecht.length) teile.push(`${r.zuUnrecht.map(i => i + 1).join(', ')}: ${txt(T.zuUnrecht)}`)
      status(statusZiel, 'fail', txt(T.nochNicht), teile.join(' · '))
    }
  })
}

/* ---- checkliste ---- */
if (uebung.typ === 'checkliste') {
  const liste = el('ol', 'checkliste'); const eintraege = []
  uebung.schritte.forEach((s, i) => {
    const li = el('li'); li.append(zwei(html('div', 'schritt-text', ''), s.text)); li.querySelector('.schritt-text').innerHTML = txt(s.text)
    document.addEventListener('rag:sprache', () => { li.querySelector('.schritt-text').innerHTML = txt(s.text) })
    const fr = el('div', 'schritt-frage'); fr.append(zwei(el('div', 'frage-text'), s.frage))
    const ul = el('ul', 'optionen')
    s.optionen.forEach((o, j) => { const l = el('li'); const lab = el('label'); const inp = el('input'); inp.type = 'radio'; inp.name = `${uebung.id}-s${i}`; inp.value = String(j); lab.append(inp, zwei(el('span'), o)); l.append(lab); ul.append(l) })
    fr.append(ul); li.append(fr); liste.append(li); eintraege.push({ li, ul, richtig: s.richtig })
  })
  body.append(liste); aktionen.append(bPruefen); body.append(aktionen, statusZiel); const h = hinweis(); if (h) body.append(h)
  bPruefen.addEventListener('click', () => {
    const gew = eintraege.map(e => { const c = e.ul.querySelector('input:checked'); return c ? [+c.value] : [] })
    const r = P.pruefeQuiz(eintraege.map(e => ({ richtig: [e.richtig] })), gew)
    if (!r.alleBeantwortet) { status(statusZiel, 'note', txt(T.fragenOffen)); return }
    eintraege.forEach((e, i) => { e.li.classList.toggle('falsch', !r.je[i].ok); e.li.classList.toggle('richtig', r.je[i].ok) })
    if (r.ok) { status(statusZiel, 'ok', txt(T.richtig), txt(uebung.rueckmeldung)); geloestMarkieren() } else status(statusZiel, 'fail', txt(T.nochNicht))
  })
}
```

Im `quiz`/`experiment`-Zweig: wenn `uebung.typ === 'experiment'`, vor den Fragen `const wz = el('div'); body.append(wz); baueWerkzeug(wz, uebung.werkzeug, uebung.parameter || {}, ctx)` (Funktion aus Aufgabe 5; bis dahin `wz.textContent = txt(T.werkzeugFolgt)`).

- [ ] **Step 3: CSS**

An `assets/rag.css` anhängen:

```css
/* ------------------------------------------------------- Uebungstypen */
.zuordnen .paar { display: grid; grid-template-columns: 1fr minmax(160px, 240px); gap: 0.6rem; align-items: center; padding: 0.45rem 0; border-bottom: 1px solid var(--line); }
.zuordnen .paar.richtig .begriff::before { content: '✓ '; color: var(--ok); }
.zuordnen .paar.falsch .begriff::before { content: '✗ '; color: var(--warn); }
.zuordnen select, .rechnen input, .luecken input { font-family: var(--sans); font-size: 0.9rem; padding: 0.35rem 0.5rem; border: 1px solid var(--line); border-radius: 6px; background: #fff; min-height: 36px; }
.sortieren { list-style: none; margin: 0; padding: 0; }
.sortieren li { display: flex; align-items: center; gap: 0.5rem; padding: 0.45rem 0.6rem; border: 1px solid var(--line); border-radius: 6px; margin-bottom: 0.4rem; background: #fff; }
.sortieren li.richtig { border-color: var(--ok); } .sortieren li.falsch { border-color: var(--warn); background: var(--warn-bg); }
.sortieren .sort-text { flex: 1; }
.btn-mini { min-width: 32px; min-height: 32px; border: 1px solid var(--line); border-radius: 6px; background: #fff; cursor: pointer; font-family: var(--sans); }
.btn-mini:disabled { opacity: 0.35; cursor: default; }
.rechnen { display: grid; gap: 0.6rem; }
.rechen-feld { display: grid; grid-template-columns: 1fr 140px auto; gap: 0.6rem; align-items: center; }
.rechnen input.richtig, .luecken input.richtig { border-color: var(--ok); background: var(--ok-bg); }
.rechnen input.falsch, .luecken input.falsch { border-color: var(--warn); background: var(--warn-bg); }
.luecken { white-space: pre-wrap; }
.luecken input { font-family: var(--mono); font-size: 0.85rem; width: 14ch; margin: 0 0.15rem; padding: 0.1rem 0.4rem; min-height: 28px; }
.belegen { display: grid; grid-template-columns: 1fr; gap: 1rem; }
@media (min-width: 1000px) { .belegen { grid-template-columns: 3fr 2fr; } }
.belegen-saetze li { padding: 0.5rem 0.6rem; border-left: 3px solid var(--line); margin-bottom: 0.5rem; }
.belegen-saetze li.richtig { border-left-color: var(--ok); } .belegen-saetze li.falsch { border-left-color: var(--warn); background: var(--warn-bg); }
.belegen-saetze .wahl { display: flex; gap: 1rem; margin-top: 0.3rem; font-size: 0.85rem; }
.belegen-quellen .quelle { background: var(--bg-soft); border: 1px solid var(--line); border-radius: 6px; padding: 0.6rem; margin-bottom: 0.6rem; }
.belegen-quellen pre { white-space: pre-wrap; font-size: 0.8rem; font-family: var(--mono); margin: 0; }
.quelle-titel { font-weight: 700; font-size: 0.85rem; margin-bottom: 0.3rem; }
.checkliste li { padding: 0.6rem 0; border-bottom: 1px solid var(--line); }
.checkliste li.richtig { border-left: 3px solid var(--ok); padding-left: 0.6rem; } .checkliste li.falsch { border-left: 3px solid var(--warn); padding-left: 0.6rem; }
.checkliste .schritt-frage { margin-top: 0.4rem; font-size: 0.9rem; }
.werkzeug { border: 1px solid var(--line); border-radius: 8px; background: #fff; padding: 1rem; margin: 1rem 0; }
.werkzeug-kopf { display: flex; flex-wrap: wrap; gap: 0.5rem; align-items: center; margin-bottom: 0.7rem; }
.werkzeug-kopf .titel { font-weight: 700; flex: 1; }
.werkzeug-status { font-size: 0.8rem; color: var(--ink-soft); }
.werkzeug-status.live { color: var(--ok); } .werkzeug-status.vorberechnet { color: var(--accent); }
.werkzeug textarea, .werkzeug input[type=text] { font-family: var(--sans); font-size: 0.9rem; padding: 0.4rem 0.6rem; border: 1px solid var(--line); border-radius: 6px; width: 100%; }
.werkzeug input[type=range] { width: 100%; accent-color: var(--signal); }
.tokens { display: flex; flex-wrap: wrap; gap: 0.25rem; margin: 0.5rem 0; }
.tokens span { font-family: var(--mono); font-size: 0.85rem; padding: 0.1rem 0.4rem; border-radius: 4px; background: var(--sand); border: 1px solid var(--line); }
.tokens span:nth-child(even) { background: var(--signal-l); }
.balken { height: 10px; background: var(--bg-soft); border-radius: 5px; overflow: hidden; border: 1px solid var(--line); }
.balken > i { display: block; height: 100%; background: var(--signal); }
```

- [ ] **Step 4: verify.mjs: typspezifische Prüfung**

Im Übungsblock je `u` ergänzen:

```js
const opt = (arr) => Array.isArray(arr) && arr.length >= 2 && arr.every(zwei)
if (u.typ === 'quiz' || u.typ === 'experiment') {
  meld(Array.isArray(u.fragen) && u.fragen.length > 0, `${u.id}: hat Fragen`)
  for (const [i, fr] of (u.fragen || []).entries()) {
    meld(zwei(fr.frage) && opt(fr.optionen), `${u.id} Frage ${i + 1}: zweisprachig mit mindestens zwei Optionen`)
    meld(Array.isArray(fr.richtig) && fr.richtig.length > 0 && fr.richtig.every(r => r >= 0 && r < fr.optionen.length), `${u.id} Frage ${i + 1}: richtige Antworten gültig`)
    if (!fr.mehrfach) meld(fr.richtig.length === 1, `${u.id} Frage ${i + 1}: Einfachauswahl mit genau einer richtigen Antwort`)
    if (fr.erklaerung) meld(zwei(fr.erklaerung), `${u.id} Frage ${i + 1}: Erklärung zweisprachig`)
  }
  if (u.typ === 'experiment') meld(WERKZEUGE.includes(u.werkzeug), `${u.id}: Werkzeug ${u.werkzeug} bekannt`)
}
if (u.typ === 'zuordnen') {
  const ids = new Set((u.kategorien || []).map(k => k.id))
  meld(ids.size >= 2 && u.kategorien.every(k => zwei(k.name)), `${u.id}: Kategorien`)
  meld(Array.isArray(u.elemente) && u.elemente.length >= 3 && u.elemente.every(e => zwei(e.text) && ids.has(e.ziel)), `${u.id}: Elemente mit gültigem Ziel`)
}
if (u.typ === 'sortieren') meld(Array.isArray(u.elemente) && u.elemente.length >= 3 && u.elemente.every(e => zwei(e.text)), `${u.id}: Elemente`)
if (u.typ === 'rechnen') meld(Array.isArray(u.felder) && u.felder.every(f => zwei(f.name) && Number.isFinite(f.loesung) && Number.isFinite(f.toleranz ?? 0)), `${u.id}: Rechenfelder`)
if (u.typ === 'luecken') {
  const n = (s) => (s.match(/___\d+___/g) || []).length
  meld(zwei(u.text) && n(u.text.de) === n(u.text.en) && n(u.text.de) === (u.luecken || []).length, `${u.id}: Lücken in beiden Sprachen = luecken.length`)
  meld((u.luecken || []).every(l => (Array.isArray(l.loesung) && l.loesung.length) || l.muster), `${u.id}: jede Lücke hat Lösung oder Muster`)
}
if (u.typ === 'belegen') {
  meld(Array.isArray(u.saetze) && u.saetze.length >= 2 && u.saetze.every(s => zwei(s.text) && typeof s.belegt === 'boolean'), `${u.id}: Sätze mit belegt-Flag`)
  meld(Array.isArray(u.quellen) && u.quellen.length > 0 && u.quellen.every(q => q.chunk ? ch.chunks.some(c => c.id === q.chunk) : (zwei(q.text))), `${u.id}: Quellen vorhanden`)
}
if (u.typ === 'checkliste') meld(Array.isArray(u.schritte) && u.schritte.every(s => zwei(s.text) && zwei(s.frage) && opt(s.optionen) && s.richtig >= 0 && s.richtig < s.optionen.length), `${u.id}: Schritte mit Prüffrage`)
if (u.typ === 'terminal') meld(Array.isArray(u.schritte) && u.schritte.every(s => zwei(s.text) && (s.muster || s.zustand)), `${u.id}: Terminalschritte`)
```

`WERKZEUGE` ist eine Konstante in verify.mjs: `['tokenizer','temperatur','kontext','vergleich','seite','chunking','embedding','karte','suche','prompt','llm','metrik','schwelle','architektur','kosten','terminal']`; der Datenblock (`ch`) muss vor dem Lab-Block stehen (Reihenfolge in der Datei anpassen).

- [ ] **Step 5: Probe-Übungen und Bedienprobe**

In `data/uebungen/lab-01.json` vorübergehend je Typ eine Probe-Übung (IDs `R01-01` … `R01-05` = quiz, zuordnen, sortieren, rechnen, luecken) anlegen, `lab-01-sprachmodelle.html` mit fünf Platzhaltern; `belegen`, `checkliste` in `lab-02.json` (R02-01, R02-02) und Platzhaltern. Im Browser (`preview_start`, Pfad `/RAG-Lab/lab-01-sprachmodelle.html`) jede Übung richtig und falsch lösen; Sprachwechsel; Fortschrittsmarke erscheint; Startseite zeigt Balken. Dann `node tools/verify.mjs` (Übungen ok, `anzahl` bei Lab 02 noch FEHL — erwartet).

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "Übungstypen zuordnen, sortieren, rechnen, luecken, belegen, checkliste

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: Modell im Browser und die vier Retrieval-Werkzeuge

**Files:**
- Create: `assets/modell.js`, `assets/transformers/` (vendored), `assets/werkzeuge/index.js`, `assets/werkzeuge/gemein.js`, `assets/werkzeuge/tokenizer.js`, `assets/werkzeuge/embedding.js`, `assets/werkzeuge/karte.js`, `assets/werkzeuge/suche.js`
- Modify: `assets/rag.js` (`baueWerkzeuge`, `ctx`), `README.md` (Versionen)

**Interfaces:**
- Produces (`modell.js`): `konfiguriere(basis)`, `zustand()` → `{ tokenizer, modell, fehler, fortschritt }` mit Werten `'aus'|'laedt'|'bereit'|'fehler'`, `ladeTokenizer()`, `tokenisiere(text)` → `{ tokens, ids }`, `ladeModell()`, `embed(text, { praefix })` → `Float32Array(384)` normalisiert, `selbsttest(frage)` → `{ kosinus, ok }`, `beiAenderung(fn)`; Event `rag:modell` mit `detail = zustand()`.
- Produces (`werkzeuge/index.js`): `WERKZEUGE` (Name → `baue`), `baueWerkzeug(ziel, name, parameter, ctx)`.
- Produces (`werkzeuge/gemein.js`): `el`, `txt`, `zwei`, `kopf(wrap, titel, ctx)` → `{ status(text, art) }`, `fmt(n, stellen, lang)`, `ladeKnopf(wrap, ctx, was)` (Knopf mit Fortschritt für `'tokenizer'|'modell'`).
- Consumes: `ctx = { basis, lang(), txt, daten(name) → Promise<json>, modell }` aus `rag.js`.

- [ ] **Step 1: transformers.js vendoren**

```bash
cd /private/tmp/claude-501/*/scratchpad 2>/dev/null || cd /tmp
mkdir -p tf && cd tf && npm pack @huggingface/transformers@4 >/dev/null && tar xzf huggingface-transformers-*.tgz
ls package/dist | head -40
```

Kopieren: die ESM-Datei mit `export` im Kopf (`package/dist/transformers.min.js` oder `transformers.js` — mit `head -c 300` prüfen), alle `package/dist/*.wasm` und zugehörigen `*.mjs`, sowie `package/LICENSE` nach `assets/transformers/`. Wenn die WASM-Dateien nicht im Paket liegen, `npm pack onnxruntime-web@<Version aus package/package.json dependencies>` und `dist/ort-wasm-simd-threaded.wasm` sowie `dist/ort-wasm-simd-threaded.mjs` kopieren. Jede Datei unter 100 MB (`ls -la`). In `README.md` die Versionen von `@huggingface/transformers` und `onnxruntime-web` notieren.

- [ ] **Step 2: `assets/modell.js`**

```js
/**
 * RAG-Lab · Modell im Browser
 *
 * Laedt Tokenizer (17 MB) und Embedding-Modell (118 MB, ONNX q8) von
 * Xenova/multilingual-e5-small ueber den Hugging-Face-Hub - erst auf Klick,
 * danach im Browser-Cache (Cache API). transformers.js ist mitgeliefert,
 * die Gewichte nicht (118 MB liegen ueber der Dateigrenze von GitHub).
 *
 * Nur WASM: q8-Gewichte sind der kleinste Download; WebGPU braeuchte fp16
 * (235 MB). Ohne Cross-Origin-Isolation (GitHub Pages) laeuft WASM einfaedig.
 */
import { env, pipeline, AutoTokenizer } from './transformers/transformers.min.js'

const MODELL = 'Xenova/multilingual-e5-small'
const Z = { tokenizer: 'aus', modell: 'aus', fehler: null, fortschritt: null }
let tok = null, extractor = null, ladeT = null, ladeM = null
const hoerer = new Set()
const melde = () => { const d = zustand(); for (const f of hoerer) f(d); document.dispatchEvent(new CustomEvent('rag:modell', { detail: d })) }

export function konfiguriere (basis = '.') {
  env.allowLocalModels = false
  env.useBrowserCache = true
  env.backends.onnx.wasm.wasmPaths = new URL('assets/transformers/', new URL(basis + '/', location.href)).href
}
export function zustand () { return { ...Z } }
export function beiAenderung (fn) { hoerer.add(fn); return () => hoerer.delete(fn) }

const fortschritt = (p) => { if (p.status === 'progress') { Z.fortschritt = { datei: p.file, prozent: Math.round(p.progress || 0), geladen: p.loaded, gesamt: p.total }; melde() } }

export function ladeTokenizer () {
  if (tok) return Promise.resolve(tok)
  if (!ladeT) {
    Z.tokenizer = 'laedt'; melde()
    ladeT = AutoTokenizer.from_pretrained(MODELL, { progress_callback: fortschritt })
      .then(t => { tok = t; Z.tokenizer = 'bereit'; Z.fortschritt = null; melde(); return t })
      .catch(e => { Z.tokenizer = 'fehler'; Z.fehler = String(e && e.message || e); ladeT = null; melde(); throw e })
  }
  return ladeT
}
export function tokenisiere (text) {
  if (!tok) throw new Error('Tokenizer nicht geladen')
  const ids = tok.encode(text, { add_special_tokens: false })
  const tokens = tok.model && tok.model.convert_ids_to_tokens ? tok.model.convert_ids_to_tokens(ids) : ids.map(i => tok.decode([i]))
  return { tokens, ids }
}
export function ladeModell () {
  if (extractor) return Promise.resolve(extractor)
  if (!ladeM) {
    Z.modell = 'laedt'; melde()
    ladeM = pipeline('feature-extraction', MODELL, { dtype: 'q8', device: 'wasm', progress_callback: fortschritt })
      .then(p => { extractor = p; Z.modell = 'bereit'; Z.fortschritt = null; melde(); return p })
      .catch(e => { Z.modell = 'fehler'; Z.fehler = String(e && e.message || e); ladeM = null; melde(); throw e })
  }
  return ladeM
}
export async function embed (text, { praefix = 'query: ' } = {}) {
  const p = await ladeModell()
  const out = await p(praefix + text, { pooling: 'mean', normalize: true })
  return Float32Array.from(out.data)
}
/** Vergleicht den Browser-Vektor einer Katalogfrage mit dem Python-Vektor aus data/fragen.json. */
export async function selbsttest (frage) {
  const v = await embed(frage.frage, { praefix: 'query: ' })
  let s = 0; for (let i = 0; i < v.length; i++) s += v[i] * frage.vMit[i]
  return { kosinus: s, ok: s > 0.99 }
}
```

Beim Ausführen prüfen: heißt die ESM-Datei anders, den Import anpassen; liefert `tok.encode` in 4.x ein Objekt statt Array, `Array.from(tok(text, { add_special_tokens: false }).input_ids.data)` verwenden. Nach dem ersten Laden im Browser `selbsttest` mit `fehler-e18` aufrufen: Kosinus > 0,99 erwartet. Liegt er darunter, `pooling`/`normalize`-Optionen gegen die Modellkarte prüfen (e5 nutzt Mean Pooling über die Attention-Maske).

- [ ] **Step 3: Registry und Helfer**

`assets/werkzeuge/gemein.js`:

```js
export const el = (tag, klasse, text) => { const n = document.createElement(tag); if (klasse) n.className = klasse; if (text != null) n.textContent = text; return n }
export const txt = (o, lang) => (o == null ? '' : typeof o === 'string' ? o : (o[lang] ?? o.de ?? ''))
export function zwei (node, o, ctx, attr) {
  const setze = () => { const t = txt(o, ctx.lang()); if (attr) node.setAttribute(attr, t); else node.textContent = t }
  setze(); document.addEventListener('rag:sprache', setze); return node
}
export const fmt = (n, stellen = 2, lang = 'de') => Number(n).toLocaleString(lang === 'de' ? 'de-DE' : 'en-GB', { minimumFractionDigits: stellen, maximumFractionDigits: stellen })
export const prozent = (x, lang = 'de') => fmt(x * 100, 0, lang) + ' %'
/** Kopfzeile eines Werkzeugs mit Titel und Statuszeile; art: 'live' | 'vorberechnet' | '' */
export function kopf (wrap, titel, ctx) {
  const k = el('div', 'werkzeug-kopf'); const t = zwei(el('span', 'titel'), titel, ctx); const s = el('span', 'werkzeug-status'); k.append(t, s); wrap.append(k)
  return { status: (o, art = '') => { s.className = 'werkzeug-status ' + art; s.textContent = txt(o, ctx.lang()) } }
}
const L = {
  tokenizerLaden: { de: 'Tokenizer laden (17 MB)', en: 'Load tokenizer (17 MB)' },
  modellLaden: { de: 'Modell laden (118 MB, einmalig)', en: 'Load model (118 MB, once)' },
  laedt: { de: 'Lädt …', en: 'Loading …' }, bereit: { de: 'Bereit', en: 'Ready' },
  fehler: { de: 'Laden fehlgeschlagen. Der Hugging-Face-Hub ist nicht erreichbar; das Werkzeug arbeitet mit den vorberechneten Daten weiter.', en: 'Loading failed. The Hugging Face Hub is unreachable; the tool continues with precomputed data.' },
  hinweis: { de: 'Die Dateien kommen vom Hugging-Face-Hub und bleiben danach im Cache Ihres Browsers.', en: 'The files come from the Hugging Face Hub and stay in your browser cache afterwards.' }
}
/** Ladeknopf mit Fortschrittsbalken; was = 'tokenizer' | 'modell'. Ruft beiBereit() sofort, wenn schon geladen. */
export function ladeKnopf (wrap, ctx, was, beiBereit) {
  const M = ctx.modell; const zeile = el('div', 'lade-zeile')
  const b = zwei(el('button', 'btn-sm primary'), was === 'modell' ? L.modellLaden : L.tokenizerLaden, ctx); b.type = 'button'
  const balken = el('div', 'balken'); const i = el('i'); balken.append(i); balken.hidden = true
  const info = zwei(el('span', 'line-hilfe'), L.hinweis, ctx); zeile.append(b, balken, info); wrap.append(zeile)
  const zeichne = (z) => {
    const st = z[was]
    b.hidden = st === 'bereit' || st === 'laedt'; balken.hidden = st !== 'laedt'
    if (st === 'laedt' && z.fortschritt) i.style.width = z.fortschritt.prozent + '%'
    if (st === 'fehler') { info.className = 'line-hilfe warn'; info.textContent = txt(L.fehler, ctx.lang()) }
    if (st === 'bereit') { zeile.hidden = true; beiBereit() }
  }
  zeichne(M.zustand()); M.beiAenderung(zeichne)
  b.addEventListener('click', () => (was === 'modell' ? M.ladeModell() : M.ladeTokenizer()).catch(() => {}))
}
```

`assets/werkzeuge/index.js`:

```js
import { el } from './gemein.js'
import * as tokenizer from './tokenizer.js'
import * as embedding from './embedding.js'
import * as karte from './karte.js'
import * as suche from './suche.js'
// Aufgabe 6 ergaenzt: temperatur, kontext, vergleich, seite, chunking, prompt, llm, metrik, schwelle, architektur, kosten
// Aufgabe 7 ergaenzt: terminal
export const WERKZEUGE = { tokenizer: tokenizer.baue, embedding: embedding.baue, karte: karte.baue, suche: suche.baue }
export function baueWerkzeug (ziel, name, parameter, ctx) {
  const b = WERKZEUGE[name]
  const wrap = el('div', 'werkzeug'); wrap.dataset.werkzeug = name; ziel.replaceChildren(wrap)
  if (!b) { wrap.textContent = 'Unbekanntes Werkzeug: ' + name; return null }
  return b(wrap, parameter || {}, ctx)
}
```

In `assets/rag.js`: `import * as Modell from './modell.js'`, `import { baueWerkzeug } from './werkzeuge/index.js'`; ein Datenlader mit Cache:

```js
const datenCache = new Map()
export function ladeDaten (name) {
  if (!datenCache.has(name)) datenCache.set(name, fetch(url('data/' + name)).then(r => { if (!r.ok) throw new Error(name); return name.endsWith('.md') ? r.text() : r.json() }))
  return datenCache.get(name)
}
function erzeugeCtx (lab, basis) { Modell.konfiguriere(basis); return { lab, basis, lang: aktuelleSprache, txt, daten: ladeDaten, modell: Modell } }
function baueWerkzeuge (root, ctx) {
  for (const z of root.querySelectorAll('[data-werkzeug]')) {
    let p = {}; try { p = JSON.parse(z.dataset.parameter || '{}') } catch { console.warn('data-parameter ungültig', z) }
    baueWerkzeug(z, z.dataset.werkzeug, p, ctx)
  }
}
```

`starteLab` ruft `baueWerkzeuge(document, ctx)` vor den Übungsboxen; der `experiment`-Zweig aus Aufgabe 4 ruft `baueWerkzeug(wz, uebung.werkzeug, uebung.parameter, ctx)`.

- [ ] **Step 4: Werkzeug `tokenizer`**

`assets/werkzeuge/tokenizer.js`:

```js
import { el, txt, zwei, kopf, ladeKnopf, fmt } from './gemein.js'
const L = {
  titel: { de: 'Tokenizer', en: 'Tokenizer' }, beispiel: { de: 'Beispiel wählen', en: 'Choose an example' },
  vorberechnet: { de: 'Vorberechnete Beispiele', en: 'Precomputed examples' }, live: { de: 'Live im Browser', en: 'Live in the browser' },
  anzahl: { de: 'Tokens', en: 'tokens' }, zeichen: { de: 'Zeichen', en: 'characters' }, ids: { de: 'IDs anzeigen', en: 'Show IDs' },
  eingabe: { de: 'Eigener Text (nach dem Laden des Tokenizers)', en: 'Your own text (after loading the tokenizer)' }
}
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx)
  const auswahl = el('select'); const ta = el('textarea'); ta.rows = 2; ta.value = p.text || 'Was bedeutet E:18?'; zwei(ta, L.eingabe, ctx, 'aria-label')
  const ausgabe = el('div', 'tokens'); const zaehl = el('div', 'line-hilfe'); const idsBox = el('label'); const idsCb = el('input'); idsCb.type = 'checkbox'; idsBox.append(idsCb, zwei(el('span'), L.ids, ctx))
  wrap.append(auswahl, ta, idsBox, ausgabe, zaehl)
  let beispiele = {}
  const zeige = (tokens, ids, text) => {
    ausgabe.replaceChildren(...tokens.map((t, i) => el('span', null, (idsCb.checked ? ids[i] + ' ' : '') + t.replace(/^▁/, '␣'))))
    zaehl.textContent = `${tokens.length} ${txt(L.anzahl, ctx.lang())} · ${text.length} ${txt(L.zeichen, ctx.lang())} · ${fmt(text.length / Math.max(1, tokens.length), 1, ctx.lang())} ${txt(L.zeichen, ctx.lang())}/Token`
  }
  let live = false
  const aktualisiere = () => {
    if (live) { const r = ctx.modell.tokenisiere(ta.value); zeige(r.tokens, r.ids, ta.value); return }
    const b = beispiele[auswahl.value]; if (b) { ta.value = b.text; zeige(b.tokens, b.ids, b.text) }
  }
  ctx.daten('tokens.json').then(d => {
    beispiele = d.beispiele
    for (const [key, b] of Object.entries(beispiele)) { const o = el('option', null, b.text); o.value = key; auswahl.append(o) }
    if (p.beispiel && beispiele[p.beispiel]) auswahl.value = p.beispiel
    k.status(L.vorberechnet, 'vorberechnet'); aktualisiere()
  })
  auswahl.addEventListener('change', aktualisiere); idsCb.addEventListener('change', aktualisiere)
  ta.addEventListener('input', () => { if (live) aktualisiere() })
  ladeKnopf(wrap, ctx, 'tokenizer', () => { live = true; auswahl.hidden = true; k.status(L.live, 'live'); aktualisiere() })
}
```

- [ ] **Step 5: Werkzeug `embedding`**

`assets/werkzeuge/embedding.js`:

```js
import { el, txt, zwei, kopf, ladeKnopf, fmt } from './gemein.js'
import { kosinus, topK } from '../pruefung.js'
const L = {
  titel: { de: 'Embedding und Kosinusähnlichkeit', en: 'Embedding and cosine similarity' },
  a: { de: 'Text A', en: 'Text A' }, b: { de: 'Text B', en: 'Text B' },
  praefix: { de: 'Präfixe query:/passage: verwenden', en: 'Use query:/passage: prefixes' },
  vergleichen: { de: 'Vergleichen', en: 'Compare' }, katalog: { de: 'Ohne Modell: zwei Katalogfragen vergleichen', en: 'Without the model: compare two catalogue questions' },
  live: { de: 'Live im Browser', en: 'Live in the browser' }, vorberechnet: { de: 'Vorberechnete Vektoren', en: 'Precomputed vectors' },
  kos: { de: 'Kosinusähnlichkeit', en: 'Cosine similarity' }, dims: { de: 'erste 8 von 384 Dimensionen', en: 'first 8 of 384 dimensions' },
  naechste: { de: 'Nächste Handbuch-Chunks zu A', en: 'Closest manual chunks to A' }, rechnet: { de: 'Rechnet …', en: 'Computing …' }
}
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx)
  const felder = el('div', 'zwei-spalten')
  const ta = el('textarea'); ta.rows = 2; ta.value = p.a || 'Laugenpumpe verstopft'; zwei(ta, L.a, ctx, 'aria-label')
  const tb = el('textarea'); tb.rows = 2; tb.value = p.b || 'Die Pumpe ist blockiert'; zwei(tb, L.b, ctx, 'aria-label')
  felder.append(ta, tb)
  const cb = el('input'); cb.type = 'checkbox'; cb.checked = p.praefix !== false; const cbl = el('label'); cbl.append(cb, zwei(el('span'), L.praefix, ctx))
  const btn = zwei(el('button', 'btn-sm primary'), L.vergleichen, ctx); btn.type = 'button'
  const katalog = el('div'); const sa = el('select'), sb = el('select'); katalog.append(zwei(el('div', 'line-hilfe'), L.katalog, ctx), sa, sb)
  const erg = el('div', 'ergebnis'); wrap.append(felder, cbl, btn, katalog, erg)
  let fragen = [], chunks = []
  Promise.all([ctx.daten('fragen.json'), ctx.daten('chunks.json')]).then(([f, c]) => {
    fragen = f.fragen; chunks = c.chunks
    for (const s of [sa, sb]) for (const q of fragen) { const o = el('option', null, q.frage); o.value = q.id; s.append(o) }
    sb.selectedIndex = Math.min(1, fragen.length - 1); k.status(L.vorberechnet, 'vorberechnet'); zeigeKatalog()
  })
  const zeigeVektor = (v) => el('code', null, '[' + Array.from(v.slice(0, 8)).map(x => fmt(x, 3, ctx.lang())).join(', ') + ', …]')
  const zeige = (va, vb, mitChunks) => {
    erg.replaceChildren()
    const z = el('p'); z.append(el('strong', null, txt(L.kos, ctx.lang()) + ': ' + fmt(kosinus(va, vb), 4, ctx.lang())))
    const d = el('p', 'line-hilfe'); d.append(txt(L.dims, ctx.lang()) + ' A ', zeigeVektor(va), ' B ', zeigeVektor(vb)); erg.append(z, d)
    if (mitChunks && chunks.length) {
      const ol = el('ol'); for (const t of topK(va, chunks, 3)) { const c = chunks.find(x => x.id === t.id); ol.append(el('li', null, `${fmt(t.score, 3, ctx.lang())} · ${c.abschnitt || c.id}: ${c.text.slice(0, 90)}…`)) }
      erg.append(zwei(el('h4'), L.naechste, ctx), ol)
    }
  }
  const zeigeKatalog = () => { const a = fragen.find(q => q.id === sa.value) || fragen[0], b = fragen.find(q => q.id === sb.value) || fragen[1]; if (a && b) zeige(cb.checked ? a.vMit : a.vOhne, cb.checked ? b.vMit : b.vOhne, false) }
  sa.addEventListener('change', zeigeKatalog); sb.addEventListener('change', zeigeKatalog)
  let live = false
  btn.addEventListener('click', async () => {
    if (!live) { zeigeKatalog(); return }
    erg.textContent = txt(L.rechnet, ctx.lang())
    const pa = cb.checked ? 'query: ' : '', pb = cb.checked ? 'passage: ' : ''
    const [va, vb] = await Promise.all([ctx.modell.embed(ta.value, { praefix: pa }), ctx.modell.embed(tb.value, { praefix: pb })])
    zeige(va, vb, cb.checked)
  })
  cb.addEventListener('change', () => { if (!live) zeigeKatalog() })
  ladeKnopf(wrap, ctx, 'modell', () => { live = true; katalog.hidden = true; k.status(L.live, 'live') })
}
```

- [ ] **Step 6: Werkzeug `karte`**

`assets/werkzeuge/karte.js` (SVG-Streudiagramm, Farben nach den sechs häufigsten Abschnitten):

```js
import { el, txt, zwei, kopf, ladeKnopf } from './gemein.js'
import { projiziere, topK } from '../pruefung.js'
const L = { titel: { de: 'Vektorraum-Karte der 346 Chunks (PCA)', en: 'Vector-space map of the 346 chunks (PCA)' },
  frage: { de: 'Frage', en: 'Question' }, projizieren: { de: 'Projizieren', en: 'Project' }, andere: { de: 'übrige Abschnitte', en: 'other sections' },
  varianz: { de: 'erklärte Varianz', en: 'explained variance' }, live: { de: 'Live: eigene Frage', en: 'Live: your own question' }, vorberechnet: { de: 'Katalogfragen', en: 'Catalogue questions' } }
const FARBEN = ['#C4602A', '#2E2418', '#6B6A1C', '#9E4A1C', '#4A6B8A', '#7A4E7A']
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx)
  const zeile = el('div', 'werkzeug-kopf'); const sel = el('select'); const ta = el('input'); ta.type = 'text'; zwei(ta, L.frage, ctx, 'aria-label'); ta.hidden = true
  const btn = zwei(el('button', 'btn-sm primary'), L.projizieren, ctx); btn.type = 'button'; zeile.append(sel, ta, btn)
  const svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg'); svg.setAttribute('viewBox', '0 0 720 460'); svg.setAttribute('class', 'karte'); svg.setAttribute('role', 'img')
  const legende = el('div', 'legende'); const tip = el('div', 'tip-box'); tip.hidden = true; wrap.append(zeile, svg, legende, tip)
  let pr, chunks, fragen, skala
  const ns = (t) => document.createElementNS('http://www.w3.org/2000/svg', t)
  Promise.all([ctx.daten('projektion.json'), ctx.daten('chunks.json'), ctx.daten('fragen.json')]).then(([a, b, c]) => {
    pr = a; chunks = b.chunks; fragen = c.fragen
    const xs = pr.punkte.map(q => q.x), ys = pr.punkte.map(q => q.y)
    const [x0, x1, y0, y1] = [Math.min(...xs), Math.max(...xs), Math.min(...ys), Math.max(...ys)]
    skala = ([x, y]) => [30 + (x - x0) / (x1 - x0) * 660, 430 - (y - y0) / (y1 - y0) * 400]
    const zaehl = new Map(); for (const c of chunks) zaehl.set(c.abschnitt, (zaehl.get(c.abschnitt) || 0) + 1)
    const top = [...zaehl.entries()].sort((a, b) => b[1] - a[1]).slice(0, 5).map(e => e[0])
    const farbe = (c) => top.includes(c.abschnitt) ? FARBEN[top.indexOf(c.abschnitt)] : '#C9C3B4'
    for (const q of pr.punkte) {
      const c = chunks.find(x => x.id === q.id); const [x, y] = skala([q.x, q.y])
      const dot = ns('circle'); dot.setAttribute('cx', x); dot.setAttribute('cy', y); dot.setAttribute('r', 4); dot.setAttribute('fill', farbe(c)); dot.setAttribute('opacity', '0.8'); dot.dataset.id = q.id
      dot.addEventListener('mouseenter', () => { tip.hidden = false; tip.textContent = `${c.id} · ${c.abschnitt || '—'}: ${c.text.slice(0, 160)}` }); svg.append(dot)
    }
    legende.replaceChildren(...top.map((t, i) => { const s = el('span'); s.style.color = FARBEN[i]; s.textContent = '● ' + (t || '—'); return s }), zwei(el('span'), L.andere, ctx))
    const v = el('span', 'line-hilfe'); v.textContent = ` · ${txt(L.varianz, ctx.lang())}: ${(pr.varianz[0] * 100).toFixed(1)} % + ${(pr.varianz[1] * 100).toFixed(1)} %`; legende.append(v)
    for (const f of fragen) { const o = el('option', null, f.frage); o.value = f.id; sel.append(o) }
    if (p.frage) sel.value = p.frage
    k.status(L.vorberechnet, 'vorberechnet'); zeichneFrage()
  })
  let stern = null, linien = []
  const zeichnePunkt = (v, label) => {
    for (const l of linien) l.remove(); linien = []; if (stern) stern.remove()
    const [x, y] = skala(projiziere(Array.from(v), pr))
    for (const t of topK(Array.from(v), chunks, 5)) { const q = pr.punkte.find(z => z.id === t.id); const [tx, ty] = skala([q.x, q.y]); const l = ns('line'); l.setAttribute('x1', x); l.setAttribute('y1', y); l.setAttribute('x2', tx); l.setAttribute('y2', ty); l.setAttribute('stroke', '#C4602A'); l.setAttribute('stroke-width', '1.5'); svg.append(l); linien.push(l) }
    stern = ns('text'); stern.setAttribute('x', x); stern.setAttribute('y', y + 6); stern.setAttribute('font-size', '22'); stern.setAttribute('text-anchor', 'middle'); stern.textContent = '★'; stern.append(); svg.append(stern)
    const t = ns('title'); t.textContent = label; stern.append(t)
  }
  const zeichneFrage = () => { const f = fragen.find(q => q.id === sel.value); if (f) zeichnePunkt(f.vMit, f.frage) }
  sel.addEventListener('change', zeichneFrage)
  let live = false
  btn.addEventListener('click', async () => { if (!live) { zeichneFrage(); return } zeichnePunkt(await ctx.modell.embed(ta.value), ta.value) })
  ladeKnopf(wrap, ctx, 'modell', () => { live = true; sel.hidden = true; ta.hidden = false; ta.value = ta.value || 'Wasser läuft aus'; k.status(L.live, 'live') })
}
```

CSS ergänzen: `.karte { width: 100%; height: auto; background: var(--bg-soft); border: 1px solid var(--line); border-radius: 8px; } .legende { display: flex; flex-wrap: wrap; gap: 0.8rem; font-size: 0.8rem; margin-top: 0.4rem; } .zwei-spalten { display: grid; grid-template-columns: 1fr; gap: 0.6rem; } @media (min-width: 800px) { .zwei-spalten { grid-template-columns: 1fr 1fr; } }`

- [ ] **Step 7: Werkzeug `suche` (der Such-Stepper)**

`assets/werkzeuge/suche.js`:

```js
import { el, txt, zwei, kopf, ladeKnopf, fmt } from './gemein.js'
import { topK, hybridKandidaten, waehleKontext, istGedeckt, fehlercodes } from '../pruefung.js'
const L = {
  titel: { de: 'Such-Stepper: von der Frage zum Kontext', en: 'Search stepper: from question to context' },
  frage: { de: 'Frage', en: 'Question' }, suchen: { de: 'Suchen', en: 'Search' },
  s1: { de: '1 · Vektorsuche, Top 12', en: '1 · Vector search, top 12' }, s2: { de: '2 · Fehlercode-Zuschlag (Hybrid)', en: '2 · Error-code boost (hybrid)' },
  s3: { de: '3 · Reranker (Cross-Encoder), vorberechnet', en: '3 · Reranker (cross-encoder), precomputed' }, s4: { de: '4 · Schwelle und Verhältnis → Kontext', en: '4 · Threshold and ratio → context' },
  s5: { de: '5 · Gedeckt?', en: '5 · Grounded?' }, schwelle: { de: 'Schwelle', en: 'Threshold' }, verhaeltnis: { de: 'Verhältnis', en: 'Ratio' },
  exakt: { de: 'exakter Code', en: 'exact code' }, ja: { de: 'Ja: Antwort wird erzeugt.', en: 'Yes: an answer is generated.' },
  nein: { de: 'Nein: „Dazu finde ich in dieser Bedienungsanleitung leider keine Information.“ Kein LLM-Aufruf.', en: 'No: “I cannot find that in this manual.” No LLM call.' },
  keinRerank: { de: 'Für eigene Fragen liegt kein Reranker-Score vor (279 MB, läuft nicht im Browser); Stufe 3 zeigt die Hybrid-Reihenfolge unverändert.', en: 'No reranker score exists for your own questions (279 MB, does not run in the browser); stage 3 shows the hybrid order unchanged.' },
  live: { de: 'Live: Vektor- und Hybridstufe im Browser', en: 'Live: vector and hybrid stages in the browser' }, vorberechnet: { de: 'Katalogfragen, alle Stufen vorberechnet', en: 'Catalogue questions, all stages precomputed' },
  codes: { de: 'erkannte Codes', en: 'codes found' }, keine: { de: 'keine', en: 'none' }
}
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx)
  const zeile = el('div', 'werkzeug-kopf'); const sel = el('select'); const inp = el('input'); inp.type = 'text'; inp.hidden = true; zwei(inp, L.frage, ctx, 'aria-label')
  const btn = zwei(el('button', 'btn-sm primary'), L.suchen, ctx); btn.type = 'button'; zeile.append(sel, inp, btn)
  const regler = el('div', 'regler')
  const rs = el('input'); rs.type = 'range'; rs.min = 0; rs.max = 1; rs.step = 0.005; rs.value = p.schwelle ?? 0.15
  const rv = el('input'); rv.type = 'range'; rv.min = 0; rv.max = 1; rv.step = 0.05; rv.value = p.verhaeltnis ?? 0.5
  const ls = el('label'); ls.append(zwei(el('span'), L.schwelle, ctx), el('output'), rs); const lv = el('label'); lv.append(zwei(el('span'), L.verhaeltnis, ctx), el('output'), rv); regler.append(ls, lv)
  const stufen = el('div', 'stufen'); wrap.append(zeile, regler, stufen)
  let fragen = [], chunks = [], aktuell = null
  const text = (id) => chunks.find(c => c.id === id).text
  const liste = (titel, eintraege, hervor = new Set()) => {
    const box = el('div', 'stufe'); box.append(zwei(el('h4'), titel, ctx)); const ol = el('ol')
    for (const e of eintraege) { const c = chunks.find(x => x.id === e.id); const li = el('li'); if (hervor.has(e.id)) li.className = 'im-kontext'
      li.append(el('span', 'score', e.score == null ? '' : fmt(e.score, 4, ctx.lang())), e.exakt ? zwei(el('span', 'badge'), L.exakt, ctx) : '', el('span', 'chunk-id', c.id), el('span', 'snippet', `${c.abschnitt ? c.abschnitt + ' · ' : ''}${c.text.replace(/\n/g, ' ').slice(0, 110)}`)); ol.append(li) }
    box.append(ol); return box
  }
  const zeige = () => {
    if (!aktuell) return
    const { frage, vektor, hybrid, rerank } = aktuell
    ls.querySelector('output').value = fmt(+rs.value, 3, ctx.lang()); lv.querySelector('output').value = fmt(+rv.value, 2, ctx.lang())
    const kontext = waehleKontext(rerank, { schwelle: +rs.value, verhaeltnis: +rv.value })
    const im = new Set(kontext.map(x => x.id)); const gedeckt = istGedeckt(frage, kontext, text, { schwelle: +rs.value })
    const codes = [...fehlercodes(frage)].filter(c => c.includes(':'))
    stufen.replaceChildren(liste(L.s1, vektor), el('p', 'line-hilfe', `${txt(L.codes, ctx.lang())}: ${codes.length ? codes.join(', ') : txt(L.keine, ctx.lang())}`), liste(L.s2, hybrid), liste(L.s3, rerank.slice(0, 5), im))
    if (aktuell.ohneRerank) stufen.append(zwei(el('p', 'warn-box'), L.keinRerank, ctx))
    stufen.append(liste(L.s4, kontext, im))
    const g = el('div', gedeckt ? 'challenge-box' : 'warn-box'); g.append(zwei(el('h4'), L.s5, ctx), zwei(el('p'), gedeckt ? L.ja : L.nein, ctx)); stufen.append(g)
  }
  rs.addEventListener('input', zeige); rv.addEventListener('input', zeige)
  Promise.all([ctx.daten('fragen.json'), ctx.daten('chunks.json')]).then(([f, c]) => {
    fragen = f.fragen; chunks = c.chunks
    for (const q of fragen) { const o = el('option', null, q.frage); o.value = q.id; sel.append(o) }
    if (p.frage) sel.value = p.frage
    k.status(L.vorberechnet, 'vorberechnet'); katalog()
  })
  const katalog = () => { const f = fragen.find(q => q.id === sel.value); if (!f) return
    aktuell = { frage: f.frage, vektor: f.vektor.map(t => ({ id: t.chunk, score: t.score })), hybrid: f.hybrid.map(t => ({ id: t.chunk, score: t.score, exakt: t.exakt })), rerank: f.rerank.map(t => ({ id: t.chunk, score: t.score, exakt: t.exakt })), ohneRerank: false }; zeige() }
  sel.addEventListener('change', katalog)
  let live = false
  btn.addEventListener('click', async () => {
    if (!live) { katalog(); return }
    const frage = inp.value.trim(); if (!frage) return
    const treffer = fragen.find(q => q.frage.trim().toLowerCase() === frage.toLowerCase())
    if (treffer) { sel.value = treffer.id; katalog(); return }
    const v = Array.from(await ctx.modell.embed(frage)); const vektor = topK(v, chunks, 12); const hybrid = hybridKandidaten(vektor, chunks, frage)
    aktuell = { frage, vektor, hybrid, rerank: hybrid, ohneRerank: true }; zeige()
  })
  ladeKnopf(wrap, ctx, 'modell', () => { live = true; inp.hidden = false; inp.value = fragen.find(q => q.id === sel.value)?.frage || ''; k.status(L.live, 'live') })
}
```

CSS: `.stufen .stufe { margin: 0.8rem 0; } .stufen ol { margin-left: 1.2rem; } .stufen li { font-size: 0.85rem; display: grid; grid-template-columns: 60px auto 44px 1fr; gap: 0.5rem; align-items: baseline; padding: 0.2rem 0; border-bottom: 1px dashed var(--line); } .stufen li.im-kontext { background: var(--signal-l); } .stufen .score { font-family: var(--mono); } .stufen .chunk-id { font-family: var(--mono); color: var(--ink-soft); } .regler { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; margin: 0.5rem 0; } .regler label { display: grid; gap: 0.2rem; font-size: 0.85rem; } .regler output { font-family: var(--mono); }`

- [ ] **Step 8: Bedienprobe und Commit**

Seite `lab-06-retrieval.html` vorübergehend mit `<div data-werkzeug="suche" data-parameter='{"frage":"fehler-e18"}'></div>` und den anderen drei Werkzeugen versehen; im Browser: Katalogfragen durchschalten, Regler bewegen (E:18 bleibt gedeckt; Schwelle 0,6 → nicht gedeckt), Modell laden (Fortschritt sichtbar), eigene Frage „Pumpe blockiert“ suchen (Stufen 1–2 live, Hinweis zu Stufe 3), Selbsttest in der Konsole: `(await import('./assets/modell.js')).selbsttest((await (await fetch('data/fragen.json')).json()).fragen[1])` → `ok: true`. Bei Fehler im Hub-Ladeweg (Proxy): Fallback muss weiter funktionieren.

```bash
git add -A && git commit -m "Modell im Browser: transformers.js, Tokenizer, Embedding, Karte, Such-Stepper

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: Die übrigen elf Werkzeuge

**Files:**
- Create: `assets/werkzeuge/temperatur.js`, `kontext.js`, `vergleich.js`, `seite.js`, `chunking.js`, `prompt.js`, `llm.js`, `metrik.js`, `schwelle.js`, `architektur.js`, `kosten.js`
- Modify: `assets/werkzeuge/index.js`, `assets/rag.css`

**Interfaces:**
- Consumes: `gemein.js`, `pruefung.js`, `chunking.js`, `data/*.json`.
- Produces: elf `baue(wrap, p, ctx)`-Funktionen; Parameter je Werkzeug unten.

- [ ] **Step 1: `temperatur.js`** (Parameter `prompt`: ID aus logits.json)

```js
import { el, txt, zwei, kopf, fmt } from './gemein.js'
import { softmax } from '../pruefung.js'
const L = { titel: { de: 'Nächstes Token und Temperatur', en: 'Next token and temperature' }, temp: { de: 'Temperatur', en: 'Temperature' },
  ziehen: { de: 'Stichprobe ziehen', en: 'Draw a sample' }, gezogen: { de: 'gezogen', en: 'drawn' },
  rest: { de: 'Bei T = 1 liegen {p} der Wahrscheinlichkeit außerhalb dieser 20 Tokens (über alle {n} Tokens des Vokabulars gerechnet).', en: 'At T = 1, {p} of the probability lies outside these 20 tokens (computed over all {n} vocabulary tokens).' },
  fort: { de: 'Aufgezeichnete Fortsetzungen', en: 'Recorded continuations' }, modell: { de: 'Modell', en: 'Model' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const sel = el('select'); const promptBox = el('pre', 'code-block')
  const lab = el('label'); const r = el('input'); r.type = 'range'; r.min = 0; r.max = 2; r.step = 0.05; r.value = p.temperatur ?? 1; const out = el('output'); lab.append(zwei(el('span'), L.temp, ctx), out, r)
  const btn = zwei(el('button', 'btn-sm'), L.ziehen, ctx); btn.type = 'button'; const balken = el('div', 'verteilung'); const rest = el('p', 'line-hilfe'); const fort = el('div')
  wrap.append(sel, promptBox, lab, btn, balken, rest, fort)
  let d, aktuell, gezogen = null
  const zeige = () => {
    const T = +r.value; out.value = fmt(T, 2, ctx.lang()); const pr = softmax(aktuell.tokens.map(t => t.logit), T)
    balken.replaceChildren(...aktuell.tokens.map((t, i) => { const z = el('div', 'v-zeile' + (gezogen === i ? ' gezogen' : '')); const b = el('div', 'balken'); const f = el('i'); f.style.width = (pr[i] * 100) + '%'; b.append(f)
      z.append(el('code', null, JSON.stringify(t.token)), b, el('span', null, fmt(pr[i] * 100, 1, ctx.lang()) + ' %')); return z }))
    const m = Math.max(...aktuell.tokens.map(t => t.logit)); const top = aktuell.tokens.reduce((s, t) => s + Math.exp(t.logit - m), 0); const alle = Math.exp(aktuell.restLogsumexp - m)
    rest.textContent = txt(L.rest, ctx.lang()).replace('{p}', fmt((1 - top / alle) * 100, 1, ctx.lang()) + ' %').replace('{n}', d.vokabular ? fmt(d.vokabular, 0, ctx.lang()) : '151 936')
    fort.replaceChildren(zwei(el('h4'), L.fort, ctx), ...Object.entries(aktuell.fortsetzungen).map(([t, s]) => { const q = el('p'); q.append(el('strong', null, `T = ${t}: `), el('span', null, aktuell.prompt + ' '), el('em', null, s)); return q }))
  }
  ctx.daten('logits.json').then(x => { d = x; for (const q of d.prompts) { const o = el('option', null, q.prompt); o.value = q.id; sel.append(o) } if (p.prompt) sel.value = p.prompt
    k.status({ de: `${txt(L.modell, 'de')}: ${d.modell}, ${d.datum}`, en: `${txt(L.modell, 'en')}: ${d.modell}, ${d.datum}` }, 'vorberechnet'); wechsel() })
  const wechsel = () => { aktuell = d.prompts.find(q => q.id === sel.value) || d.prompts[0]; promptBox.textContent = aktuell.prompt + ' ▁'; gezogen = null; zeige() }
  sel.addEventListener('change', wechsel); r.addEventListener('input', () => { gezogen = null; zeige() })
  btn.addEventListener('click', () => { const pr = softmax(aktuell.tokens.map(t => t.logit), +r.value); let u = Math.random(), i = 0; while (i < pr.length - 1 && (u -= pr[i]) > 0) i++; gezogen = i; zeige() })
}
```

`tools/logits.py` schreibt zusätzlich `"vokabular": len(tok)` in den Kopf (nachziehen). CSS: `.verteilung .v-zeile { display: grid; grid-template-columns: 140px 1fr 60px; gap: 0.5rem; align-items: center; font-size: 0.85rem; padding: 0.1rem 0; } .v-zeile.gezogen { background: var(--signal-l); }`

- [ ] **Step 2: `kontext.js`** (Parameter `fenster` in Tokens, `k`)

```js
import { el, txt, zwei, kopf, fmt } from './gemein.js'
const L = { titel: { de: 'Kontextfenster-Füllstand', en: 'Context window fill level' }, fenster: { de: 'Kontextfenster (Tokens)', en: 'Context window (tokens)' },
  k: { de: 'Chunks im Kontext (FINAL_K)', en: 'Chunks in context (FINAL_K)' }, antwort: { de: 'Antwortbudget (ANSWER_MAX_TOKENS)', en: 'Answer budget (ANSWER_MAX_TOKENS)' },
  teile: { system: { de: 'Systemprompt', en: 'System prompt' }, frage: { de: 'Frage', en: 'Question' }, chunks: { de: 'Handbuchauszüge', en: 'Manual excerpts' }, antwort: { de: 'Antwort', en: 'Answer' }, frei: { de: 'frei', en: 'free' } },
  ueber: { de: 'Das passt nicht: {n} Tokens zu viel.', en: 'This does not fit: {n} tokens too many.' }, hinweis: { de: 'Tokenzahlen mit dem e5-Tokenizer gemessen (tokens.json); Chatmodelle zählen etwas anders, die Größenordnung bleibt.', en: 'Token counts measured with the e5 tokenizer (tokens.json); chat models count slightly differently, the order of magnitude stays.' } }
const FENSTER = [4096, 8192, 32768, 131072]
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const f = el('select'); for (const n of FENSTER) { const o = el('option', null, n.toLocaleString('de-DE')); o.value = n; f.append(o) } f.value = p.fenster || 8192
  const rk = el('input'); rk.type = 'range'; rk.min = 1; rk.max = 12; rk.value = p.k || 5; const ra = el('input'); ra.type = 'range'; ra.min = 128; ra.max = 4096; ra.step = 128; ra.value = 1024
  const lf = el('label'); lf.append(zwei(el('span'), L.fenster, ctx), f); const lk = el('label'); lk.append(zwei(el('span'), L.k, ctx), el('output'), rk); const la = el('label'); la.append(zwei(el('span'), L.antwort, ctx), el('output'), ra)
  const balken = el('div', 'fuellstand'); const legende = el('div', 'legende'); const meld = el('p', 'line-hilfe'); wrap.append(el('div', 'regler', ), balken, legende, meld); wrap.querySelector('.regler').append(lf, lk, la)
  let z
  const zeige = () => {
    const fenster = +f.value, kk = +rk.value, ant = +ra.value; lk.querySelector('output').value = kk; la.querySelector('output').value = ant
    const mittel = z.chunkTokens.reduce((a, b) => a + b, 0) / z.chunkTokens.length
    const teile = [['system', z.systemprompt, '#2E2418'], ['frage', 14, '#9E4A1C'], ['chunks', Math.round(kk * mittel), '#C4602A'], ['antwort', ant, '#6B6A1C']]
    const summe = teile.reduce((s, t) => s + t[1], 0); balken.replaceChildren(); legende.replaceChildren()
    for (const [name, n, farbe] of teile) { const seg = el('i'); seg.style.width = Math.min(100, n / fenster * 100) + '%'; seg.style.background = farbe; balken.append(seg); const l = el('span'); l.style.color = farbe; l.textContent = `■ ${txt(L.teile[name], ctx.lang())} ${n.toLocaleString('de-DE')}`; legende.append(l) }
    const frei = fenster - summe; const l = el('span'); l.textContent = `□ ${txt(L.teile.frei, ctx.lang())} ${Math.max(0, frei).toLocaleString('de-DE')}`; legende.append(l)
    meld.textContent = frei < 0 ? txt(L.ueber, ctx.lang()).replace('{n}', (-frei).toLocaleString('de-DE')) : txt(L.hinweis, ctx.lang()); meld.className = frei < 0 ? 'warn-box' : 'line-hilfe'
  }
  ctx.daten('tokens.json').then(d => { z = d.zaehlungen; k.status({ de: 'aus tokens.json', en: 'from tokens.json' }, 'vorberechnet'); zeige() })
  for (const e of [f, rk, ra]) e.addEventListener('input', zeige)
}
```

CSS: `.fuellstand { display: flex; height: 22px; border: 1px solid var(--line); border-radius: 6px; overflow: hidden; background: var(--bg-soft); } .fuellstand i { display: block; height: 100%; }`

- [ ] **Step 3: `vergleich.js`** (Parameter `frage`: Schlüssel wie `e18`)

```js
import { el, txt, zwei, kopf, fmt } from './gemein.js'
import { parseAntwort } from '../pruefung.js'
const L = { titel: { de: 'Dieselbe Frage: ohne und mit Handbuch', en: 'The same question: without and with the manual' }, nackt: { de: 'Ohne Handbuch (nur das Modell)', en: 'Without the manual (model only)' },
  rag: { de: 'Mit Handbuch (RAG)', en: 'With the manual (RAG)' }, quellen: { de: 'Übergebene Auszüge', en: 'Excerpts passed to the model' }, tokens: { de: 'Tokens ein/aus', en: 'tokens in/out' }, sek: { de: 's', en: 's' },
  abgelehnt: { de: 'Abgelehnt vor dem Modellaufruf', en: 'Rejected before the model call' }, roh: { de: 'Rohantwort anzeigen', en: 'Show raw answer' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const sel = el('select'); const grid = el('div', 'zwei-spalten'); wrap.append(sel, grid)
  let d
  const karte = (titel, e) => {
    const box = el('div', 'concept-box'); box.append(zwei(el('h4'), titel, ctx)); if (!e) return box
    if (e.abgelehnt) { box.append(zwei(el('p', 'warn-box'), L.abgelehnt, ctx), el('p', null, e.antwort)) }
    else if (e.modus === 'rag') { const r = parseAntwort(e.antwort); if (r.ok) { box.append(el('p', null, r.summary)); if (r.intro) box.append(el('p', null, r.intro)); const ul = el('ul', 'checkliste'); for (const s of r.schritte) { const li = el('li'); const cb = el('input'); cb.type = 'checkbox'; li.append(cb, ' ', el('span', null, s.replace(/\*\*/g, ''))); ul.append(li) } box.append(ul) } else box.append(el('p', 'warn-box', e.antwort))
      const det = el('details'); det.append(zwei(el('summary'), L.quellen, ctx)); for (const q of e.quellen || []) det.append(el('pre', 'code-block', (q.abschnitt ? q.abschnitt + '\n' : '') + q.text)); box.append(det) }
    else box.append(el('p', null, e.antwort))
    const det = el('details'); det.append(zwei(el('summary'), L.roh, ctx), el('pre', 'code-block', e.antwort)); box.append(det)
    if (e.usage) box.append(el('p', 'line-hilfe', `${e.modell} · ${txt(L.tokens, ctx.lang())} ${e.usage.ein}/${e.usage.aus} · ${fmt(e.sekunden, 1, ctx.lang())} ${txt(L.sek, ctx.lang())}`))
    return box
  }
  const zeige = () => { const key = sel.value; grid.replaceChildren(karte(L.nackt, d.eintraege.find(e => e.id === key + '-nackt')), karte(L.rag, d.eintraege.find(e => e.id === key + '-rag'))) }
  ctx.daten('antworten.json').then(x => { d = x; const keys = [...new Set(d.eintraege.map(e => e.id.replace(/-(nackt|rag)$/, '')))]; for (const key of keys) { const o = el('option', null, d.eintraege.find(e => e.id === key + '-nackt').frage); o.value = key; sel.append(o) } if (p.frage) sel.value = p.frage; k.status({ de: `aufgezeichnet ${d.datum}`, en: `recorded ${d.datum}` }, 'vorberechnet'); zeige() })
  sel.addEventListener('change', zeige); document.addEventListener('rag:sprache', () => d && zeige())
}
```

- [ ] **Step 4: `seite.js`** (Parameter `markierungen: [{ muster, hinweis: {de,en} }]`)

```js
import { el, txt, zwei, kopf } from './gemein.js'
const L = { titel: { de: 'PDF-Seite 33 neben dem Markdown', en: 'PDF page 33 next to the Markdown' }, links: { de: 'Gedruckte Seite', en: 'Printed page' }, rechts: { de: 'Markdown des neuen Parsers (Docling, Seitenmarker)', en: 'Markdown from the new parser (Docling, page marker)' }, klick: { de: 'Markierte Stellen anklicken.', en: 'Click the highlighted spots.' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const grid = el('div', 'zwei-spalten seite')
  const li = el('figure'); const img = el('img'); img.src = ctx.basis + '/assets/seite-33.png'; img.alt = 'Seite 33 der Gebrauchsanleitung: Hinweise im Anzeigefeld, Störungen'; img.loading = 'lazy'; li.append(img, zwei(el('figcaption'), L.links, ctx))
  const re = el('div'); re.append(zwei(el('h4'), L.rechts, ctx)); const pre = el('pre', 'code-block markdown'); re.append(pre); const erkl = el('div', 'tip-box'); erkl.hidden = true; re.append(erkl)
  grid.append(li, re); wrap.append(grid); k.status(L.klick, '')
  ctx.daten('seite-33.md').then(md => {
    const marks = (p.markierungen || []).map(m => ({ re: new RegExp(m.muster, 'g'), hinweis: m.hinweis }))
    let html = md.replace(/[&<>]/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;' }[c]))
    marks.forEach((m, i) => { html = html.replace(m.re, s => `<mark data-i="${i}">${s}</mark>`) })
    pre.innerHTML = html
    pre.addEventListener('click', ev => { const m = ev.target.closest('mark'); if (!m) return; erkl.hidden = false; erkl.textContent = txt(marks[+m.dataset.i].hinweis, ctx.lang()) })
  })
}
```

CSS: `.seite img { width: 100%; border: 1px solid var(--line); } .markdown { max-height: 520px; overflow: auto; white-space: pre-wrap; } .markdown mark { background: var(--signal-l); border-bottom: 2px solid var(--signal); cursor: pointer; }`

- [ ] **Step 5: `chunking.js` (Werkzeug)** (Parameter `abschnitt`, `strategie`, `groesse`, `overlap`)

```js
import { el, txt, zwei, kopf, fmt } from './gemein.js'
import { chunkeFest, chunkeSaetze, chunkeMarkdown, explodiereTabellen, begrenzeLaenge } from '../chunking.js'
import { schaetzeTokens } from '../pruefung.js'
const L = { titel: { de: 'Chunking-Spielplatz', en: 'Chunking playground' }, quelle: { de: 'Abschnitt des Handbuchs', en: 'Section of the manual' }, ganz: { de: 'Ganzes Handbuch', en: 'Whole manual' },
  strategie: { de: 'Strategie', en: 'Strategy' }, fest: { de: 'Feste Länge (Zeichen)', en: 'Fixed length (characters)' }, saetze: { de: 'Sätze (Zeichen)', en: 'Sentences (characters)' }, pipeline: { de: 'Markdown → Tabellenzeilen → Tokens (Fallbeispiel)', en: 'Markdown → table rows → tokens (case study)' },
  groesse: { de: 'Größe', en: 'Size' }, overlap: { de: 'Überlappung', en: 'Overlap' }, chunks: { de: 'Chunks', en: 'chunks' }, tokens: { de: 'Tokens', en: 'tokens' }, geschaetzt: { de: 'geschätzt', en: 'estimated' }, exakt: { de: 'exakt', en: 'exact' },
  e18: { de: 'E:18-Zeile allein in einem Chunk', en: 'E:18 row alone in a chunk' }, ja: { de: 'ja', en: 'yes' }, nein: { de: 'nein', en: 'no' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const sel = el('select'); const strat = el('select')
  for (const [v, l] of [['fest', L.fest], ['saetze', L.saetze], ['pipeline', L.pipeline]]) { const o = el('option'); o.value = v; zwei(o, l, ctx); strat.append(o) } strat.value = p.strategie || 'fest'
  const rg = el('input'); rg.type = 'range'; rg.min = 100; rg.max = 2000; rg.step = 20; rg.value = p.groesse || 500; const ro = el('input'); ro.type = 'range'; ro.min = 0; ro.max = 200; ro.step = 10; ro.value = p.overlap || 50
  const lg = el('label'); lg.append(zwei(el('span'), L.groesse, ctx), el('output'), rg); const lo = el('label'); lo.append(zwei(el('span'), L.overlap, ctx), el('output'), ro)
  const regler = el('div', 'regler'); regler.append(lg, lo); const summe = el('p'); const liste = el('div', 'chunk-liste'); wrap.append(sel, strat, regler, summe, liste)
  let md, abschnitte
  const zaehler = (t) => (ctx.modell.zustand().tokenizer === 'bereit' ? ctx.modell.tokenisiere(t).tokens.length : schaetzeTokens(t))
  const zeige = () => {
    const g = +rg.value, o = +ro.value; lg.querySelector('output').value = g; lo.querySelector('output').value = o
    const quelle = sel.value === '*' ? md : abschnitte.find(a => a.ueberschrift === sel.value).text
    let chunks
    if (strat.value === 'fest') chunks = chunkeFest(quelle, { groesse: g, overlap: o })
    else if (strat.value === 'saetze') chunks = chunkeSaetze(quelle, { groesse: g, overlap: o })
    else chunks = begrenzeLaenge(explodiereTabellen(chunkeMarkdown(quelle), 1600), { groesse: Math.min(440, Math.round(g / 3.8)), overlap: Math.round(o / 3.8), zaehler })
    const exakt = ctx.modell.zustand().tokenizer === 'bereit'
    const allein = chunks.some(c => c.text.includes('E:18') && c.text.length < 400 && !c.text.includes('E:23'))
    const laengen = chunks.map(c => c.text.length)
    summe.textContent = `${chunks.length} ${txt(L.chunks, ctx.lang())} · ${Math.min(...laengen)}–${Math.max(...laengen)} Z. · ${txt(L.e18, ctx.lang())}: ${txt(allein ? L.ja : L.nein, ctx.lang())}`
    liste.replaceChildren(...chunks.slice(0, 60).map((c, i) => { const box = el('div', 'chunk' + (c.text.includes('E:18') ? ' e18' : '')); box.append(el('div', 'chunk-kopf', `#${i + 1} · ${c.text.length} Z. · ${zaehler(c.text)} ${txt(L.tokens, ctx.lang())} (${txt(exakt ? L.exakt : L.geschaetzt, ctx.lang())})${c.art ? ' · ' + c.art : ''}`), el('pre', null, c.text)); return box }))
    k.status(exakt ? { de: 'Tokenzahlen exakt', en: 'Exact token counts' } : { de: 'Tokenzahlen geschätzt (Tokenizer im Werkzeug „Tokenizer“ laden)', en: 'Token counts estimated (load the tokenizer in the “Tokenizer” tool)' }, exakt ? 'live' : 'vorberechnet')
  }
  ctx.daten('handbuch.md').then(t => { md = t; abschnitte = chunkeMarkdown(md); const o0 = el('option'); o0.value = '*'; zwei(o0, L.ganz, ctx); sel.append(o0)
    for (const a of abschnitte) if (a.ueberschrift) { const o = el('option', null, a.ueberschrift.slice(0, 70)); o.value = a.ueberschrift; sel.append(o) }
    sel.value = p.abschnitt || 'Hinweise im Anzeigefeld'; zeige() })
  for (const e of [sel, strat, rg, ro]) e.addEventListener('input', zeige); ctx.modell.beiAenderung(() => md && zeige())
}
```

CSS: `.chunk-liste { max-height: 560px; overflow: auto; } .chunk { border: 1px solid var(--line); border-radius: 6px; margin: 0.4rem 0; padding: 0.4rem 0.6rem; } .chunk.e18 { border-color: var(--signal); background: var(--signal-l); } .chunk-kopf { font-size: 0.75rem; color: var(--ink-soft); font-family: var(--mono); } .chunk pre { white-space: pre-wrap; font-size: 0.78rem; margin: 0.2rem 0 0; max-height: 160px; overflow: auto; }`

- [ ] **Step 6: `prompt.js`** (Parameter `frage`)

```js
import { el, txt, zwei, kopf, fmt } from './gemein.js'
import { parseAntwort, schaetzeTokens } from '../pruefung.js'
const L = { titel: { de: 'Prompt-Baukasten: was das Modell wirklich sieht', en: 'Prompt builder: what the model really sees' }, system: { de: 'Systemrolle', en: 'System role' }, nutzer: { de: 'Nutzerrolle (JSON als Daten)', en: 'User role (JSON as data)' },
  budget: { de: 'Kontextbudget 14 000 Zeichen', en: 'Context budget 14,000 characters' }, antwort: { de: 'Rohantwort des Modells', en: 'Raw model answer' }, geparst: { de: 'Geparst (parseAntwort)', en: 'Parsed (parseAntwort)' }, zeichen: { de: 'Zeichen', en: 'characters' }, tokensCa: { de: 'Tokens, geschätzt', en: 'tokens, estimated' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const sel = el('select'); const sys = el('pre', 'code-block'); const usr = el('pre', 'code-block'); const balken = el('div', 'balken'); const bi = el('i'); balken.append(bi); const bm = el('p', 'line-hilfe'); const roh = el('pre', 'code-block'); const gep = el('div')
  wrap.append(sel, zwei(el('h4'), L.system, ctx), sys, zwei(el('h4'), L.nutzer, ctx), usr, zwei(el('h4'), L.budget, ctx), balken, bm, zwei(el('h4'), L.antwort, ctx), roh, zwei(el('h4'), L.geparst, ctx), gep)
  let d
  const zeige = () => {
    const e = d.eintraege.find(x => x.id === sel.value); if (!e || !e.messages) return
    sys.textContent = e.messages[0].content; const u = JSON.parse(e.messages[1].content); usr.textContent = JSON.stringify(u, null, 2)
    const n = u.handbuch.length; bi.style.width = Math.min(100, n / 14000 * 100) + '%'; bm.textContent = `${n.toLocaleString('de-DE')} / 14 000 ${txt(L.zeichen, ctx.lang())} · ≈ ${schaetzeTokens(e.messages[0].content + e.messages[1].content)} ${txt(L.tokensCa, ctx.lang())} · ${txt({ de: 'gemeldet', en: 'reported' }, ctx.lang())}: ${e.usage ? e.usage.ein : '–'}`
    roh.textContent = e.antwort; const r = parseAntwort(e.antwort); gep.replaceChildren()
    if (r.ok) { gep.append(el('p', null, r.summary)); if (r.intro) gep.append(el('p', null, r.intro)); const ul = el('ul', 'checkliste'); for (const s of r.schritte) { const li = el('li'); const cb = el('input'); cb.type = 'checkbox'; li.append(cb, ' ', s.replace(/\*\*/g, '')); ul.append(li) } gep.append(ul) } else gep.append(el('p', 'warn-box', 'kein-format'))
  }
  ctx.daten('antworten.json').then(x => { d = x; for (const e of d.eintraege.filter(e => e.modus === 'rag' && e.messages)) { const o = el('option', null, e.frage); o.value = e.id; sel.append(o) } if (p.frage) sel.value = p.frage + '-rag'; k.status({ de: `aufgezeichnet ${d.datum}`, en: `recorded ${d.datum}` }, 'vorberechnet'); zeige() })
  sel.addEventListener('change', zeige)
}
```

- [ ] **Step 7: `llm.js`** (Bring your own LLM)

```js
import { el, txt, zwei, kopf, fmt } from './gemein.js'
import { parseAntwort } from '../pruefung.js'
const L = { titel: { de: 'Eigenes Modell anschließen (LM Studio oder Ollama)', en: 'Connect your own model (LM Studio or Ollama)' }, endpunkt: { de: 'Endpunkt (OpenAI-kompatibel)', en: 'Endpoint (OpenAI-compatible)' },
  verbinden: { de: 'Verbinden', en: 'Connect' }, modell: { de: 'Modell', en: 'Model' }, frage: { de: 'Frage', en: 'Question' }, mitKontext: { de: 'Handbuchauszüge mitschicken (RAG)', en: 'Send manual excerpts (RAG)' }, senden: { de: 'Senden', en: 'Send' },
  verbunden: { de: 'Verbunden: {n} Modelle', en: 'Connected: {n} models' }, keinServer: { de: 'Keine Verbindung. Läuft der Server? Ist CORS eingeschaltet (LM Studio: „Enable CORS“; Ollama: OLLAMA_ORIGINS)? Safari blockiert Aufrufe an http://localhost von einer HTTPS-Seite; nutzen Sie Chrome, Edge oder Firefox oder öffnen Sie das Lab lokal.', en: 'No connection. Is the server running? Is CORS enabled (LM Studio: “Enable CORS”; Ollama: OLLAMA_ORIGINS)? Safari blocks calls to http://localhost from an HTTPS page; use Chrome, Edge or Firefox, or open the lab locally.' },
  abgelehnt: { de: 'Die Suche findet keinen belegten Kontext: Das Fallbeispiel würde ablehnen und kein Modell aufrufen. Zum Vergleich können Sie ohne Kontext senden.', en: 'Retrieval finds no grounded context: the case study would refuse and not call a model. You can still send without context for comparison.' },
  laeuft: { de: 'Antwort läuft …', en: 'Answer streaming …' }, fertig: { de: 'fertig', en: 'done' }, geparst: { de: 'Geparst', en: 'Parsed' }, keinFormat: { de: 'Das Modell hat das Antwortformat nicht eingehalten (server.py würde die Antwort verwerfen).', en: 'The model did not keep the answer format (server.py would discard the answer).' },
  aufgezeichnet: { de: 'Ohne eigenen Server: aufgezeichnete Antworten (Werkzeug „Vergleich“ weiter oben).', en: 'Without your own server: recorded answers (tool “comparison” above).' }, laenge: { de: 'Antwort am Tokenlimit abgeschnitten (finish_reason = length).', en: 'Answer cut off at the token limit (finish_reason = length).' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx)
  const ep = el('input'); ep.type = 'text'; ep.value = (() => { try { return localStorage.getItem('rag:llm') || 'http://localhost:1234/v1' } catch { return 'http://localhost:1234/v1' } })(); zwei(ep, L.endpunkt, ctx, 'aria-label')
  const bv = zwei(el('button', 'btn-sm'), L.verbinden, ctx); bv.type = 'button'; const modelle = el('select'); const fragen = el('select'); const cb = el('input'); cb.type = 'checkbox'; cb.checked = true; const cbl = el('label'); cbl.append(cb, zwei(el('span'), L.mitKontext, ctx))
  const bs = zwei(el('button', 'btn-sm primary'), L.senden, ctx); bs.type = 'button'; bs.disabled = true; const meld = el('p', 'line-hilfe'); const aus = el('pre', 'code-block'); const gep = el('div')
  wrap.append(ep, bv, modelle, fragen, cbl, bs, meld, aus, gep); zwei(meld, L.aufgezeichnet, ctx)
  let F, C, A
  Promise.all([ctx.daten('fragen.json'), ctx.daten('chunks.json'), ctx.daten('antworten.json')]).then(([f, c, a]) => { F = f.fragen; C = c.chunks; A = a; for (const q of F) { const o = el('option', null, q.frage); o.value = q.id; fragen.append(o) } if (p.frage) fragen.value = p.frage })
  const basis = () => ep.value.trim().replace(/\/+$/, '')
  bv.addEventListener('click', async () => {
    try { const r = await fetch(basis() + '/models'); if (!r.ok) throw new Error(r.status); const d = await r.json(); modelle.replaceChildren(); for (const m of d.data || []) if (!/embed/i.test(m.id)) { const o = el('option', null, m.id); modelle.append(o) }
      try { localStorage.setItem('rag:llm', basis()) } catch {} bs.disabled = !modelle.options.length; k.status({ de: txt(L.verbunden, 'de').replace('{n}', modelle.options.length), en: txt(L.verbunden, 'en').replace('{n}', modelle.options.length) }, 'live'); meld.textContent = '' }
    catch (e) { k.status(L.keinServer, 'vorberechnet'); meld.textContent = txt(L.keinServer, ctx.lang()); bs.disabled = true }
  })
  bs.addEventListener('click', async () => {
    const f = F.find(q => q.id === fragen.value); const kontext = f.kontext.map(id => C.find(c => c.id === id).text).join('\n\n')
    let messages
    if (cb.checked) { if (!f.gedeckt) { meld.textContent = txt(L.abgelehnt, ctx.lang()); return } messages = [{ role: 'system', content: A.systemprompt }, { role: 'user', content: JSON.stringify({ frage: f.frage, handbuch: kontext }) }] }
    else messages = [{ role: 'system', content: A.nacktSystem }, { role: 'user', content: f.frage }]
    aus.textContent = ''; gep.replaceChildren(); meld.textContent = txt(L.laeuft, ctx.lang()); bs.disabled = true
    try {
      const r = await fetch(basis() + '/chat/completions', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ model: modelle.value, messages, temperature: 0, max_tokens: 1024, stream: true }) })
      if (!r.ok || !r.body) throw new Error(String(r.status))
      const reader = r.body.getReader(); const dec = new TextDecoder(); let puffer = '', text = '', finish = null
      while (true) { const { value, done } = await reader.read(); if (done) break; puffer += dec.decode(value, { stream: true }); const zeilen = puffer.split('\n'); puffer = zeilen.pop()
        for (const z of zeilen) { if (!z.startsWith('data:')) continue; const d = z.slice(5).trim(); if (d === '[DONE]') continue; try { const j = JSON.parse(d); const ch = j.choices && j.choices[0]; if (ch) { finish = ch.finish_reason || finish; const delta = ch.delta && ch.delta.content; if (delta) { text += delta; aus.textContent = text } } } catch {} } }
      meld.textContent = txt(L.fertig, ctx.lang()) + (finish === 'length' ? ' · ' + txt(L.laenge, ctx.lang()) : '')
      if (cb.checked) { const pr = parseAntwort(text); gep.append(zwei(el('h4'), L.geparst, ctx)); if (pr.ok) { gep.append(el('p', null, pr.summary)); const ul = el('ul', 'checkliste'); for (const s of pr.schritte) { const li = el('li'); const c = el('input'); c.type = 'checkbox'; li.append(c, ' ', s.replace(/\*\*/g, '')); ul.append(li) } gep.append(ul) } else gep.append(zwei(el('p', 'warn-box'), L.keinFormat, ctx)) }
    } catch (e) { meld.textContent = txt(L.keinServer, ctx.lang()) } finally { bs.disabled = false }
  })
}
```

- [ ] **Step 8: `metrik.js`** (Parameter `variante`: `vektor|hybrid|rerank`)

```js
import { el, txt, zwei, kopf, fmt } from './gemein.js'
import { bewerteTreffer, metriken } from '../pruefung.js'
const L = { titel: { de: 'Metrik-Rechner: zehn Fragen, fünf Treffer', en: 'Metric calculator: ten questions, five hits' }, variante: { de: 'Variante', en: 'Variant' }, vektor: { de: 'Vektor allein', en: 'Vector only' }, hybrid: { de: 'Hybrid ohne Reranker', en: 'Hybrid without reranker' }, rerank: { de: 'Hybrid mit Reranker', en: 'Hybrid with reranker' },
  frage: { de: 'Frage', en: 'Question' }, rang: { de: 'Rang des 1. Treffers', en: 'Rank of 1st hit' }, gef: { de: 'gefunden / erwartet', en: 'found / expected' }, hit5: { de: 'Hit@5', en: 'Hit@5' }, hit1: { de: 'Hit@1', en: 'Hit@1' }, mrr: { de: 'MRR', en: 'MRR' }, abd: { de: 'Abdeckung', en: 'Coverage' },
  pruefen: { de: 'Prüfen', en: 'Check' }, aufdecken: { de: 'Aufdecken', en: 'Reveal' }, richtig: { de: 'Alle vier Werte stimmen.', en: 'All four values are correct.' }, falsch: { de: 'Noch nicht: {n} Wert(e) weichen ab (Toleranz ±0,01).', en: 'Not yet: {n} value(s) differ (tolerance ±0.01).' }, kein: { de: '–', en: '–' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const sel = el('select'); for (const v of ['vektor', 'hybrid', 'rerank']) { const o = el('option'); o.value = v; zwei(o, L[v], ctx); sel.append(o) } sel.value = p.variante || 'vektor'
  const tab = el('div', 'table-scroll'); const form = el('div', 'rechnen'); const eing = {}
  for (const key of ['hit5', 'hit1', 'mrr', 'abd']) { const l = el('label', 'rechen-feld'); l.append(zwei(el('span'), L[key], ctx)); const i = el('input'); i.type = 'text'; i.inputMode = 'decimal'; l.append(i); form.append(l); eing[key] = i }
  const bp = zwei(el('button', 'btn-sm primary'), L.pruefen, ctx); const ba = zwei(el('button', 'btn-sm'), L.aufdecken, ctx); bp.type = ba.type = 'button'; const meld = el('p'); wrap.append(sel, tab, form, bp, ba, meld)
  let F, C, ist
  const zeige = () => {
    const faelle = F.filter(q => q.art === 'eval').map(q => { const l = q[sel.value === 'vektor' ? 'vektor' : sel.value === 'hybrid' ? 'hybrid' : 'rerank'].slice(0, 5); return { q, b: bewerteTreffer(l.map(t => C.find(c => c.id === t.chunk).text), q.erwartet) } })
    ist = metriken(faelle.map(f => f.b)); const t = el('table'); t.innerHTML = `<thead><tr><th>${txt(L.frage, ctx.lang())}</th><th>${txt(L.rang, ctx.lang())}</th><th>${txt(L.gef, ctx.lang())}</th></tr></thead>`
    const tb = el('tbody'); for (const f of faelle) { const tr = el('tr'); tr.append(el('td', null, f.q.frage), el('td', null, f.b.ersterRang == null ? txt(L.kein, ctx.lang()) : String(f.b.ersterRang)), el('td', null, `${f.b.gefunden} / ${f.b.erwartet}`)); tb.append(tr) } t.append(tb); tab.replaceChildren(t); meld.textContent = ''; for (const i of Object.values(eing)) { i.value = ''; i.className = '' }
  }
  Promise.all([ctx.daten('fragen.json'), ctx.daten('chunks.json')]).then(([f, c]) => { F = f.fragen; C = c.chunks; k.status({ de: 'aus fragen.json (Kandidaten vor Kontextfilter, wie das Protokoll)', en: 'from fragen.json (candidates before context filter, like the protocol)' }, 'vorberechnet'); zeige() })
  sel.addEventListener('change', zeige)
  const soll = () => ({ hit5: ist.trefferquote, hit1: ist.hitAt1, mrr: ist.mrr, abd: ist.abdeckung })
  const zahl = (s) => { const v = parseFloat(String(s).replace(',', '.').replace('%', '')); return String(s).includes('%') || v > 1 ? v / 100 : v }
  bp.addEventListener('click', () => { const s = soll(); let n = 0; for (const [key, i] of Object.entries(eing)) { const ok = Math.abs(zahl(i.value) - s[key]) <= 0.01; i.className = ok ? 'richtig' : 'falsch'; if (!ok) n++ } meld.textContent = n ? txt(L.falsch, ctx.lang()).replace('{n}', n) : txt(L.richtig, ctx.lang()); meld.className = n ? 'warn-box' : 'challenge-box'; wrap.dispatchEvent(new CustomEvent('rag:werkzeug', { bubbles: true, detail: { werkzeug: 'metrik', ok: !n } })) })
  ba.addEventListener('click', () => { const s = soll(); for (const [key, i] of Object.entries(eing)) i.value = fmt(s[key], 3, ctx.lang()) })
}
```

- [ ] **Step 9: `schwelle.js`**

```js
import { el, txt, zwei, kopf, fmt } from './gemein.js'
const L = { titel: { de: 'Schwellen-Regler: 14 echte Fälle', en: 'Threshold slider: 14 real cases' }, schwelle: { de: 'Schwelle GUARDRAIL_MIN_SCORE', en: 'Threshold GUARDRAIL_MIN_SCORE' },
  ang: { de: 'Fragen aus dem Handbuch angenommen', en: 'in-manual questions accepted' }, abg: { de: 'Fragen aus dem Handbuch abgelehnt (falsche Ablehnung)', en: 'in-manual questions rejected (false rejection)' }, negAbg: { de: 'Fremdfragen abgelehnt', en: 'off-topic questions rejected' }, negAng: { de: 'Fremdfragen angenommen (Halluzinationsrisiko)', en: 'off-topic questions accepted (hallucination risk)' },
  log: { de: 'Achse logarithmisch, damit die Fremdfragen (Scores um 0,0001) sichtbar bleiben.', en: 'Logarithmic axis so the off-topic questions (scores around 0.0001) stay visible.' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const lab = el('label'); const r = el('input'); r.type = 'range'; r.min = -4; r.max = 0; r.step = 0.02; r.value = Math.log10(p.schwelle || 0.15); const out = el('output'); lab.append(zwei(el('span'), L.schwelle, ctx), out, r)
  const svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg'); svg.setAttribute('viewBox', '0 0 720 260'); svg.setAttribute('class', 'karte'); const zaehl = el('div', 'legende'); wrap.append(lab, svg, zaehl, zwei(el('p', 'line-hilfe'), L.log, ctx))
  const ns = (t) => document.createElementNS('http://www.w3.org/2000/svg', t); const x = (s) => 40 + (Math.log10(Math.max(s, 1e-4)) + 4) / 4 * 650
  let F
  const zeige = () => {
    const t = Math.pow(10, +r.value); out.value = fmt(t, 3, ctx.lang()); svg.replaceChildren()
    const achse = ns('line'); achse.setAttribute('x1', 40); achse.setAttribute('x2', 690); achse.setAttribute('y1', 130); achse.setAttribute('y2', 130); achse.setAttribute('stroke', '#5B4E3E'); svg.append(achse)
    for (const s of [0.0001, 0.001, 0.01, 0.1, 1]) { const tx = ns('text'); tx.setAttribute('x', x(s)); tx.setAttribute('y', 150); tx.setAttribute('font-size', '11'); tx.setAttribute('text-anchor', 'middle'); tx.textContent = String(s).replace('.', ctx.lang() === 'de' ? ',' : '.'); svg.append(tx) }
    const sl = ns('line'); sl.setAttribute('x1', x(t)); sl.setAttribute('x2', x(t)); sl.setAttribute('y1', 30); sl.setAttribute('y2', 230); sl.setAttribute('stroke', '#C4602A'); sl.setAttribute('stroke-width', '2'); sl.setAttribute('stroke-dasharray', '4 3'); svg.append(sl)
    let ang = 0, abg = 0, nAbg = 0, nAng = 0
    for (const f of F.filter(q => q.art !== 'beispiel')) { const pos = f.art === 'eval'; const s = f.obersterScore; const drin = s >= t; if (pos) drin ? ang++ : abg++; else drin ? nAng++ : nAbg++
      const c = ns('circle'); c.setAttribute('cx', x(s)); c.setAttribute('cy', pos ? 95 : 165); c.setAttribute('r', 7); c.setAttribute('fill', pos ? (drin ? '#2F7D32' : '#B23B1E') : (drin ? '#B23B1E' : '#2F7D32')); const ti = ns('title'); ti.textContent = `${f.frage} · ${fmt(s, 4, ctx.lang())}`; c.append(ti); svg.append(c)
      if (pos && !drin || !pos && drin) { const l = ns('text'); l.setAttribute('x', x(s)); l.setAttribute('y', pos ? 80 : 190); l.setAttribute('font-size', '10'); l.setAttribute('text-anchor', 'middle'); l.textContent = f.id; svg.append(l) } }
    zaehl.replaceChildren(el('span', null, `● ${ang} ${txt(L.ang, ctx.lang())}`), el('span', null, `● ${abg} ${txt(L.abg, ctx.lang())}`), el('span', null, `● ${nAbg} ${txt(L.negAbg, ctx.lang())}`), el('span', null, `● ${nAng} ${txt(L.negAng, ctx.lang())}`))
  }
  ctx.daten('fragen.json').then(f => { F = f.fragen; k.status({ de: 'oberster Reranker-Score je Frage (vorberechnet)', en: 'top reranker score per question (precomputed)' }, 'vorberechnet'); zeige() }); r.addEventListener('input', zeige)
}
```

- [ ] **Step 10: `architektur.js`** (klickbares SVG; Texte im Modul)

```js
import { el, txt, zwei, kopf } from './gemein.js'
const K = [
  { id: 'gui', x: 20, y: 100, w: 130, name: { de: 'Browser', en: 'Browser' }, datei: 'index.html', aufgabe: { de: 'Anbieter- und Suchumschalter, Frage senden, SSE lesen, Antwort und Quellen anzeigen. Maskiert alle Modell- und Nutzertexte.', en: 'Provider and search toggle, send the question, read SSE, show answer and sources. Escapes all model and user text.' }, verbot: { de: 'Speichert nie einen API-Schlüssel (kein localStorage, kein Cookie).', en: 'Never stores an API key (no localStorage, no cookie).' } },
  { id: 'flask', x: 200, y: 100, w: 140, name: { de: 'Flask-App', en: 'Flask app' }, datei: 'server.py', aufgabe: { de: 'Validiert Eingaben (1–2000 Zeichen, 16 KiB), prüft Loopback und Origin, orchestriert Retrieval und Antwort, streamt SSE.', en: 'Validates input (1–2,000 characters, 16 KiB), checks loopback and origin, orchestrates retrieval and answer, streams SSE.' }, verbot: { de: 'Gibt nie eine ungeparste Modellantwort als Handbuchbeleg aus.', en: 'Never returns an unparsed model answer as manual evidence.' } },
  { id: 'rag', x: 390, y: 30, w: 150, name: { de: 'Hybrid-Retrieval', en: 'Hybrid retrieval' }, datei: 'rag_engine.py', aufgabe: { de: 'Chunking, Embeddings, Index, Fehlercode-Lookup, Reranking, Schwelle, Quellenangabe.', en: 'Chunking, embeddings, index, error-code lookup, reranking, threshold, source reference.' }, verbot: { de: 'Nutzt nie einen Index, dessen Fingerprint nicht zu Quelle und Modell passt.', en: 'Never uses an index whose fingerprint does not match source and model.' } },
  { id: 'store', x: 590, y: 30, w: 120, name: { de: 'Vektorspeicher', en: 'Vector store' }, datei: 'storage/*.json', aufgabe: { de: 'SimpleVectorStore, Docstore, Index-Metadaten als JSON; .cache_key mit dem Fingerprint.', en: 'SimpleVectorStore, docstore, index metadata as JSON; .cache_key holding the fingerprint.' }, verbot: { de: 'Kein Datenbankserver, keine Cloud.', en: 'No database server, no cloud.' } },
  { id: 'pageindex', x: 390, y: 170, w: 150, name: 'PageIndex', datei: 'pageindex_engine.py', aufgabe: { de: 'Vektorlose Auswahl: das LLM wählt Abschnitts-IDs aus Titeln und Zusammenfassungen (experimentell).', en: 'Vectorless selection: the LLM picks section IDs from titles and summaries (experimental).' }, verbot: { de: 'Akzeptiert nie IDs außerhalb des aktuellen Batches.', en: 'Never accepts IDs outside the current batch.' } },
  { id: 'llm', x: 200, y: 200, w: 140, name: { de: 'LLM-Client', en: 'LLM client' }, datei: 'llm_client.py', aufgabe: { de: 'Ein OpenAI-kompatibler Client für LM Studio und OpenAI; Profil je Anbieter; Temperatur 0.', en: 'One OpenAI-compatible client for LM Studio and OpenAI; profile per provider; temperature 0.' }, verbot: { de: 'Liest den OpenAI-Schlüssel nie aus Datei oder Umgebung.', en: 'Never reads the OpenAI key from a file or the environment.' } },
  { id: 'keys', x: 20, y: 200, w: 130, name: { de: 'Sitzungsschlüssel', en: 'Session keys' }, datei: 'session_keys.py', aufgabe: { de: 'Schlüssel nur im Arbeitsspeicher, je Browsersitzung, acht Stunden; CSRF-Token.', en: 'Keys only in memory, per browser session, eight hours; CSRF token.' }, verbot: { de: 'Serialisiert nie auf Platte oder in Cookies.', en: 'Never serialises to disk or cookies.' } },
  { id: 'lms', x: 590, y: 170, w: 120, name: 'LM Studio', datei: 'http://127.0.0.1:1234/v1', aufgabe: { de: 'Lokaler OpenAI-kompatibler Server mit dem geladenen Chatmodell.', en: 'Local OpenAI-compatible server with the loaded chat model.' }, verbot: { de: 'Verlässt den Rechner nicht.', en: 'Never leaves the machine.' } },
  { id: 'openai', x: 590, y: 250, w: 120, name: 'OpenAI API', datei: 'https://api.openai.com/v1', aufgabe: { de: 'Cloud-Alternative; Frage und Auszüge werden übertragen, Kosten entstehen.', en: 'Cloud alternative; question and excerpts are transmitted, costs arise.' }, verbot: { de: 'Wird nie ohne Schlüssel aus der Oberfläche aufgerufen.', en: 'Never called without a key entered in the interface.' } }
]
const KANTEN = [['gui', 'flask'], ['flask', 'rag'], ['flask', 'pageindex'], ['rag', 'store'], ['flask', 'llm'], ['flask', 'keys'], ['llm', 'lms'], ['llm', 'openai'], ['pageindex', 'llm']]
const L = { titel: { de: 'Architektur des Fallbeispiels: Komponente anklicken', en: 'Architecture of the case study: click a component' }, aufgabe: { de: 'Aufgabe', en: 'Task' }, verbot: { de: 'Darf nie', en: 'Must never' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg'); svg.setAttribute('viewBox', '0 0 740 320'); svg.setAttribute('class', 'karte'); const panel = el('div', 'concept-box'); wrap.append(svg, panel)
  const ns = (t) => document.createElementNS('http://www.w3.org/2000/svg', t); const mitte = (c) => [c.x + c.w / 2, c.y + 22]
  const zeichne = () => { svg.replaceChildren()
    for (const [a, b] of KANTEN) { const A = K.find(c => c.id === a), B = K.find(c => c.id === b); const l = ns('line'); const [x1, y1] = mitte(A), [x2, y2] = mitte(B); l.setAttribute('x1', x1); l.setAttribute('y1', y1); l.setAttribute('x2', x2); l.setAttribute('y2', y2); l.setAttribute('stroke', '#C9C3B4'); l.setAttribute('stroke-width', '2'); svg.append(l) }
    for (const c of K) { const g = ns('g'); g.style.cursor = 'pointer'; const r = ns('rect'); r.setAttribute('x', c.x); r.setAttribute('y', c.y); r.setAttribute('width', c.w); r.setAttribute('height', 44); r.setAttribute('rx', 8); r.setAttribute('fill', c.id === (p.aktiv || '') ? '#F5E3D6' : '#fff'); r.setAttribute('stroke', '#2E2418'); const t = ns('text'); t.setAttribute('x', c.x + c.w / 2); t.setAttribute('y', c.y + 27); t.setAttribute('text-anchor', 'middle'); t.setAttribute('font-size', '13'); t.setAttribute('font-weight', '700'); t.textContent = txt(c.name, ctx.lang()); g.append(r, t); g.addEventListener('click', () => { p.aktiv = c.id; zeichne(); panel.replaceChildren(el('h4', null, txt(c.name, ctx.lang())), el('p', null, c.datei), el('p', null, `${txt(L.aufgabe, ctx.lang())}: ${txt(c.aufgabe, ctx.lang())}`), el('p', null, `${txt(L.verbot, ctx.lang())}: ${txt(c.verbot, ctx.lang())}`)) }); svg.append(g) } }
  zeichne(); document.addEventListener('rag:sprache', zeichne); k.status({ de: 'aus docs/TECHNICAL.md des Fallbeispiels', en: 'from the case study’s docs/TECHNICAL.md' }, '')
}
```

- [ ] **Step 11: `kosten.js`** (Voreinstellungen mit Datum; Preise in Aufgabe 16 recherchiert und hier eingetragen)

```js
import { el, txt, zwei, kopf, fmt } from './gemein.js'
const L = { titel: { de: 'Kostenrechner: Cloud gegen lokal', en: 'Cost calculator: cloud versus local' }, ein: { de: 'Tokens Eingabe je Anfrage', en: 'Input tokens per request' }, aus: { de: 'Tokens Ausgabe je Anfrage', en: 'Output tokens per request' }, tag: { de: 'Anfragen je Tag', en: 'Requests per day' },
  pEin: { de: 'Preis Eingabe (USD je 1 Mio. Tokens)', en: 'Input price (USD per 1M tokens)' }, pAus: { de: 'Preis Ausgabe (USD je 1 Mio. Tokens)', en: 'Output price (USD per 1M tokens)' }, hw: { de: 'Lokaler Rechner (USD, Anschaffung)', en: 'Local machine (USD, purchase)' }, monate: { de: 'Nutzungsdauer (Monate)', en: 'Useful life (months)' }, strom: { de: 'Strom je Monat (USD)', en: 'Electricity per month (USD)' },
  cloud: { de: 'Cloud je Monat', en: 'Cloud per month' }, lokal: { de: 'Lokal je Monat', en: 'Local per month' }, stand: { de: 'Preise: Voreinstellung mit Stand im Lab-Text; bitte gegen die aktuelle Preisliste prüfen.', en: 'Prices: defaults dated in the lab text; check against the current price list.' } }
export function baue (wrap, p, ctx) {
  const k = kopf(wrap, L.titel, ctx); const form = el('div', 'rechnen'); const f = {}
  for (const [key, def] of [['ein', p.ein ?? 355], ['aus', p.aus ?? 121], ['tag', p.tag ?? 200], ['pEin', p.pEin ?? 0.4], ['pAus', p.pAus ?? 1.6], ['hw', p.hw ?? 2500], ['monate', p.monate ?? 48], ['strom', p.strom ?? 5]]) { const l = el('label', 'rechen-feld'); l.append(zwei(el('span'), L[key], ctx)); const i = el('input'); i.type = 'number'; i.step = 'any'; i.value = def; l.append(i); form.append(l); f[key] = i }
  const erg = el('div', 'challenge-box'); wrap.append(form, erg, zwei(el('p', 'line-hilfe'), L.stand, ctx))
  const zeige = () => { const v = Object.fromEntries(Object.entries(f).map(([k2, i]) => [k2, +i.value || 0])); const cloud = 30 * v.tag * (v.ein * v.pEin + v.aus * v.pAus) / 1e6; const lokal = v.hw / Math.max(1, v.monate) + v.strom
    erg.replaceChildren(el('p', null, `${txt(L.cloud, ctx.lang())}: ${fmt(cloud, 2, ctx.lang())} USD`), el('p', null, `${txt(L.lokal, ctx.lang())}: ${fmt(lokal, 2, ctx.lang())} USD`)) }
  for (const i of Object.values(f)) i.addEventListener('input', zeige); zeige(); k.status({ de: 'Rechnung im Browser', en: 'Computed in the browser' }, '')
}
```

- [ ] **Step 12: Registry, CSS, Bedienprobe, Commit**

`index.js` um die elf Importe und Einträge ergänzen. Jedes Werkzeug einmal auf einer Probeseite aufrufen (DE und EN, 390 px). `node tools/verify.mjs`.

```bash
git add -A && git commit -m "Werkzeuge: Temperatur, Kontext, Vergleich, Seite, Chunking, Prompt, LLM, Metrik, Schwelle, Architektur, Kosten

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: Nachgebildetes Terminal für Lab 10

**Files:**
- Create: `assets/terminal.js`, `assets/werkzeuge/terminal.js`
- Modify: `assets/rag.js` (Zweig `terminal` in `baueBox`), `assets/rag.css` (Terminal-Klassen aus `../PITM/assets/pitm.css`, Abschnitt „terminal“), `assets/werkzeuge/index.js`, `tools/pruefung.test.mjs`
- Read: `../PITM/assets/terminal.js` (Aufbau, Dialekte), `../PITM/assets/pitm.js:411-560` (`baueTerminal`), `../PITM/assets/pruefung.js` (`schrittErfuellt`)

**Interfaces:**
- Produces: `neueWelt(os, szenario)`, `fuehreAus(welt, zeile)` → `{ zeilen: [{ art: 'aus'|'fehler'|'hinweis', text }], hinweis?: string }`, `zustandTrifft(welt, zustand)`, `schrittErfuellt(welt, schritt, zeile, vorher, ergebnis)`, `baueTerminal(ziel, { os, szenario, schritte, id, beiFertig }, ctx)`.
- Übungs-JSON: `{ "typ": "terminal", "os": "mac"|"win"|"alle", "szenario": { "lmStudioLaeuft": true, "modelle": ["gemma-4-12b-it-mlx","qwen3.5-27b-…","gemma-4-31b-it-mlx","text-embedding-nomic-embed-text-v1.5"], "portBelegt": false, "geklont": false }, "schritte": [{ "text": {de,en}, "muster": "regex", "zustand": {…}, "erwarteterFehler": "schluessel" }] }`
- Zustandsprädikate: `pfad`, `datei`, `venvVorhanden`, `venvAktiv`, `venvPython`, `pakete`, `devPakete`, `profil` (Teilstring in `profiles/local/.env`), `serverLaeuft`, `cors`, `modelleGeprueft`, `antwortErhalten`, `testsGelaufen`.

- [ ] **Step 1: Tests für Welt und Befehle**

An `tools/pruefung.test.mjs` anhängen:

```js
import { neueWelt, fuehreAus, zustandTrifft, schrittErfuellt } from '../assets/terminal.js'
const lauf = (w, ...zeilen) => zeilen.map(z => fuehreAus(w, z)).at(-1)
test('terminal mac: klonen, venv, aktivieren, installieren, starten', () => {
  const w = neueWelt('mac', { lmStudioLaeuft: true, modelle: ['gemma-4-12b-it-mlx'] })
  assert.match(lauf(w, 'git clone https://github.com/swrobuts/SiemensWashingMachineTroubleShooting_LocalLLM.git').zeilen[0].text, /Cloning into/)
  assert.equal(zustandTrifft(w, { datei: 'SiemensWashingMachineTroubleShooting_LocalLLM/requirements.txt' }), true)
  lauf(w, 'cd SiemensWashingMachineTroubleShooting_LocalLLM')
  assert.equal(lauf(w, 'python server.py').hinweis, 'kein-modul')          // ohne venv: ModuleNotFoundError
  lauf(w, 'python3.12 -m venv .venv'); assert.equal(zustandTrifft(w, { venvVorhanden: true, venvPython: '3.12' }), true)
  assert.equal(zustandTrifft(w, { venvAktiv: true }), false)
  lauf(w, 'source .venv/bin/activate'); assert.equal(zustandTrifft(w, { venvAktiv: true }), true)
  assert.match(lauf(w, 'python -m pip install -r requirements.txt').zeilen.at(-1).text, /Successfully installed/)
  assert.equal(zustandTrifft(w, { pakete: true }), true)
  lauf(w, 'cp profiles/local/.env.example profiles/local/.env')
  assert.equal(lauf(w, 'python lokale_ki.py "Was bedeutet E:18?"').hinweis, undefined)   // genau ein Chatmodell: Autoauswahl
  const r = lauf(w, 'python server.py'); assert.match(r.zeilen.at(-1).text, /Running on http:\/\/127\.0\.0\.1:3001/)
  assert.equal(zustandTrifft(w, { serverLaeuft: true }), true)
})
test('terminal: Fehlerbilder', () => {
  const w = neueWelt('mac', { lmStudioLaeuft: false, modelle: ['a', 'b'], geklont: true })
  lauf(w, 'cd SiemensWashingMachineTroubleShooting_LocalLLM', 'python3 -m venv .venv', 'source .venv/bin/activate')
  assert.equal(lauf(w, 'python -m pip install -r requirements.txt').hinweis, 'python-version')
  lauf(w, 'deactivate', 'rm -rf .venv', 'python3.12 -m venv .venv', 'source .venv/bin/activate', 'python -m pip install -r requirements.txt', 'cp profiles/local/.env.example profiles/local/.env')
  assert.equal(lauf(w, 'curl http://127.0.0.1:1234/v1/models').hinweis, 'lms-aus')
  w.lmStudio.laeuft = true
  assert.equal(lauf(w, 'python lokale_ki.py "Was bedeutet E:18?"').hinweis, 'modell-id')
  lauf(w, 'echo "LOCAL_LLM_MODEL=a" >> profiles/local/.env')
  assert.equal(zustandTrifft(w, { profil: 'LOCAL_LLM_MODEL=a' }), true)
  assert.equal(lauf(w, 'python lokale_ki.py "Was bedeutet E:18?"').hinweis, undefined)
  const w2 = neueWelt('win', { lmStudioLaeuft: true, modelle: ['a'], geklont: true, portBelegt: true })
  lauf(w2, 'cd SiemensWashingMachineTroubleShooting_LocalLLM', 'py -3.12 -m venv .venv', '.venv\\Scripts\\activate', 'python -m pip install -r requirements.txt', 'copy profiles\\local\\.env.example profiles\\local\\.env')
  assert.equal(lauf(w2, 'python server.py').hinweis, 'port-belegt')
})
test('schrittErfuellt: Muster und Zustand, erwarteter Fehler', () => {
  const w = neueWelt('mac', { geklont: true }); lauf(w, 'cd SiemensWashingMachineTroubleShooting_LocalLLM')
  const r = fuehreAus(w, 'python3.12 -m venv .venv')
  assert.equal(schrittErfuellt(w, { muster: '-m venv', zustand: { venvVorhanden: true } }, 'python3.12 -m venv .venv', false, r), true)
  const f = fuehreAus(w, 'python server.py')
  assert.equal(schrittErfuellt(w, { erwarteterFehler: 'kein-modul' }, 'python server.py', false, f), true)
})
```

- [ ] **Step 2: `assets/terminal.js`**

```js
/**
 * RAG-Lab · Nachgebildete Kommandozeile fuer Lab 10
 *
 * Aus PITM-Lab uebernommen und auf das Fallbeispiel umgebaut: ein Dateibaum im
 * Speicher, zwei Shells (zsh auf dem Mac, PowerShell auf Windows), und die
 * Werkzeuge, die der Nachbau braucht - git, python/py, pip, curl, lms. Die
 * Ausgaben sind den echten nachempfunden, einschliesslich der Fehlerbilder aus
 * dem Pruefbericht des Fallbeispiels (venv ohne 3.12, Modell-ID, Port belegt,
 * LM Studio aus). Hinweise der Umgebung sind uebersetzt und mit → markiert.
 */
const ordner = (kinder = {}) => ({ typ: 'ordner', kinder })
const datei = (inhalt = '') => ({ typ: 'datei', inhalt })
export const REPO = 'SiemensWashingMachineTroubleShooting_LocalLLM'
const REPO_URL = `https://github.com/swrobuts/${REPO}.git`
const PAKETE = ['flask==3.1.3', 'llama-index-core==0.14.24', 'llama-index-embeddings-huggingface==0.8.0', 'sentence-transformers==6.1.0', 'transformers==5.17.0', 'torch==2.14.0', 'openai==2.54.0', 'python-dotenv==1.2.3', 'filelock==4.0.1']
const ANTWORT_E18 = 'Der Fehlercode E:18 bedeutet, dass die Laugenpumpe oder der Ablaufschlauch verstopft ist.\n\n- [ ] **Laugenpumpe:** Laugenpumpe reinigen (Seite 30).\n- [ ] **Ablaufschlauch:** Ablaufschlauch am Siphon reinigen (Seite 31).\n\nQuelle: Handbuch: Hinweise im Anzeigefeld · Querverweise auf Seite 30/31'
const projekt = () => ordner({
  'README.md': datei('# Waschmaschinen-RAG: LM Studio und OpenAI in einer Anwendung\n'), 'requirements.txt': datei(PAKETE.join('\n') + '\n'),
  'requirements-dev.txt': datei('-r requirements.txt\npytest==9.1.1\n'), 'server.py': datei('"""Local web app."""\n'), 'rag_engine.py': datei(''), 'llm_client.py': datei(''), 'lokale_ki.py': datei(''), 'parser.py': datei(''),
  '.env.example': datei('HOST=127.0.0.1\nPORT=3001\nLLM_PROVIDER=local\nRETRIEVAL_MODE=hybrid\n'), 'siemens-handbuch.pdf': datei('%PDF'), 'siemens_wissen.md': datei('## Ihre neue Waschmaschine\n'),
  profiles: ordner({ local: ordner({ '.env.example': datei('LOCAL_LLM_ENDPOINT=http://127.0.0.1:1234/v1\n# Exact LM Studio model identifier. Leave blank only with exactly one chat model.\nLOCAL_LLM_MODEL=\n') }), openai: ordner({ '.env.example': datei('OPENAI_MODEL=gpt-4.1-mini\n') }) }),
  eval: ordner({ 'questions.json': datei('{}'), 'run_eval.py': datei('') }), tests: ordner({}), docs: ordner({}), storage: ordner({})
})
export function neueWelt (os = 'mac', szenario = {}) {
  const w = { os, benutzer: 'studi', rechner: os === 'mac' ? 'macbook' : 'PC-BA', pfad: ['~'],
    wurzel: ordner({ Projekte: ordner({}) }), venv: null, aktiv: false, pakete: new Set(), devPakete: false,
    lmStudio: { laeuft: szenario.lmStudioLaeuft ?? true, cors: false, modelle: szenario.modelle || ['gemma-4-12b-it-mlx', 'qwen3.5-27b-claude-4.6-opus-distilled-mlx', 'gemma-4-31b-it-mlx', 'text-embedding-nomic-embed-text-v1.5'] },
    portBelegt: !!szenario.portBelegt, server: false, modelleGeprueft: false, antwortErhalten: false, testsGelaufen: false, indexGebaut: false }
  w.pfad = ['~', 'Projekte']
  if (szenario.geklont) w.wurzel.kinder.Projekte.kinder[REPO] = projekt()
  return w
}
/* ------------------------------------------------------------- Pfade */
const knoten = (w, teile) => { let k = w.wurzel; for (const t of teile.slice(1)) { if (!k || k.typ !== 'ordner' || !k.kinder[t]) return null; k = k.kinder[t] } return k }
const aufloesen = (w, p) => { const sep = /[\\/]+/; let teile = p.startsWith('~') || p.startsWith('/') || /^[A-Za-z]:/.test(p) ? ['~'] : [...w.pfad]; for (const t of p.replace(/^~[\\/]?|^[A-Za-z]:[\\/]?|^\//, '').split(sep).filter(Boolean)) { if (t === '..') { if (teile.length > 1) teile.pop() } else if (t !== '.') teile.push(t) } return teile }
const pfadText = (w) => w.os === 'mac' ? w.pfad.join('/') : 'C:\\Users\\studi' + w.pfad.slice(1).map(t => '\\' + t).join('')
export const prompt = (w) => (w.aktiv ? '(.venv) ' : '') + (w.os === 'mac' ? `${w.benutzer}@${w.rechner} ${w.pfad.at(-1)} % ` : `PS ${pfadText(w)}> `)
const chatModelle = (w) => w.lmStudio.modelle.filter(m => !/embed/i.test(m))
const profil = (w) => { const k = knoten(w, [...aufloesen(w, `~/Projekte/${REPO}`), 'profiles', 'local', '.env']); return k && k.typ === 'datei' ? k.inhalt : null }
const imProjekt = (w) => w.pfad.length >= 3 && w.pfad[2] === REPO
/* --------------------------------------------------------------- Befehle */
const A = (text) => ({ art: 'aus', text }), F = (text) => ({ art: 'fehler', text }), H = (text) => ({ art: 'hinweis', text })
const unbekannt = (w, cmd) => ({ zeilen: [F(w.os === 'mac' ? `zsh: command not found: ${cmd}` : `${cmd} : The term '${cmd}' is not recognized as the name of a cmdlet, function, script file, or operable program.`)], hinweis: 'unbekannt' })
const python = (w, argv) => {
  const exe = argv[0]
  if (argv[1] === '--version' || argv[1] === '-V') {
    if (w.aktiv) return { zeilen: [A(`Python ${w.venv.python === '3.12' ? '3.12.12' : '3.9.6'}`)] }
    if (exe === 'python3.12' || (exe === 'py' && argv[1] === '-3.12')) return { zeilen: [A('Python 3.12.12')] }
    return { zeilen: [A(w.os === 'mac' ? 'Python 3.9.6' : 'Python 3.13.2')] }
  }
  if (exe === 'py' && argv[1] === '-3.12') { argv = ['python3.12', ...argv.slice(2)]; if (argv[1] === '--version') return { zeilen: [A('Python 3.12.12')] } }
  if (argv[1] === '-m' && argv[2] === 'venv') {
    const ziel = argv[3] || '.venv'; if (!imProjekt(w)) return { zeilen: [H('→ Zuerst in den Projektordner wechseln (cd ' + REPO + ').')], hinweis: 'nicht-im-projekt' }
    const version = (argv[0] === 'python3.12' || (w.os === 'win' && argv[0] === 'py')) ? '3.12' : (w.os === 'mac' ? '3.9' : '3.13')
    const proj = knoten(w, w.pfad); proj.kinder[ziel] = ordner({ bin: ordner({ activate: datei(''), python: datei('') }), Scripts: ordner({ 'activate': datei(''), 'Activate.ps1': datei('') }), 'pyvenv.cfg': datei(`version = ${version}`) })
    w.venv = { python: version, name: ziel }; return { zeilen: [H(`→ Virtuelle Umgebung ${ziel} mit Python ${version === '3.12' ? '3.12.12' : version} angelegt.`)] }
  }
  if (!w.aktiv || !w.venv) {
    if (argv[1] === '-m' && argv[2] === 'pip') return { zeilen: [F(w.os === 'mac' ? 'error: externally-managed-environment' : 'ERROR: Could not install packages due to an OSError: [WinError 5] Access is denied'), H('→ Ohne aktive venv landen Pakete in der System-Installation. Erst `source .venv/bin/activate` bzw. `.venv\\Scripts\\activate`.')], hinweis: 'venv-inaktiv' }
    if (/\.py$/.test(argv[1] || '')) return { zeilen: [A('Traceback (most recent call last):'), A(`  File "${argv[1]}", line 1, in <module>`), F("ModuleNotFoundError: No module named 'flask'"), H('→ Die Pakete sind nicht in der aktiven Umgebung. venv aktivieren und installieren.')], hinweis: 'kein-modul' }
  }
  if (argv[1] === '-m' && argv[2] === 'pip' && argv[3] === 'install') {
    if (w.venv.python !== '3.12') return { zeilen: [A('Collecting flask==3.1.3'), F('ERROR: Ignored the following versions that require a different python version: 2.14.0 Requires-Python >=3.12'), F('ERROR: Could not find a version that satisfies the requirement torch==2.14.0'), H(`→ Diese venv nutzt Python ${w.venv.python}; das Projekt braucht 3.12. venv löschen und mit python3.12 (Mac) bzw. py -3.12 (Windows) neu anlegen.`)], hinweis: 'python-version' }
    const dev = (argv[5] || '').includes('dev'); const zeilen = PAKETE.map(p => A(`Collecting ${p}`)); zeilen.push(A('Downloading torch-2.14.0 (…)'), A('Installing collected packages: ' + PAKETE.map(p => p.split('==')[0]).join(', ') + (dev ? ', pytest' : '')), A('Successfully installed ' + PAKETE.map(p => p.replace('==', '-')).join(' ') + (dev ? ' pytest-9.1.1' : '')))
    PAKETE.forEach(p => w.pakete.add(p.split('==')[0])); if (dev) w.devPakete = true; return { zeilen }
  }
  if (argv[1] === '-m' && argv[2] === 'pytest') { if (!w.devPakete) return { zeilen: [F('No module named pytest'), H('→ python -m pip install -r requirements-dev.txt')], hinweis: 'kein-pytest' } w.testsGelaufen = true; return { zeilen: [A('......................................................................................'), A('86 passed in 12.31s')] } }
  if (/\.py$/.test(argv[1] || '')) {
    if (!w.pakete.has('flask')) return { zeilen: [A('Traceback (most recent call last):'), F("ModuleNotFoundError: No module named 'flask'"), H('→ python -m pip install -r requirements.txt')], hinweis: 'kein-modul' }
    if (!imProjekt(w)) return { zeilen: [F(`python: can't open file '${argv[1]}': [Errno 2] No such file or directory`)], hinweis: 'falscher-ordner' }
    const erst = !w.indexGebaut; const lade = erst ? [A('INFO:rag_engine:Baue Index neu (Quelle/Konfiguration geaendert): storage'), A('INFO:rag_engine:Index gebaut und persistiert (346 Abschnitte).')] : [A('INFO:rag_engine:Lade persistenten Index aus storage (Cache-Treffer).')]
    if (argv[1] === 'server.py') { if (w.portBelegt) return { zeilen: [F('OSError: [Errno 48] Address already in use'), H('→ Port 3001 ist belegt: alten Server beenden oder PORT=3002 setzen.')], hinweis: 'port-belegt' } w.server = true; w.indexGebaut = true; return { zeilen: [A(' * Serving Flask app \'server\''), A(' * Running on http://127.0.0.1:3001'), H('→ Jetzt http://127.0.0.1:3001 im Browser öffnen. Der erste Suchlauf lädt die Modelle und baut den Index.')] } }
    if (argv[1] === 'lokale_ki.py') {
      if (!w.lmStudio.laeuft) return { zeilen: [...lade, F('openai.APIConnectionError: Connection error.'), H('→ LM Studio läuft nicht oder der Server ist aus. In LM Studio den lokalen Server starten (Port 1234).')], hinweis: 'lms-aus' }
      const p = profil(w); const id = p && (p.match(/^LOCAL_LLM_MODEL=(.+)$/m) || [])[1]
      if (!id && chatModelle(w).length !== 1) return { zeilen: [...lade, F('RuntimeError: LOCAL_LLM_MODEL auf die genaue ID des geladenen Chatmodells setzen.'), H(`→ LM Studio bietet ${chatModelle(w).length} Chatmodelle an; die Autoauswahl ist nicht eindeutig. ID in profiles/local/.env eintragen.`)], hinweis: 'modell-id' }
      if (id && !w.lmStudio.modelle.includes(id)) return { zeilen: [...lade, F(`openai.NotFoundError: Model "${id}" not found`), H('→ Die ID muss genau der in LM Studio angezeigten entsprechen (curl …/v1/models).')], hinweis: 'modell-falsch' }
      w.indexGebaut = true; w.antwortErhalten = true; return { zeilen: [...lade, ...ANTWORT_E18.split('\n').map(A)] }
    }
    if (argv[1] === 'eval/run_eval.py') { w.indexGebaut = true; return { zeilen: [A('🔎 Eval — Embedding: intfloat/multilingual-e5-small | top_k=12 → Rerank(BAAI/bge-reranker-v2-m3) → 5'), A('✅ [wasser-austritt] Recall 60% | 1. Treffer Rang 1'), A('✅ [fehler-e18] Recall 100% | 1. Treffer Rang 1'), A('… (10 Fragen)'), A('Trefferquote (≥1 Stichwort in Top-5): 10/10 = 100%'), A('hit@1 (Top-Knoten relevant):               10/10 = 100%'), A('MRR (mittlerer reziproker Rang):            1.00'), A('Ø Recall über alle Stichwörter:            93%')] } }
    return { zeilen: [A(`→ ${argv[1]} ausgeführt.`)] }
  }
  return unbekannt(w, exe)
}
const CMDS = {
  pwd: (w) => ({ zeilen: [A(pfadText(w))] }),
  ls: (w, a) => { const k = knoten(w, a[1] ? aufloesen(w, a[1]) : w.pfad); if (!k || k.typ !== 'ordner') return { zeilen: [F(`ls: ${a[1]}: No such file or directory`)], hinweis: 'kein-pfad' }; const alle = a.includes('-a') || a.includes('-la'); return { zeilen: [A(Object.keys(k.kinder).filter(n => alle || !n.startsWith('.')).sort().join('  ') || '')] } },
  cd: (w, a) => { const t = a[1] ? aufloesen(w, a[1]) : ['~']; const k = knoten(w, t); if (!k || k.typ !== 'ordner') return { zeilen: [F(w.os === 'mac' ? `cd: no such file or directory: ${a[1]}` : `Set-Location: Cannot find path '${a[1]}' because it does not exist.`)], hinweis: 'kein-pfad' }; w.pfad = t; return { zeilen: [] } },
  cat: (w, a) => { const k = knoten(w, aufloesen(w, a[1] || '')); if (!k || k.typ !== 'datei') return { zeilen: [F(`cat: ${a[1]}: No such file or directory`)], hinweis: 'kein-pfad' }; return { zeilen: k.inhalt.split('\n').filter((_, i, arr) => i < arr.length - 1 || _).map(A) } },
  cp: (w, a) => { const q = knoten(w, aufloesen(w, a[1] || '')); const zt = aufloesen(w, a[2] || ''); const zo = knoten(w, zt.slice(0, -1)); if (!q || q.typ !== 'datei' || !zo) return { zeilen: [F(`cp: ${a[1]}: No such file or directory`)], hinweis: 'kein-pfad' }; zo.kinder[zt.at(-1)] = datei(q.inhalt); return { zeilen: [] } },
  rm: (w, a) => { const t = aufloesen(w, a.filter(x => !x.startsWith('-')).at(-1) || ''); const o = knoten(w, t.slice(0, -1)); if (!o || !o.kinder[t.at(-1)]) return { zeilen: [F(`rm: ${t.at(-1)}: No such file or directory`)], hinweis: 'kein-pfad' }; if (o.kinder[t.at(-1)].typ === 'ordner' && !a.includes('-r') && !a.includes('-rf')) return { zeilen: [F(`rm: ${t.at(-1)}: is a directory`)], hinweis: 'ordner' }; delete o.kinder[t.at(-1)]; if (t.at(-1) === (w.venv && w.venv.name)) { w.venv = null; w.aktiv = false; w.pakete.clear(); w.devPakete = false } return { zeilen: [] } },
  mkdir: (w, a) => { const t = aufloesen(w, a[1] || ''); const o = knoten(w, t.slice(0, -1)); if (!o) return { zeilen: [F('mkdir: No such file or directory')], hinweis: 'kein-pfad' }; o.kinder[t.at(-1)] = ordner(); return { zeilen: [] } },
  echo: (w, a, roh) => { const m = /^echo\s+(.+?)\s*(>>|>)\s*(\S+)$/.exec(roh); if (!m) return { zeilen: [A(a.slice(1).join(' ').replace(/^["']|["']$/g, ''))] }; const inhalt = m[1].replace(/^["']|["']$/g, ''); const t = aufloesen(w, m[3]); const o = knoten(w, t.slice(0, -1)); if (!o) return { zeilen: [F('No such file or directory')], hinweis: 'kein-pfad' }; const alt = o.kinder[t.at(-1)]; o.kinder[t.at(-1)] = datei((m[2] === '>>' && alt ? alt.inhalt : '') + inhalt + '\n'); return { zeilen: [] } },
  git: (w, a) => { if (a[1] === 'clone') { const name = (a[2] || '').replace(/\.git$/, '').split('/').at(-1); if (!a[2] || !a[2].includes(REPO)) return { zeilen: [F(`fatal: repository '${a[2] || ''}' not found`)], hinweis: 'repo' }; const o = knoten(w, w.pfad); if (o.kinder[name]) return { zeilen: [F(`fatal: destination path '${name}' already exists and is not an empty directory.`)], hinweis: 'existiert' }; o.kinder[name] = projekt(); return { zeilen: [A(`Cloning into '${name}'...`), A('remote: Enumerating objects: 412, done.'), A('Receiving objects: 100% (412/412), 8.31 MiB | 6.20 MiB/s, done.')] } }
    if (a[1] === 'status') return { zeilen: imProjekt(w) ? [A('On branch main'), A("Your branch is up to date with 'origin/main'."), A('nothing to commit, working tree clean')] : [F('fatal: not a git repository (or any of the parent directories): .git')] }
    if (a[1] === 'log') return { zeilen: [A('5a3f74c Fix synced model cache and blank summary-only answers'), A('eec74d4 Unify local and OpenAI RAG with session-only GUI keys and validated retrieval'), A('dc85695 feat(guardrail): refuse to hallucinate when the question isn\'t in the manual'), A('d9f62d9 feat(phase1): two-stage retrieval — vector overfetch + cross-encoder rerank'), A('0266af9 feat(phase0): e5 prefix fix, persistent index, retrieval eval harness')] }
    return unbekannt(w, 'git ' + a[1]) },
  source: (w, a) => { if (w.os !== 'mac') return unbekannt(w, 'source'); if (!w.venv || !/activate$/.test(a[1] || '')) return { zeilen: [F(`source: no such file or directory: ${a[1] || ''}`)], hinweis: 'kein-pfad' }; w.aktiv = true; return { zeilen: [] } },
  deactivate: (w) => { w.aktiv = false; return { zeilen: [] } },
  curl: (w, a) => { const u = a.find(x => x.startsWith('http')) || ''; if (u.includes(':1234')) { if (!w.lmStudio.laeuft) return { zeilen: [F('curl: (7) Failed to connect to 127.0.0.1 port 1234 after 2 ms: Couldn\'t connect to server'), H('→ In LM Studio den lokalen Server starten.')], hinweis: 'lms-aus' } w.modelleGeprueft = true; return { zeilen: [A(JSON.stringify({ data: w.lmStudio.modelle.map(id => ({ id, object: 'model', owned_by: 'organization_owner' })), object: 'list' }, null, 2))] } }
    if (u.includes(':3001')) { if (!w.server) return { zeilen: [F('curl: (7) Failed to connect to 127.0.0.1 port 3001')], hinweis: 'server-aus' } return { zeilen: [A('{"model_connection":"not_checked","status":"ok"}')] } }
    return { zeilen: [F('curl: (6) Could not resolve host')], hinweis: 'host' } },
  lms: (w, a) => { if (a[1] === 'server' && a[2] === 'start') { w.lmStudio.laeuft = true; w.lmStudio.cors = a.includes('--cors'); return { zeilen: [A('Starting server...'), A(`Success! Server is now running on port 1234${w.lmStudio.cors ? ' (CORS enabled)' : ''}`)] } } return unbekannt(w, 'lms ' + (a[1] || '')) },
  open: (w, a) => ({ zeilen: [H(`→ Der Browser öffnet ${a[1] || ''}.`)] }),
  clear: () => ({ zeilen: [], leeren: true })
}
const ALIAS = { win: { dir: 'ls', type: 'cat', copy: 'cp', del: 'rm', md: 'mkdir', 'Get-ChildItem': 'ls', 'Get-Content': 'cat', 'Copy-Item': 'cp', 'Remove-Item': 'rm', 'Set-Location': 'cd', start: 'open', 'Get-Location': 'pwd' }, mac: {} }
export function fuehreAus (w, roh) {
  const zeile = roh.trim(); if (!zeile) return { zeilen: [] }
  const argv = zeile.match(/"[^"]*"|'[^']*'|\S+/g).map(t => t.replace(/^["']|["']$/g, ''))
  let cmd = argv[0]
  if (w.os === 'win' && /^\.?\.?\\?\.venv\\Scripts\\[Aa]ctivate(\.ps1)?$/.test(cmd.replace(/^\.\\/, ''))) { if (!w.venv) return { zeilen: [F(`.venv\\Scripts\\activate : The term '.venv\\Scripts\\activate' is not recognized`)], hinweis: 'kein-pfad' } w.aktiv = true; return { zeilen: [] } }
  if (w.os === 'mac' && cmd === '.' ) { cmd = 'source'; argv[0] = 'source' }
  if (/^python(3(\.\d+)?)?$|^py$/.test(cmd)) return python(w, argv)
  cmd = ALIAS[w.os][cmd] || cmd
  if (w.os === 'mac' && ['dir', 'type', 'copy', 'del', 'md'].includes(cmd)) return unbekannt(w, cmd)
  const f = CMDS[cmd]; if (!f) return unbekannt(w, cmd)
  const erg = f(w, argv, zeile); if (erg.zeilen.some(z => z.art === 'fehler') && !erg.hinweis) erg.hinweis = 'fehler'; return erg
}
/* ------------------------------------------------------------ Pruefung */
export function zustandTrifft (w, z) {
  if (!z) return true
  if (z.pfad != null && w.pfad.join('/') !== z.pfad) return false
  if (z.datei) { const k = knoten(w, aufloesen(w, z.datei.startsWith('~') ? z.datei : `~/Projekte/${z.datei}`)); if (!k) return false }
  if (z.venvVorhanden && !w.venv) return false
  if (z.venvPython && (!w.venv || w.venv.python !== z.venvPython)) return false
  if (z.venvAktiv != null && w.aktiv !== z.venvAktiv) return false
  if (z.pakete && !w.pakete.has('flask')) return false
  if (z.devPakete && !w.devPakete) return false
  if (z.profil && !(profil(w) || '').includes(z.profil)) return false
  if (z.serverLaeuft && !w.server) return false
  if (z.cors && !w.lmStudio.cors) return false
  if (z.modelleGeprueft && !w.modelleGeprueft) return false
  if (z.antwortErhalten && !w.antwortErhalten) return false
  if (z.testsGelaufen && !w.testsGelaufen) return false
  return true
}
export function schrittErfuellt (w, schritt, zeile, vorher, ergebnis) {
  const fehl = !!(ergebnis && ergebnis.zeilen.some(z => z.art === 'fehler'))
  if (schritt.erwarteterFehler) { if (!fehl || ergebnis.hinweis !== schritt.erwarteterFehler) return false } else if (fehl) return false
  if (schritt.muster && !new RegExp(schritt.muster, 'i').test(zeile)) return false
  if (schritt.zustand) { if (!zustandTrifft(w, schritt.zustand)) return false; if (!schritt.muster && vorher) return false }
  return true
}
/* ------------------------------------------------------------- Ablage */
export const serialisiere = (w) => JSON.stringify({ ...w, pakete: [...w.pakete] })
export const deserialisiere = (s) => { const w = JSON.parse(s); w.pakete = new Set(w.pakete); return w }
```

`baueTerminal(ziel, opt, ctx)` wird aus `../PITM/assets/pitm.js:411-560` übernommen (Kopf mit Ampel und Shell-Name, Schirm, Eingabezeile, Auftragskasten, Befehlsverlauf mit Pfeiltasten, Knöpfe „Leeren“ und „Zurücksetzen“); Änderungen: OS-Umschalter mac/win im Kopf (Übung `os: 'alle'`), Weltzustand nach jedem Befehl unter `rag:terminal:<opt.id>` serialisiert und beim Aufbau wiederhergestellt, Hinweiszeilen (`art: 'hinweis'`) gedimmt, ein Schalter „LM Studio läuft“ (Checkbox, setzt `welt.lmStudio.laeuft`), Übersetzung der Hinweise über `ctx.lang()` (Hinweistexte deutsch im Modul; für EN ein Wörterbuch `HINWEISE_EN` mit denselben Schlüsseln, Text nach `→` ersetzt).

- [ ] **Step 3: Werkzeug und Übungstyp**

`assets/werkzeuge/terminal.js`: `export function baue (wrap, p, ctx) { baueTerminal(wrap, { os: p.os || 'alle', szenario: p.szenario || {}, id: p.id || 'frei' }, ctx) }`; in `index.js` registrieren.

In `rag.js` `baueBox`: `if (uebung.typ === 'terminal') { const h = el('div'); body.append(h, statusZiel); baueTerminal(h, { os: uebung.os || 'alle', szenario: uebung.szenario || {}, schritte: uebung.schritte, id: uebung.id, beiFertig: () => { status(statusZiel, 'ok', txt(T.richtig), txt(uebung.rueckmeldung)); geloestMarkieren() } }, ctx) }`

Terminal-CSS aus `../PITM/assets/pitm.css` (Block „terminal“) übernehmen, Farben auf Umbra-Hintergrund `#2E2418` mit Sandschrift `#EFE7D6`.

- [ ] **Step 4: Tests, Bedienprobe, Commit**

Run: `node --test tools/pruefung.test.mjs` — Expected: alle PASS. Browser: Lab-10-Probeseite mit einem freien Terminal, beide Shells, Verlauf, Zurücksetzen.

```bash
git add -A && git commit -m "Nachgebildetes Terminal für den Nachbau: Welt, Befehle, Fehlerbilder, Übungstyp

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: Lab 01 · Sprachmodelle

**Files:**
- Create: `data/uebungen/lab-01.json` (5 Übungen), `diagramme/quellen/llm-training.mmd`, `diagramme/quellen/token-vorhersage.mmd`, `tools/gen_diagramme.mjs`
- Modify: `lab-01-sprachmodelle.html`, `QUELLEN.md` (Abschnitt Lab 01)

**Interfaces:**
- Consumes: Werkzeuge `tokenizer`, `temperatur`, `kontext`; `data/tokens.json`, `data/logits.json`.
- Produces: `tools/gen_diagramme.mjs` für alle folgenden Labs.

- [ ] **Step 1: Recherche (WebFetch/WebSearch, Ergebnisse mit Datum in QUELLEN.md)**

Zu prüfen und zu zitieren: Vaswani et al. 2017 „Attention Is All You Need“ (arXiv 1706.03762): Transformer, Self-Attention, keine Rekurrenz; Brown et al. 2020 (arXiv 2005.14165): In-Context Learning, 175 Mrd. Parameter; Ouyang et al. 2022 (arXiv 2203.02155): Instruktionstuning, RLHF; Sennrich et al. 2016 (arXiv 1508.07909): BPE; Kudo/Richardson 2018 (arXiv 1808.06226): SentencePiece; Ji et al. 2023 „Survey of Hallucination“ (ACM CSUR); Holtzman et al. 2020 „Curious Case of Neural Text Degeneration“ (arXiv 1904.09751): Temperatur, Top-p; Modellkarte `intfloat/multilingual-e5-small` (Tokenizer XLM-R, 250 002 Einträge — Zahl aus tokens.json); Kontextfenster: aktuelle Werte für gemma-4 (LM-Studio-Modellkarte) mit Stand — sonst generische Größen ohne Modellname.

- [ ] **Step 2: Diagrammgenerator**

`tools/gen_diagramme.mjs`: liest `diagramme/quellen/*.mmd`; ersetzt `[[de|en]]`-Platzhalter je Sprache; schreibt `assets/diagramme/<name>-<lang>.svg` über `npx -y @mermaid-js/mermaid-cli -i tmp.mmd -o out.svg -c diagramme/theme.json -b transparent`; `diagramme/theme.json`: `{"theme":"base","themeVariables":{"primaryColor":"#EFE7D6","primaryTextColor":"#2E2418","primaryBorderColor":"#2E2418","lineColor":"#5B4E3E","secondaryColor":"#F8F4EC","tertiaryColor":"#F5E3D6","fontFamily":"Space Grotesk, sans-serif","fontSize":"14px"}}`. Scheitert mermaid-cli (Chromium-Download), Diagramm mit dem MCP-Werkzeug `validate_and_render_mermaid_diagram` prüfen und als Ausweichlösung `<pre class="mermaid">` mit vendored `mermaid.min.js` (jsDelivr, Version pinnen) einbinden; im README festhalten, welcher Weg gilt.

`llm-training.mmd`:

```
flowchart LR
  A[[[de|Vortraining|en|Pre-training]]<br/>[[de|nächstes Token auf Billionen Tokens Text|en|next token on trillions of tokens of text]]] --> B[[[de|Instruktionstuning|en|Instruction tuning]]<br/>[[de|Frage-Antwort-Paare|en|prompt–response pairs]]]
  B --> C[[[de|Präferenzabgleich|en|Preference alignment]]<br/>[[de|Bewertungen, RLHF/DPO|en|ratings, RLHF/DPO]]]
  C --> D([[[de|Chatmodell, z. B. gemma-4-12b-it|en|Chat model, e.g. gemma-4-12b-it]]])
```

`token-vorhersage.mmd`: `flowchart LR` mit „Die Laugenpumpe ist“ → Tokenizer → IDs → Transformer → Verteilung (verstopft 41 %, defekt 12 %, …) → Sampling → „verstopft“ (Zahlen aus logits.json übernehmen; als `data-wert` nicht prüfbar im SVG, deshalb im Fließtext daneben mit Marker nennen).

Einbindung im HTML: `<figure class="diagramm"><span lang="de"><img src="assets/diagramme/llm-training-de.svg" alt="…"></span><span lang="en"><img src="assets/diagramme/llm-training-en.svg" alt="…"></span><figcaption>…</figcaption></figure>`.

- [ ] **Step 3: Übungen (`lab-01.json`)**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R01-01 | experiment `tokenizer` (`beispiel: "e18"`) | „Was bedeutet E:18?“ und „Laugenpumpe reinigen“ tokenisieren | Fragen: Anzahl Tokens von „Was bedeutet E:18?“ (aus tokens.json, Optionen ±2); warum „Laugenpumpe“ in mehrere Stücke zerfällt (richtig: seltenes Wort, Subwörter); DE braucht mehr Tokens als EN für denselben Satz (richtig, aus tokens.json `satz-de`/`satz-en`) |
| R01-02 | quiz | Was das Modell tut | richtig: „berechnet eine Wahrscheinlichkeitsverteilung über das nächste Token“; falsch: „schlägt in einer Datenbank nach“, „versteht die Bedeutung wie ein Mensch“ |
| R01-03 | experiment `temperatur` (`prompt: "e18"`) | Regler, Stichprobe | Fragen: bei T = 0 immer dasselbe Token (richtig: argmax); hohe T macht seltene Tokens wahrscheinlicher (richtig); warum das Fallbeispiel T = 0 nutzt (richtig: reproduzierbare, belegte Antworten) |
| R01-04 | rechnen | Kontextfenster 8 192: Systemprompt s (tokens.json), Frage 14, 5 Chunks × Mittelwert m (tokens.json), Antwort 1 024 → belegt gesamt; frei | `loesung` = s + 14 + 5·round(m) + 1024 (aus Daten berechnen und als Zahl eintragen; verify prüft den Wert über `data-wert`-Marker `tokens.systemprompt`), Toleranz 5 |
| R01-05 | quiz (mehrfach) | Halluzination und Wissensstand | richtig: „entsteht, weil das Modell immer ein plausibles nächstes Token erzeugt“, „Fakten nach dem Trainingsende kennt das Modell nicht“; falsch: „lässt sich mit mehr Parametern vollständig abstellen“ |

- [ ] **Step 4: Seite schreiben**

Abschnitte (`section-block`-IDs): `#tokens` (Tokenizer-Werkzeug, Erklärung Subwörter, BPE/SentencePiece, Zahl der Vokabeleinträge als `data-wert="tokens.vokabular"`), `#vorhersage` (Diagramm token-vorhersage, Temperatur-Werkzeug, Softmax-Formel als HTML), `#transformer` (Attention in vier Sätzen, Diagramm llm-training), `#kontext` (Kontext-Werkzeug, 14 000-Zeichen-Budget des Fallbeispiels, Beleg: `CONTEXT_MAX_CHARS`), `#halluzination` (Mechanismus, Wissensstand, Beispiel aus antworten.json `e18-nackt` als Zitat mit Datum), `#uebungen`, `#zusammenfassung`, `#quellen`. Roter Faden: der Satz „Was bedeutet E:18?“ als Beispiel in jedem Abschnitt. Begriffskarte (`class="befehl"`, wie DABA) für Token, Kontextfenster, Temperatur, Halluzination.

`data-wert`-Marker: `tokens.vokabular`, `tokens.systemprompt`, `tokens.e18Chunk`, `logits.modell`. In `verify.mjs` eine Tabelle `WERTE` anlegen: Schlüssel → Funktion über die Daten → erwarteter Text (Formatierung `de-DE` mit schmalem Leerzeichen für Tausender) und jeden Marker im HTML dagegen prüfen.

- [ ] **Step 5: Prüfen und Commit**

`node tools/verify.mjs` (Lab 01 komplett ok), Browser DE/EN, 390 px; alle fünf Übungen lösen.

```bash
git add -A && git commit -m "Lab 01: Sprachmodelle

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 9: Lab 02 · Warum RAG?

**Files:**
- Create: `data/uebungen/lab-02.json`, `diagramme/quellen/rag-ueberblick.mmd`
- Modify: `lab-02-warum-rag.html`, `QUELLEN.md`

- [ ] **Step 1: Recherche**

Lewis et al. 2020 (arXiv 2005.11401): Retriever (DPR) + Generator (BART), Gewichte des Generators bleiben bei Anfrage unverändert; Gao et al. 2023 (arXiv 2312.10997): Naive / Advanced / Modular RAG; Karpukhin et al. 2020 (arXiv 2004.04906) DPR; Liu et al. 2023 „Lost in the Middle“ (arXiv 2307.03172): Leistung sinkt bei relevanten Passagen in der Mitte langer Kontexte; Fine-Tuning vs RAG: Ovadia et al. 2023 (arXiv 2312.05934) „Fine-Tuning or Retrieval?“; OpenAI-Preisseite (Stand abrufen, Datum notieren; Preis gpt-4.1-mini je 1 Mio. Tokens ein/aus); DSGVO Art. 28 Auftragsverarbeitung (kurz, ohne Rechtsberatung); LM-Studio-Doku „Offline operation“.

- [ ] **Step 2: Diagramm `rag-ueberblick.mmd`**

```
flowchart LR
  subgraph I[[[de|Vorbereitung (einmal)|en|Preparation (once)]]]
    D[[[de|Handbuch PDF|en|Manual PDF]]] --> P[[[de|Parser → Markdown|en|Parser → Markdown]]] --> C[[[de|Chunks|en|Chunks]]] --> E[[[de|Embeddings|en|Embeddings]]] --> V[([[de|Vektorspeicher|en|Vector store]])]
  end
  subgraph A[[[de|Anfrage (jedes Mal)|en|Query (every time)]]]
    Q[[[de|Frage|en|Question]]] --> QE[[[de|Embedding|en|Embedding]]] --> S[[[de|Suche + Reranking|en|Search + reranking]]] --> K[[[de|Kontext|en|Context]]] --> L[[[de|LLM|en|LLM]]] --> R[[[de|belegte Antwort|en|grounded answer]]]
  end
  V -.-> S
```

- [ ] **Step 3: Übungen (`lab-02.json`)**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R02-01 | experiment `vergleich` (`frage: "e18"`) | nackt gegen RAG | Fragen: Was fehlt der nackten Antwort (richtig: Beleg aus dem Handbuch; die genaue Ursache laut Tabelle); was die RAG-Antwort zusätzlich liefert (Quelle mit Seite) |
| R02-02 | belegen | Sätze der nackten Antwort `e18-nackt` (aus antworten.json in 3–5 Sätze zerlegt, wörtlich) gegen den E:18-Chunk (`quellen: [{chunk: <E:18-Chunk-ID>}]`) | `belegt` je Satz nach Prüfung gegen den Chunk-Text festlegen (beim Schreiben der JSON den Chunk lesen) |
| R02-03 | zuordnen | Szenarien → Ansatz | Kategorien: prompting, longcontext, finetuning, rag. Elemente: „Ton und Format einer Antwort ändern“ → prompting; „ein einzelnes 20-seitiges Dokument, einmalig“ → longcontext; „Stil eines Hauses dauerhaft lernen, keine Fakten“ → finetuning; „5 000 Handbücher, wöchentlich aktualisiert, Antworten mit Quelle“ → rag; „Fehlercodes exakt zitieren“ → rag; „Fachjargon eines Teams sprechen“ → finetuning |
| R02-04 | quiz | Definition nach Lewis 2020 | richtig: „Retriever holt Passagen, Generator antwortet damit; die Modellgewichte bleiben bei der Anfrage unverändert“ |
| R02-05 | rechnen | Kosten: 1 000 Anfragen × (355 ein + 121 aus) Tokens bei den recherchierten Preisen → USD | `loesung` aus Preisen berechnen, Toleranz 0,01; Preise und Datum im Aufgabentext; Marker `antworten.tokens.e18` |

- [ ] **Step 4: Seite**

Abschnitte: `#luecke` (die drei Lücken: Wissensstand, Firmenwissen, Belegbarkeit; Werkzeug `vergleich`), `#idee` (Diagramm rag-ueberblick; Lewis 2020), `#alternativen` (Tabelle: Prompting, Long Context, Fine-Tuning, RAG mit Kriterien Aktualität, Belegbarkeit, Kosten je Anfrage, Aufwand; Lost in the Middle als Beleg gegen „alles in den Kontext“), `#lokal` (warum lokal: Datenschutz, Kosten mit Datum, Verfügbarkeit; Werkzeug `kosten` mit `pEin/pAus` aus der Recherche), `#fallbeispiel` (das Fallbeispiel in fünf Sätzen: Handbuch, e5-small, Reranker, Flask, LM Studio/OpenAI), `#uebungen`, `#zusammenfassung`, `#quellen`.

- [ ] **Step 5: Prüfen, Commit** (`Lab 02: Warum RAG?`)

---

### Task 10: Lab 03 · Dokumente aufbereiten

**Files:** `data/uebungen/lab-03.json`, `diagramme/quellen/aufbereitung.mmd`, `lab-03-aufbereiten.html`, `QUELLEN.md`

- [ ] **Step 1: Recherche**

Docling Technical Report (arXiv 2408.09869, IBM), Docling-Doku (`DocumentConverter`, `export_to_markdown(page_no=…)` — genaue Signatur prüfen); PyMuPDF-Doku (`page.get_text`); pypdf; Unstructured (kurz); PDF-Spezifikation: Text als positionierte Glyphen, keine Lesereihenfolge (ISO 32000, Einführung); OCR-Begriff.

- [ ] **Step 2: Diagramm `aufbereitung.mmd`**

`flowchart LR`: PDF (48 Seiten) → Docling → Markdown je Seite mit `<!-- pdf-page: N -->` → Prüfung gegen PDF (Tabellen, Warnhinweise) → aktive Wissensdatei → Cache-Fingerprint ändert sich → Index neu.

- [ ] **Step 3: Übungen**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R03-01 | experiment `seite` (`markierungen`: Muster `S t r ö n u`, `Ï`, `Ü`, `~ Seite \d+`, `<!-- pdf-page: 33 -->` mit Hinweisen) | Artefakte finden | Fragen: welches Symbol die OCR ersetzt (Ü = Start/Pause-Taste laut Handbuch-Legende), was „~ Seite 30“ ist (Querverweis, keine Fundseite), was der Seitenmarker leistet (Provenienz) |
| R03-02 | quiz | Warum PDF-Extraktion verlustbehaftet ist | richtig: PDF speichert Glyphen mit Koordinaten, keine Lesereihenfolge und keine Tabellenstruktur |
| R03-03 | sortieren | Aufbereitungsschritte in Reihenfolge | Elemente (richtig geordnet): PDF beschaffen → mit Docling parsen → Seitenmarker prüfen → Tabellen und Warnhinweise gegen das PDF vergleichen → aktive Wissensdatei ersetzen → Index neu bauen (Fingerprint) |
| R03-04 | luecken | Parseraufruf: `python parser.py --pdf ___1___ --output ___2___` | Lösungen: `siemens-handbuch.pdf`; `siemens_wissen_neu.md` (auch `siemens_wissen.md` akzeptieren? nein: der Text verlangt ausdrücklich eine neue Datei, nur `siemens_wissen_neu.md`) |
| R03-05 | quiz (mehrfach) | Provenienz | richtig: „pdf_page in den Metadaten stammt aus dem Seitenmarker“, „Querverweise im Text sind keine Fundseiten“; falsch: „Docling erkennt Warnsymbole zuverlässig als Text“ |

- [ ] **Step 4: Seite**

Abschnitte: `#pdf` (was ein PDF ist; Diagramm), `#docling` (Parser, Alternativen mit Stand, Codeauszug `parser.py` als `code-block`), `#artefakte` (Werkzeug `seite`; die zerschossene Überschrift der Notentriegelung als Beispiel, `manual_context.py` BOUNDARIES als Beleg), `#provenienz` (Seitenmarker, Querverweise, `format_source_reference`), `#pruefen` (warum die aktive Datei nicht blind ersetzt wurde: AUDIT.md „Neue Extraktionen zuerst mit dem PDF vergleichen“), `#uebungen`, `#zusammenfassung`, `#quellen`. Marker: `handbuch.seiten` (48), `handbuch.woerter`, `chunks.abschnitte` (Zahl der `##`-Überschriften: 187 — aus handbuch.md zählen in verify).

- [ ] **Step 5: Prüfen, Commit** (`Lab 03: Dokumente aufbereiten`)

---

### Task 11: Lab 04 · Chunking

**Files:** `data/uebungen/lab-04.json`, `diagramme/quellen/chunking-fluss.mmd`, `lab-04-chunking.html`, `QUELLEN.md`

- [ ] **Step 1: Recherche**

LlamaIndex-Doku: `MarkdownNodeParser`, `SentenceSplitter` (chunk_size in Tokens, Overlap, Tokenizer-Parameter), `excluded_embed_metadata_keys`; e5-Modellkarte: maximale Länge 512 Tokens; Chunking-Vergleiche: Pinecone „Chunking Strategies“ (Datum), LangChain `RecursiveCharacterTextSplitter` (zur Einordnung); Anthropic „Contextual Retrieval“ (Sept. 2024) als Einordnung für „Dokumentkontext: …“-Präfix.

- [ ] **Step 2: Diagramm `chunking-fluss.mmd`** — vier Stufen aus `prepare_nodes()`: Markdown-Abschnitte (MarkdownNodeParser) → Kontextgruppen (manual_context, „Dokumentkontext:“-Präfix) → Tabellen-Explosion (>1 600 Zeichen, je Zeile ein Node) → SentenceSplitter 440/40 mit e5-Tokenizer → Nodes mit Metadaten (section, pdf_page, context_group), Metadaten vom Embedding ausgeschlossen.

- [ ] **Step 3: Übungen**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R04-01 | experiment `chunking` (`abschnitt: "Hinweise im Anzeigefeld", strategie: "fest", groesse: 500, overlap: 50`) | Feste Länge gegen Pipeline | Fragen: Bei fester Länge 500 liegt E:18 in einem Chunk mit anderen Codes (richtig); Pipeline macht aus der Tabellenzeile einen eigenen Chunk (richtig); warum das die Suche verbessert (richtig: der Vektor beschreibt genau einen Code, nicht die ganze Tabelle) |
| R04-02 | quiz | Tokenmaß | richtig: „weil das Embedding-Modell höchstens 512 Tokens verarbeitet und 440 + Präfix + Sonderzeichen darunter bleiben“ |
| R04-03 | rechnen | Chunkzahl: Text mit 12 000 Tokens, Größe 440, Überlappung 40 → Schritt 400 → ceil((12000 − 440)/400) + 1 = 30 | `loesung: 30, toleranz: 0`; Lösungsweg im JSON |
| R04-04 | zuordnen | Feld → „im Embedding-Text“ / „nur Metadaten“ | „Dokumentkontext: Laugenpumpe …“-Präfix → text; `section` → meta; `pdf_page` → meta; `context_group` → meta; Tabellenkopf „Anzeige: … Ursache/Abhilfe: …“ → text; `exact_code_match` → meta |
| R04-05 | luecken | `.env`: `CHUNK_TOKENS=___1___`, `CHUNK_OVERLAP=___2___`, `MAX_NODE_CHARS=___3___` | 440, 40, 1600 |
| R04-06 | quiz | Pumpenreinigung | richtig: „Die Warnung und die Schritte lagen in getrennten Chunks; die Kontextgruppe liefert die ganze Anleitung“; Beleg AUDIT.md „Fehlender Arbeitskontext“ |

- [ ] **Step 4: Seite**

Abschnitte: `#warum`, `#strategien` (Tabelle fest/Sätze/Struktur/Tabellenzeile mit Vor- und Nachteil), `#tokens` (Tokenizer des Embedding-Modells, 440/40, `data-wert="tokens.maxChunkTokens"`), `#tabellen` (Port-Ausschnitt `_explode_markdown_tables` als Codeblock, Ergebnis E:18-Chunk wörtlich mit Marker `chunks.e18Text`), `#metadaten`, `#kontextgruppen`, Werkzeug `chunking`, `#uebungen`, `#zusammenfassung`, `#quellen`.

- [ ] **Step 5: Prüfen, Commit** (`Lab 04: Chunking`)

---

### Task 12: Lab 05 · Embeddings

**Files:** `data/uebungen/lab-05.json`, `diagramme/quellen/bi-encoder.mmd`, `lab-05-embeddings.html`, `QUELLEN.md`

- [ ] **Step 1: Recherche**

Wang et al. 2022 „Text Embeddings by Weakly-Supervised Contrastive Pre-training“ (arXiv 2212.03533) und Wang et al. 2024 „Multilingual E5“ (arXiv 2402.05672): Präfixe `query:`/`passage:`, Dimensionen (small 384, base 768, large 1024), Maximallänge 512; Reimers/Gurevych 2019 Sentence-BERT (arXiv 1908.10084): Bi-Encoder, Mean Pooling; BGE-M3 (Chen et al. 2024, arXiv 2402.03216): 1024 Dimensionen, 8 192 Tokens; nomic-embed-text (Modellkarte); MTEB-Leaderboard (Stand); Kosinusähnlichkeit (Definition).

- [ ] **Step 2: Diagramm `bi-encoder.mmd`** — zwei Türme: „query: Was bedeutet E:18?“ → Encoder → Vektor (384) · „passage: Anzeige: E:18; …“ → Encoder → Vektor (384) → Kosinus.

- [ ] **Step 3: Übungen**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R05-01 | rechnen | Kosinus von a = (1, 2, 2), b = (2, 0, 1) | 4/(3·√5) = 0,5963; Toleranz 0,005; Lösungsweg |
| R05-02 | experiment `embedding` (`a: "Laugenpumpe verstopft", b: "Die Pumpe ist blockiert"`) | Ähnlichkeit | Fragen: Ähnlichkeit hoch trotz anderer Wörter (richtig); Vergleich mit „Kindersicherung deaktivieren“ niedriger (richtig; Katalogfrage `kindersicherung` gegen `pumpe-blockiert` aus fragen.json als Beleg) |
| R05-03 | experiment `suche` (`frage: "fehler-e18"`) mit Präfix-Vergleich im Text | Präfixfehler: Rang des E:18-Chunks mit und ohne Präfix (aus `vektor` gegen `vektorOhne` in fragen.json) | Fragen: „Ohne Präfix steht der E:18-Chunk auf Rang …“ (Wert aus Daten, Marker `fragen.e18.rangOhne` und `rangMit`) |
| R05-04 | experiment `karte` (`frage: "wasser-laeuft"`) | Karte lesen | Fragen: Nachbarn gehören zum Abschnitt „Störungen, was tun?“ (aus Daten prüfen und Option entsprechend setzen); PCA zeigt … % der Varianz (Marker `projektion.varianz`) |
| R05-05 | quiz | Modellwahl für deutsches Handbuch, CPU, 440-Token-Chunks | richtig: multilingual-e5-small (mehrsprachig, 512 Tokens, 118 M Parameter, schnell); falsch: englisches Modell; Modell mit 256 Tokens Maximallänge |
| R05-06 | quiz | Bi-Encoder gegen Cross-Encoder | richtig: „Bi-Encoder rechnet Frage und Passage getrennt (vorberechenbar); Cross-Encoder liest beide zusammen (genauer, teurer)“ |

- [ ] **Step 4: Seite**

Abschnitte: `#vektor` (384 Zahlen, Beispielvektor aus chunks.json gekürzt), `#kosinus` (Formel, Rechenbeispiel), `#biencoder` (Diagramm, Mean Pooling, Normalisierung), `#praefix` (der größte Fehler der Ausgangsversion: Zitat rag_engine.py-Kommentar; Rangvergleich aus Daten), `#modelle` (Tabelle e5-small/base/large, bge-m3, nomic mit Dimension, Länge, Sprache, Stand), Werkzeuge `embedding` und `karte`, `#grenzen` („Wasser schießt aus der Maschine“ Score aus fragen.json, Marker), `#uebungen`, `#zusammenfassung`, `#quellen`.

- [ ] **Step 5: Prüfen, Commit** (`Lab 05: Embeddings`)

---

### Task 13: Lab 06 · Retrieval und Reranking

**Files:** `data/uebungen/lab-06.json`, `diagramme/quellen/retrieval-stufen.mmd`, `lab-06-retrieval.html`, `QUELLEN.md`

- [ ] **Step 1: Recherche**

Robertson/Zaragoza 2009 „The Probabilistic Relevance Framework: BM25 and Beyond“; Cormack et al. 2009 „Reciprocal Rank Fusion“ (SIGIR); Malkov/Yashunin 2018 HNSW (arXiv 1603.09320); Nogueira/Cho 2019 „Passage Re-ranking with BERT“ (arXiv 1901.04085); BGE-Reranker-v2-m3-Modellkarte (Sigmoid-Hinweis, Sprachen); LlamaIndex `SentenceTransformerRerank`, `SimpleVectorStore` (exakte Suche, keine ANN); pgvector, Qdrant, Chroma (je Doku-Startseite, Stand); Hybrid-Suche-Doku (z. B. Qdrant „Hybrid Queries“) zur Einordnung von BM25+RRF.

- [ ] **Step 2: Diagramm `retrieval-stufen.mmd`** — Frage → `query:`-Embedding → Top-12 (Kosinus, exakt über 346 Vektoren) → Fehlercode-Regex → exakte Chunks vorn (Score 1) → Cross-Encoder bewertet jedes Paar (Sigmoid) → Top-5 → Schwelle 0,15 und Boden 0,5 × bester → Kontextgruppen erweitern → Kontext (≤ 14 000 Zeichen).

- [ ] **Step 3: Übungen**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R06-01 | experiment `suche` (`frage: "fehler-e18"`) | Stufen verfolgen | Fragen: In welcher Stufe erreicht der E:18-Chunk Rang 1 (aus Daten: bei reiner Vektorsuche Rang r, mit Hybrid Rang 1 — Optionen mit Werten aus fragen.json); Reranker-Score des E:18-Chunks (Marker `fragen.e18.rerankScore`, Optionen ±0,1) |
| R06-02 | quiz (mehrfach) | Welche Eingaben treffen E:18 | richtig: „E18“, „e:18“, „Fehler 18“, „Fehlercode: 18“; falsch: „E:180“, „18“ |
| R06-03 | sortieren | Stufen in Reihenfolge | Frage einbetten → Top-12 Vektorsuche → exakte Fehlercode-Chunks voranstellen → Reranker bewertet Kandidaten → Top-5 → Schwelle und Verhältnis → Kontextgruppen erweitern → Kontext ans Modell |
| R06-04 | rechnen | Boden: bester Score 0,98, Verhältnis 0,5, Schwelle 0,15 → Boden 0,49; Kandidaten 0,98 / 0,60 / 0,40 / 0,20 / 0,10 → wie viele bleiben | Felder: Boden 0,49 (Toleranz 0,001), Anzahl 2 (Toleranz 0) |
| R06-05 | zuordnen | Eigenschaft → Verfahren (bm25, dense, cross, rrf) | „zählt exakte Wortübereinstimmungen, gewichtet seltene Wörter“ → bm25; „findet Synonyme und Umschreibungen“ → dense; „liest Frage und Passage gemeinsam, langsam, genau“ → cross; „verschmilzt zwei Ranglisten ohne Scores zu vergleichen“ → rrf; „braucht keinen Vektorindex“ → bm25; „im Fallbeispiel nicht eingesetzt“ → rrf (Text sagt: weder BM25 noch RRF; Element eindeutig formulieren: „kombiniert Ranglisten 1/(k+Rang)“ → rrf) |
| R06-06 | experiment `suche` (`frage: "wasser-schiesst"`) | Grenze | Fragen: oberster Score unter 0,15 → abgelehnt (richtig); dieselbe Absicht als „Wasser läuft aus“ wird gefunden (Marker `fragen.wasserLaeuft.score`); Schwelle senken hätte welche Folge (richtig: mehr Fremdfragen angenommen, vgl. Lab 08) |

- [ ] **Step 4: Seite**

Abschnitte: `#topk` (exakte Suche über 346 Vektoren; ab wann ANN/HNSW), `#speicher` (SimpleVectorStore JSON; pgvector/Qdrant/Chroma-Tabelle), `#hybrid` (Regex `_CODE_RE` wörtlich; BM25/RRF Einordnung), `#reranker` (Cross-Encoder, Sigmoid, Overfetch 12 → 5, Beleg `get_reranker`), `#schwelle` (0,15 und 0,5; Zitat „Projektparameter, keine kalibrierte Wahrheitswahrscheinlichkeit“), `#ablation` (Balkendiagramm HTML mit `data-wert="eval.vector.hit5"` etc. für 80/90/100 % und MRR), Werkzeug `suche`, `#uebungen`, `#zusammenfassung`, `#quellen`.

- [ ] **Step 5: Prüfen, Commit** (`Lab 06: Retrieval und Reranking`)

---

### Task 14: Lab 07 · Prompt und Generierung

**Files:** `data/uebungen/lab-07.json`, `diagramme/quellen/sse-ablauf.mmd`, `lab-07-generierung.html`, `QUELLEN.md`

- [ ] **Step 1: Recherche**

OpenAI Chat Completions API (Rollen system/user, `temperature`, `max_tokens`, `stream`, `finish_reason` = `length`, `stream_options.include_usage`); MDN Server-Sent Events (Format `event:`/`data:`), MDN Fetch Streams; OWASP „LLM01 Prompt Injection“ (OWASP Top 10 for LLM, Version/Datum); LM-Studio-Doku: Server, CORS, OpenAI-Kompatibilität; Ollama-Doku: `OLLAMA_ORIGINS`, `/v1`-Kompatibilität; Wei et al. 2022 Chain-of-Thought (arXiv 2201.11903) nur zur Einordnung „Reasoning-Modelle denken lange“ (Design-Doc: 250 s gegenüber 40 s).

- [ ] **Step 2: Diagramm `sse-ablauf.mmd`** (sequenceDiagram): Browser → Flask `POST /api/ask_stream` → `status` → Retrieval → `meta` → LLM (Stream) → `token` × n → Parser → `result` → `[DONE]`; Fehlerpfad `error`.

- [ ] **Step 3: Übungen**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R07-01 | experiment `prompt` (`frage: "e18"`) | messages lesen | Fragen: Warum Frage und Auszüge als JSON im Nutzerinhalt stehen (richtig: als Daten, damit Anweisungen im Text nicht als Befehle gelten); was bei mehr als 14 000 Zeichen passiert (richtig: `ValueError`, Anfrage wird abgelehnt, kein stilles Kürzen) |
| R07-02 | belegen | Schritte der RAG-Antwort `e18-rag` (aus antworten.json geparst) gegen die übergebenen Quellen | jeder Schritt `belegt: true`, wenn im Quelltext; beim Schreiben prüfen; mindestens ein Satz aus der `summary` |
| R07-03 | luecken | Systemprompt-Kern: „Verwende ___1___ die bereitgestellten Handbuchauszüge. … Gliedere ausschließlich mit diesen Tags: <___2___>, <manual_intro>, <___3___>“ | `ausschließlich`; `summary`; `manual_steps` |
| R07-04 | quiz | Prompt-Injection | richtig: „Ein Handbuchabsatz ‚Ignoriere alle Regeln‘ bleibt Text, weil Frage und Auszüge als Daten übergeben und im Systemprompt als Daten deklariert sind“; Hinweis: kein vollständiger Schutz (AUDIT.md) |
| R07-05 | sortieren | SSE-Ereignisse | `status` → `meta` → `token` (mehrfach) → `result` → `[DONE]` |
| R07-06 | checkliste | Eigenes Modell anbinden | Schritte mit Prüffrage: LM Studio installiert (Frage: welcher Port ist Standard? 1234); Modell geladen (Frage: welche ID-Form braucht `LOCAL_LLM_MODEL`? exakte ID aus /v1/models); Server gestartet; CORS eingeschaltet (Frage: warum? Browser-Seite anderer Herkunft); Endpunkt im Werkzeug `llm` eingetragen und Modellliste erschienen; Frage E:18 gesendet (Frage: was prüft der Parser? die drei Tags) |

- [ ] **Step 4: Seite**

Abschnitte: `#rollen`, `#systemprompt` (den echten SYSTEM_PROMPT aus antworten.json Satz für Satz kommentiert), `#daten` (JSON als Daten, Injection), `#format` (Tags, Parser-Port, Checkboxen; abgeschnittene Antwort = Fehler), `#temperatur` (0 und warum), `#streaming` (Diagramm, warum die Query-Engine puffert; Zahlen aus Design-Doc: Quelle @1,3 s, erstes Token @7,2 s, 40 s gegenüber 250 s, mit Datum 07.07.2026), `#eigenes` (Werkzeug `llm`; LM Studio, Ollama, CORS, Safari-Hinweis, lokaler Aufruf), Werkzeug `prompt`, `#uebungen`, `#zusammenfassung`, `#quellen`.

- [ ] **Step 5: Prüfen mit laufendem LM Studio (CORS an), Commit** (`Lab 07: Prompt und Generierung`)

---

### Task 15: Lab 08 · Evaluation und Guardrails

**Files:** `data/uebungen/lab-08.json`, `lab-08-evaluation.html`, `QUELLEN.md`

- [ ] **Step 1: Recherche**

Es et al. 2023 RAGAS (arXiv 2309.15217): Faithfulness, Answer Relevance, Context Precision/Recall; MRR/Hit@k-Definitionen (Manning et al., Introduction to Information Retrieval, Kap. 8); nDCG kurz; Saad-Falcon et al. 2023 ARES (arXiv 2311.09476) als Einordnung; das Protokoll `data/eval/*.json` (Felder `keyword_hit_rate`, `keyword_hit_at_1`, `keyword_mrr`, `keyword_coverage`, `metric_note`).

- [ ] **Step 2: Übungen**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R08-01 | experiment `metrik` (`variante: "vektor"`) | Vier Werte selbst berechnen | Fragen (nach dem Werkzeug): Welche Frage findet bei reiner Vektorsuche keinen Treffer (aus Daten: `fehler-e18` und `kein-wassereinlauf`); was Hit@1 misst |
| R08-02 | rechnen | MRR aus Rängen 1, 2, 1, –, 3 | (1 + 0,5 + 1 + 0 + 0,333)/5 = 0,5667, Toleranz 0,002 |
| R08-03 | experiment `schwelle` | Regler | Fragen: bei 0,15 wird eine Handbuchfrage abgelehnt — welche (`kein-wassereinlauf`, Score aus Daten, Marker `fragen.keinWassereinlauf.score`); bei 0,05 werden alle 10 angenommen, wie viele Fremdfragen (Daten: 0, weil Negativscores < 0,03 — prüfen: Bosch 0,027 < 0,05 → 0); was eine niedrigere Schwelle riskiert |
| R08-04 | quiz (mehrfach) | Was der Stichwort-Proxy nicht misst | richtig: „ob die generierte Antwort richtig ist“, „ob alle relevanten Abschnitte gefunden wurden (Recall)“; falsch: „ob mindestens ein erwartetes Wort im Kontext steht“ (das misst er) |
| R08-05 | zuordnen | RAGAS-Maße → Bedeutung | faithfulness → „Behauptungen der Antwort sind durch den Kontext gedeckt“; answer_relevance → „Antwort passt zur Frage“; context_precision → „gefundene Passagen sind relevant“; context_recall → „alle nötigen Passagen wurden gefunden“ |
| R08-06 | quiz | Ablation lesen | Welche Komponente hebt E:18 von 0 auf 100 % Abdeckung (Hybrid-Fehlercode); welche hebt „wasser-austritt“ (Reranker) — Werte aus data/eval mit Markern |

- [ ] **Step 3: Seite**

Abschnitte: `#trennen` (Retrieval getrennt messen; `run_eval.py` ohne LLM), `#masse` (Hit@k, Hit@1, MRR, Abdeckung mit Formeln), `#zehnfragen` (Tabelle aus eval/questions.json mit Stichwörtern), `#ablation` (drei Varianten, Tabelle mit Markern), `#negativ` (vier Fälle mit Scores), `#schwelle` (Abwägung, Werkzeug `schwelle`, der Grenzfall 0,119), `#antwortqualitaet` (RAGAS-Maße, was fehlt: annotierter Testbestand; Zitat AUDIT „Verbleibende Grenzen“ Nr. 2), Werkzeug `metrik`, `#uebungen`, `#zusammenfassung`, `#quellen`.

- [ ] **Step 4: Prüfen, Commit** (`Lab 08: Evaluation und Guardrails`)

---

### Task 16: Lab 09 · Architektur, Betrieb, Sicherheit

**Files:** `data/uebungen/lab-09.json`, `diagramme/quellen/architektur.mmd`, `diagramme/quellen/cache-fingerprint.mmd`, `lab-09-architektur.html`, `QUELLEN.md`; `assets/werkzeuge/kosten.js` (Preise mit Datum eintragen)

- [ ] **Step 1: Recherche**

Flask-Sicherheitsdoku (Web Security: CSRF, Cookies HttpOnly/SameSite, CSP); OWASP CSRF Cheat Sheet; MDN CSP, Origin-Header, `Sec-Fetch-Site`; Python `hashlib.sha256`, `filelock`; OpenAI-Preisliste (Datum) für gpt-4.1-mini; LM-Studio-Systemvoraussetzungen (RAM je Modellgröße, Stand); Strompreis-Annahme als editierbarer Parameter (keine Quelle nötig, als Annahme gekennzeichnet).

- [ ] **Step 2: Diagramme** — `architektur.mmd`: der Flowchart aus TECHNICAL.md des Fallbeispiels (übersetzt); `cache-fingerprint.mmd`: Markdown-Inhalt + Modellname + Parser-Version + Chunk-Parameter → SHA-256 → `.cache_key` gleich? → laden / neu bauen (FileLock).

- [ ] **Step 3: Übungen**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R09-01 | experiment `architektur` | Komponenten anklicken | Fragen: welche Datei prüft Eingabelänge und Origin (server.py); wo der OpenAI-Schlüssel liegt (Arbeitsspeicher, SessionKeys); was `storage/` enthält (JSON-Vektorspeicher, kein Datenbankserver) |
| R09-02 | zuordnen | Bedrohung → Maßnahme | „Schlüssel landet im Browser-Speicher“ → „nur im RAM des Servers, je Sitzung, 8 h“; „fremde Webseite ruft die lokale API auf“ → „Loopback und Origin-Prüfung“; „Formular von fremder Seite setzt den Schlüssel“ → „CSRF-Token“; „Handbuchtext enthält Anweisungen“ → „Kontext als JSON-Daten, Systemprompt deklariert Daten“; „alter Index nach Modellwechsel“ → „SHA-256-Fingerprint“; „16-MB-Anfrage“ → „Body-Limit 16 KiB, Frage ≤ 2 000 Zeichen“ |
| R09-03 | luecken | Fingerprint-Zutaten: `compute_cache_key(md_path, ___1___, parser_version, max_node_chars, ___2___, chunk_overlap)` | `embed_model`; `chunk_tokens` |
| R09-04 | rechnen | Kosten: 200 Anfragen/Tag × 30 Tage × (355 + 121 Tokens) bei recherchierten Preisen → USD je Monat | aus Preisen berechnen, Toleranz 0,05; Werkzeug `kosten` mit denselben Voreinstellungen |
| R09-05 | sortieren | Ablauf einer Anfrage | POST /api/ask_stream → Validierung (Länge, Modus, Anbieter) → Retrieval → gedeckt? → messages bauen (Budget) → LLM-Stream → Parser → result → [DONE] |

- [ ] **Step 4: Seite**

Abschnitte: `#komponenten` (Werkzeug `architektur`; Tabelle Datei/Aufgabe aus TECHNICAL.md), `#anfrage` (Sequenz, Routen-Tabelle GET/POST), `#cache` (Diagramm, FileLock, HF-Cache außerhalb OneDrive — MAC-LIVE.md), `#schluessel` (SessionKeys, CSRF, Cookies), `#netz` (Loopback, Origin, CSP, Sec-Fetch-Site), `#kosten` (Werkzeug `kosten`, Tokenzahlen aus LIVE.md mit Datum, Preise mit Datum), `#grenzen` (Mehrbenutzer, Folgefragen, QR nur mit statischer Leseseite), `#uebungen`, `#zusammenfassung`, `#quellen`.

- [ ] **Step 5: Prüfen, Commit** (`Lab 09: Architektur, Betrieb, Sicherheit`)

---

### Task 17: Lab 10 · Nachbauen

**Files:** `data/uebungen/lab-10.json`, `diagramme/quellen/nachbau-phasen.mmd` (gitGraph), `lab-10-nachbauen.html`, `QUELLEN.md`

- [ ] **Step 1: Recherche**

Python-Doku `venv`; pip-Doku `requirements.txt`, PEP 668 (externally-managed-environment); LM-Studio-Doku (Download, Modell laden, Server, CORS, Systemanforderungen, Stand); Git-Doku `clone`; Windows PowerShell Execution Policy für `Activate.ps1` (`Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`); die Commit-Historie des Fallbeispiels (`git log`, Hashes: 0266af9, d9f62d9, 1a52f03, 6f2e9a7, 0617fa3, dc85695, b71a58e, eec74d4, 5a3f74c); README des Fallbeispiels (Start-Abschnitt wörtlich als Vorlage).

- [ ] **Step 2: Diagramm `nachbau-phasen.mmd`** (gitGraph mit den echten Commits: Baseline → Phase 0 Präfix-Fix, Persistenz, Eval → Phase 1 Reranking → Phase 1+ Tabellen-Chunking, Hybrid → Phase 2a Quellen → Phase 2b SSE → Guardrail → PageIndex → Vereinigung lokal/OpenAI → Cache-Fix).

- [ ] **Step 3: Übungen**

| ID | Typ | Inhalt | Prüfdaten |
|---|---|---|---|
| R10-01 | checkliste | Voraussetzungen | Python 3.12 (Frage: welcher Befehl zeigt die Version? `python3.12 --version` / `py -3.12 --version`); Git; LM Studio; freier Speicher (Frage: wofür die GB? Modellgewichte und Python-Pakete); Projektordner außerhalb von OneDrive (Frage: warum? Modellcache und venv sind nicht portabel) |
| R10-02 | terminal (`os: "alle"`, `szenario: { geklont: false }`) | Einrichten | Schritte: `git clone …` (zustand datei `…/requirements.txt`); `cd` ins Projekt (pfad); venv mit 3.12 anlegen (venvVorhanden, venvPython 3.12); aktivieren (venvAktiv); `python -m pip install -r requirements.txt` (pakete) |
| R10-03 | terminal (`szenario: { geklont: true, modelle: [drei Chatmodelle + Embedding] }`) | Starten | Schritte: venv anlegen und aktivieren; installieren; Profil kopieren (datei `…/profiles/local/.env`); `curl …/v1/models` (modelleGeprueft); Modell-ID eintragen (profil `LOCAL_LLM_MODEL=gemma-4-12b-it-mlx`); `python lokale_ki.py "Was bedeutet E:18?"` (antwortErhalten); `python server.py` (serverLaeuft) |
| R10-04 | zuordnen | Fehlerbild → Ursache | `ModuleNotFoundError: No module named 'flask'` → venv nicht aktiv oder Pakete fehlen; `JSONDecodeError … modules.json` → Modellcache über OneDrive synchronisiert, Dateien leer; `LOCAL_LLM_MODEL auf die genaue ID … setzen` → mehrere Chatmodelle geladen; `Address already in use` → Port 3001 belegt; `UnicodeEncodeError … cp1252` → Windows-Konsole (Logging statt Emoji-Print); Seite zeigt keine Antworten bei `file://` → HTML direkt geöffnet statt über den Server |
| R10-05 | checkliste | Eigenes Dokument einbinden | PDF ablegen (Frage: welcher Befehl parst? `python parser.py --pdf … --output …`); Extraktion gegen PDF prüfen (Frage: was zuerst? Tabellen und Warnhinweise); Wissensdatei ersetzen (Frage: was passiert mit dem Index? Fingerprint ändert sich, Neuaufbau); `eval/questions.json` mit eigenen Fragen und Stichwörtern; `run_eval.py` laufen lassen (Frage: was misst es? Stichworttreffer, keine Antwortkorrektheit) |

- [ ] **Step 4: Seite**

Abschnitte: `#voraussetzungen`, `#einrichten` (Befehle mac/win als Befehlskarten, PEP 668, Execution Policy), `#lmstudio` (Modell laden, Server, CORS, Modell-ID), `#erste-frage` (`lokale_ki.py`, Oberfläche, erste Suche lädt Modelle), `#pruefen` (pytest, run_eval), `#phasen` (gitGraph; je Phase ein Satz mit Messwert aus dem Design-Doc: Recall 64 → 96 %, hit@1 70 → 100 % vom 07.07.2026), `#fehlerbilder` (Tabelle aus MAC-LIVE.md und AUDIT.md), `#eigenes-dokument`, freies Terminal (`data-werkzeug="terminal"`), `#uebungen`, `#zusammenfassung`, `#quellen`.

- [ ] **Step 5: Prüfen, Commit** (`Lab 10: Nachbauen`)

---

### Task 18: Startseite, README, QUELLEN

**Files:** `index.html`, `README.md`, `QUELLEN.md`, `data/QUELLE.md` (Herkunft der Handbuchdaten)

- [ ] **Step 1: Startseite** — Hero: Tag „Lernumgebung · Retrieval-Augmented Generation“, Titel „RAG“, Untertitel „Antworten, die im Dokument stehen“, Beschreibung (zehn Labs, das Modell des Fallbeispiels läuft im Browser, jede Zahl gemessen), Badges (Sprachmodelle, Embeddings, Retrieval, Reranking, LM Studio, Ohne Anmeldung, Ohne Installation), Stats (10 Labs, 55 Übungen, 346 Chunks, 24 Katalogfragen — Marker), Kacheln aus `LABS`, Abschnitt „Wie das hier funktioniert“ (drei Kästen), Fortschritts-Hinweis, „Zum Ausprobieren“ mit Werkzeug `suche` (`frage: "fehler-e18"`), „Erster Schritt“.
- [ ] **Step 2: README** nach PROM-Muster: Live-Link, Aufbau (Dateibaum), „Wie das Modell in den Browser kommt“ (transformers.js-Version, q8, Hub, Cache, Fallback, Selbsttest), neun Übungstypen (Tabelle), Werkzeuge (Tabelle), Daten neu erzeugen (`tools/daten.sh`, Fallbeispiel-Pfad), Prüfen (`npm test`, `npm run verify`, Browser-Skripte), Veröffentlichen, Fortschritt, Herkunft der Inhalte, Lizenzen (transformers.js Apache-2.0, ONNX Runtime MIT, Modell MIT laut Modellkarte prüfen), Hinweis Musterlösungen öffentlich; Mermaid-Diagramm der Pipeline nativ im README.
- [ ] **Step 3: QUELLEN.md** — je Lab die Quellen mit Titel, Autoren, Jahr, URL, Abrufdatum; `data/QUELLE.md`: Handbuchtexte aus dem Fallbeispiel-Repo, BSH/Siemens als Rechteinhaber, Nutzung zu Lehrzwecken.
- [ ] **Step 4: Commit** (`Startseite, README, Quellen`)

---

### Task 19: Textdurchsicht mit /folientext-pruefen

**Files:** alle `lab-*.html`, `index.html`, `data/uebungen/*.json`

- [ ] **Step 1:** Skill `/folientext-pruefen` aufrufen; statt PowerPoint-Shapes die Einleitungstexte (`.lab-intro`, erste `<p>` je `section-block`, `.concept-box`, `.tip-box`, `.warn-box`, `.challenge-box`, `aufgabe`/`hinweis`/`rueckmeldung`/`erklaerung` der Übungen) mit einem Node-Skript `tools/pruefung/texte.mjs` einsammeln (DE und EN), die beiden Mustersuchen des Skills darüber laufen lassen, jeden Treffer ersetzen oder als Ausnahme begründen, identische Einleitungen aufspüren, Zahlangaben gegen `data/` prüfen.
- [ ] **Step 2:** Bericht als Abschnitt in `PRUEFBERICHT.md` (je geänderter Stelle eine Zeile).
- [ ] **Step 3: Commit** (`Textdurchsicht`)

---

### Task 20: Abnahme

**Files:** `tools/pruefung/durchlauf.js`, `tools/pruefung/audit.js`, `PRUEFBERICHT.md`

- [ ] **Step 1: `durchlauf.js`** — in der Konsole einer Lab-Seite: für jede `.uebung` je Typ die Musterlösung eintragen (quiz/experiment: `richtig`-Indizes; zuordnen: `ziel`; sortieren: Reihenfolge herstellen durch Klicks; rechnen: `loesung`; luecken: erste `loesung` bzw. ein Beispiel für `muster` aus dem JSON-Feld `beispiel` (Feld in luecken ergänzen, verify prüft es); belegen: `belegt`; checkliste: `richtig`; terminal: Befehlsfolge aus JSON-Feld `loesung` (Liste von Zeilen je Übung, verify prüft, dass sie alle Schritte erfüllt — dafür `tools/verify.mjs` mit `fuehreAus`/`schrittErfuellt` erweitern)), „Prüfen“ klicken, „Richtig“ erwarten; Ergebnisliste ausgeben. Aus `../DABA/tools/pruefung/durchlauf.js` und PROM ableiten.
- [ ] **Step 2: `audit.js`** aus `../DABA/tools/pruefung/audit.js` übernehmen (Überschriftensprünge, Alternativtexte, Bedienelemente ohne Beschriftung, tote Links, horizontaler Überlauf, deutsche Reste im EN-Modus).
- [ ] **Step 3: Läufe** — `npm test`, `npm run verify`; jede der elf Seiten in DE und EN bei 390/768/1440 px (in-app Browser: `resize_window`); `durchlauf.js` je Lab; Modell laden (Hub) und Selbsttest; Hub blockiert simulieren (in der Konsole `env.remoteHost = 'https://0.0.0.0/'` vor dem Laden) → Fallback sichtbar; LLM-Werkzeug gegen LM Studio mit CORS aus (Fehlertext) und an (Stream); Terminal-Übungen mac und win.
- [ ] **Step 4: `PRUEFBERICHT.md`** nach PROM-Muster: Stand, Läufe, Befunde, Behobenes, Grenzen.
- [ ] **Step 5: Commit** (`Abnahme: Durchlauf, Audit, Prüfbericht`)

---

### Task 21: Veröffentlichen

- [ ] **Step 1:** `gh repo create swrobuts/RAG-Lab --public --source=. --push`
- [ ] **Step 2:** `gh api repos/swrobuts/RAG-Lab/pages -X POST -f source[branch]=main -f source[path]=/`; warten bis `gh api repos/swrobuts/RAG-Lab/pages` `"status":"built"` meldet.
- [ ] **Step 3:** SHA-256-Vergleich: `for f in assets/rag.js data/chunks.json data/uebungen/lab-06.json lab-06-retrieval.html; do echo "$f"; shasum -a 256 "$f" | cut -c1-16; curl -sL "https://swrobuts.github.io/RAG-Lab/$f" | shasum -a 256 | cut -c1-16; done` — Paare müssen gleich sein.
- [ ] **Step 4:** Live-Seite im Browser öffnen: Startseite, Lab 06 mit Modell-Ladeweg (Hub), Lab 07 LLM-Werkzeug (Safari-Hinweis prüfen), Lab 10 Terminal.
- [ ] **Step 5:** README-Live-Link prüfen; Memory-Eintrag `rag-lab-projekt.md` anlegen (Repo, Pfad, Stand, offene Punkte); abschließende Meldung an den Nutzer mit Live-Link, Prüfständen und offenen Punkten.

---

## Self-Review

**Spec coverage:** Leitentscheidungen → Aufgaben 1, 5, 7; Kapitelstruktur → 8–17; neun Übungstypen → 3, 4, 7; 16 Werkzeuge → 5, 6, 7; Modell im Browser → 5; Eigenes LLM → 6 (llm.js), 14; Daten → 2; Architektur/Dateien → 1–7; Gestaltung → 1; Recherche/Quellen/Texte → 8–19; Prüfung → 3, 4, 20; Veröffentlichung → 21; Arbeitspakete 1–10 des Specs → Aufgaben 1–2 (Paket 1–2), 3–4 (3), 5 (4), 6 (5), 7 (6), 8–18 (7), 19 (8), 20 (9), 21 (10). Risiko „Zahlen im Text = Daten“ → `data-wert`-Marker in Aufgabe 8 Schritt 4 (Tabelle `WERTE` in verify) — die Schlüssel werden in den Lab-Aufgaben genannt und in verify zentral gepflegt.

**Placeholder scan:** Keine TBD/TODO. Offene Entscheidungen sind als Prüfschritte formuliert (ESM-Dateiname von transformers.js, `encode`-Rückgabeform, mermaid-cli-Ausweichweg, Preise mit Datum aus der Recherche).

**Type consistency:** `baueWerkzeug(ziel, name, parameter, ctx)` (Aufgabe 5) wird in Aufgabe 4 und 5 gleich verwendet; `ctx = { lab, basis, lang, txt, daten, modell }`; `zwei(node, o, ctx, attr)` in `gemein.js` hat vier Parameter, `zwei(node, o, attr)` in `rag.js` drei (unterschiedliche Module, jeweils konsistent verwendet); Prüferfunktionen aus Aufgabe 3 werden in Aufgabe 4 mit denselben Signaturen aufgerufen; `fragen.json`-Felder (`vMit`, `vOhne`, `vektor`, `vektorOhne`, `hybrid`, `rerank`, `kontext`, `gedeckt`, `obersterScore`, `erwartet`, `art`) werden in 3, 5, 6 identisch gelesen; Terminal-Prädikate in 7 und 17 identisch.
